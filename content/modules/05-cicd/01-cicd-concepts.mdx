---
title: "CI/CD: Automating the Path from Code to Production"
description: "Manual deploy pipelines are slow and error-prone. CI/CD automates the build, test, and deploy lifecycle so every code push is safely integrated and deployable."
module: "CI/CD Pipelines"
moduleSlug: "05-cicd"
lessonSlug: "01-cicd-concepts"
order: 1
difficulty: "Foundation"
estimatedMinutes: 20
prerequisites: ["04-sysadmin/02-systemd", "03-docker/07-dockerfile-best-practices"]
tags: ["cicd", "continuous-integration", "continuous-delivery", "continuous-deployment", "pipelines", "automation"]
---

## Overview

Your team ships code by SSH-ing into a server, running `git pull`, crossing fingers, and hoping nothing breaks. It works — until it doesn't. A missing dependency on the server, a test that only runs on the developer's machine, a deployment at 11pm that takes the site down for 20 minutes. These are symptoms of a manual process that doesn't scale.

**CI/CD (Continuous Integration / Continuous Delivery or Deployment)** is the practice of automating the entire journey from code commit to running production software. Every push triggers a pipeline: build the code, run tests, and deploy — automatically, reproducibly, with full visibility into what happened and why.

This lesson explains the concepts. The next two lessons make them concrete with GitHub Actions and a real Docker pipeline.

## How It Works

### The Problem CI/CD Solves

Without automation, software delivery has predictable failure modes:

- **Integration hell:** Developers work in isolation for days or weeks, then merge everything at once. Conflicts and bugs multiply.
- **"Works on my machine":** The server has different software versions, environment variables, or dependencies than the developer's laptop.
- **Manual steps get skipped:** Under pressure, someone skips the test suite. A bug ships.
- **Infrequent deployments are scary:** If you deploy rarely, each deployment is a large, risky event. Teams avoid deploying, which makes each deployment even larger and riskier.

CI/CD solves this by making integration and deployment routine, automated, and small.

### Continuous Integration (CI)

**Continuous Integration** means every code push to the shared repository triggers an automated build and test run. The system validates the code immediately, before anyone else pulls it.

The key properties:
- **Triggered automatically:** No human remembers to run the tests — the system does it on every push.
- **Runs in a clean environment:** The build runs on a fresh machine (not anyone's laptop), with exactly the dependencies specified in the repository.
- **Fails fast and notifies:** If the build or tests fail, the developer is notified immediately — when the change is still fresh in their mind, not three days later.

CI catches integration bugs in minutes instead of days. When every developer integrates their changes multiple times per day against a known-good baseline, the scope of any conflict is tiny.

### Continuous Delivery vs Continuous Deployment

"CD" is used for two related but distinct practices:

**Continuous Delivery:** The pipeline builds and tests every commit, producing a deployment-ready **artifact** (a Docker image, a compiled binary, a release package). Deployment to production is a manual decision — but the artifact is always ready to deploy. The team can ship at any time with confidence.

**Continuous Deployment:** Goes one step further. If the build and tests pass, the artifact is automatically deployed to production. No human approval step. This requires high confidence in the test suite.

Most organizations practice Continuous Delivery. Continuous Deployment is common for SaaS products with strong test coverage and mature monitoring.

### Pipeline Stages

A pipeline is a sequence of stages. Each stage gates the next — if a stage fails, the pipeline stops and reports the failure. No broken code advances.

```
Push → [Build] → [Test] → [Deploy to Staging] → [Deploy to Prod]
         ↓ fail    ↓ fail
       [Notify]  [Notify]
```

**Build:** Compile the code, install dependencies, create the artifact. For a Node.js app with Docker, this means running `npm ci` and `docker build`. For a Go binary, `go build`. If the build fails, there is nothing to test.

**Test:** Run the automated test suite. Unit tests, integration tests, linting. If tests fail, the artifact is not promoted.

**Deploy to Staging:** Deploy the artifact to a staging environment — a copy of production, but not customer-facing. Run smoke tests against the live staging service.

**Deploy to Production:** Deploy the same artifact to production. Because it has already passed build, test, and staging, confidence is high.

### Artifacts: The Output of a Build

An **artifact** is the output of the Build stage — something concrete that can be stored, versioned, and deployed. Artifacts travel through pipeline stages rather than being rebuilt at each step. This guarantees that exactly what was tested is exactly what gets deployed.

Common artifact types:
| Type | Example | Storage |
|------|---------|---------|
| Container image | `ghcr.io/myorg/myapp:a1b2c3d` | Container registry (ghcr.io, Docker Hub) |
| Compiled binary | `myapp-linux-amd64` | Object storage (S3, GCS) |
| Package | `myapp-1.2.3.tar.gz` | Package registry (npm, PyPI) |
| Test report | `junit.xml` | Pipeline artifact storage |

The Docker image tag includes the commit SHA (`a1b2c3d`) — you can always trace a running container back to the exact commit that produced it.

<Callout type="deep-dive" title="Why sysadmin knowledge matters for CI/CD">
When a CI/CD pipeline "deploys," it is managing services on a machine — starting processes, checking health, handling failures. Everything you learned about systemd, logging, and monitoring applies directly.

A deployment step is essentially: stop the old service, pull the new artifact, start the new service, verify the health endpoint. That is systemd lifecycle management plus Docker image management. The pipeline automates what a sysadmin would do manually. When a deployment fails at 3am and wakes you up, you debug it using the same tools: `journalctl`, `systemctl status`, container logs, health check endpoints.

A pipeline is just an automated sysadmin.
</Callout>

### The Feedback Loop: Why Speed Matters

The value of CI/CD is proportional to how fast the feedback loop is. A pipeline that takes 45 minutes to run is better than no pipeline, but not much. A pipeline that reports results in 3 minutes changes how developers work.

When feedback is fast:
- Developers fix bugs while the change is still in working memory
- Broken builds block everyone, so the team fixes them immediately
- Frequent small deployments become normal — the "scary deployment" problem disappears

Pipeline speed is an engineering investment. Caching dependencies, running tests in parallel, and keeping build images small all exist to keep the feedback loop tight.

## Hands-On Exercise

<ExerciseCard
  title="Map Your Manual Process to a CI/CD Pipeline"
  difficulty="Foundation"
  scenario="Your team ships code by SSH-ing into a production server and running git pull. This works for small projects but has caused three incidents this quarter: a missing npm package crashed the app, an untested config change took the site down for 40 minutes, and a developer accidentally deployed from the wrong branch. The CTO wants CI/CD. Map out what a pipeline would look like."
  objective="By the end of this exercise, you will be able to identify CI vs CD stages, name what each pipeline stage does, and explain the artifact concept — all without writing any YAML yet."
  annotated={true}
  steps={[
    {
      step: 1,
      description: "List every manual step currently done when shipping code. Include everything: local test runs, git operations, SSH commands, npm commands, service restarts, health checks.",
      command: "# Write these down: checkout code, npm install, run tests, npm run build, copy files, restart service, verify health",
      annotations: [
        { token: "#", description: "Marks the rest of the line as a comment — the shell ignores it. Used here as a thinking prompt, not a runnable command." }
      ]
    },
    {
      step: 2,
      description: "Classify each step: is it CI (validate the code) or CD (deploy the artifact)? The boundary is the artifact — everything before creating the artifact is CI, everything after is CD.",
      command: "# CI: checkout, install deps, run tests, build. CD: deploy to staging, deploy to prod.",
      annotations: [
        { token: "#", description: "Marks the rest of the line as a comment. Used here to show the classification result — read this as the expected answer to the step above." }
      ]
    },
    {
      step: 3,
      description: "Identify the artifact in your pipeline. For a Node.js Docker app, the artifact is the Docker image. What information should be in the image tag to make it traceable?",
      command: "# Example tag: ghcr.io/myorg/myapp:abc1234 where abc1234 is the commit SHA",
      annotations: [
        { token: "#", description: "Marks the rest of the line as a comment. The example tag format shows how a commit SHA appended after the colon makes any running container traceable to its source code." }
      ]
    },
    {
      step: 4,
      description: "Draw the pipeline stages in order, showing what happens on failure at each stage. What should happen if the tests fail? What should happen if the staging deploy fails?",
      command: "# Push → [Build] → [Test] → [Staging Deploy] → [Prod Deploy]\n# Each stage: fail → notify, stop pipeline",
      annotations: [
        { token: "#", description: "Marks the rest of the line as a comment. The two comment lines together show the pipeline shape and the failure policy: every stage that fails sends a notification and halts the pipeline." }
      ]
    },
    {
      step: 5,
      description: "Identify which of the three incidents would have been caught by the pipeline. The missing npm package: caught in Build (npm ci fails). Wrong branch: caught by trigger (pipeline only runs on main). Untested config change: depends — was it in the test suite?",
      command: "# Incident 1: npm ci would have failed. Incident 3: would depend on test coverage.",
      annotations: [
        { token: "#", description: "Marks the rest of the line as a comment. These are the analysis conclusions — read each incident against the pipeline stages to see which stage would have caught it." }
      ]
    }
  ]}
>
  <ScenarioQuestion
    question="Your team's pipeline takes 45 minutes to run. The CTO says that's fine — at least it's automated. You disagree. Using the three incidents from the scenario, explain specifically why a slow feedback loop is still a problem even when the pipeline is automated."
    answer="The 40-minute config change incident is the clearest example: even with automation, a 45-minute pipeline means the developer has moved on to other work by the time the failure is reported. When the bug alert arrives, they must context-switch back to code they wrote nearly an hour ago, recall what they changed, and debug in a colder mental state. Fast automation (3-5 minutes) means the developer still has the change in working memory when the failure lands — they fix it immediately rather than losing 30 minutes to context reconstruction. The missing npm package bug (Incident 1) is also relevant: a fast build stage would fail and notify within minutes of the push, not after the developer has already closed that mental tab."
  />
  <ScenarioQuestion
    question="You drew the pipeline stages in Step 4. The wrong-branch incident (Incident 2) is caught by the trigger configuration, not by any pipeline stage. How does this illustrate the difference between what CI validates and what the pipeline trigger controls?"
    answer="CI stages (Build, Test) validate whether the code is correct — they check that dependencies install, tests pass, and the artifact can be built. But they cannot prevent deploying from the wrong branch, because that is a question of which code is allowed to enter the pipeline at all. Trigger configuration answers: under what conditions does this pipeline even run? By setting the trigger to run only on pushes to the main branch, the pipeline refuses to build feature branches and developer forks entirely. This is pipeline access control, not code validation. The lesson is that a complete CI/CD system has both: triggers that control what enters the pipeline, and stages that validate what is inside it."
  />
  <VerificationChecklist
    title="Verify Your Understanding"
    items={[
      {
        id: "ci-cd-distinction",
        label: "You can explain the difference between CI (validate code) and CD (deploy artifact)",
        hint: "CI = build + test on every push. CD = the artifact is always deployable (delivery) or automatically deployed (deployment)."
      },
      {
        id: "pipeline-stages",
        label: "You can name the four main pipeline stages in order",
        hint: "Build → Test → Deploy to Staging → Deploy to Production. Each gates the next."
      },
      {
        id: "artifact-concept",
        label: "You understand why artifacts matter — the same built thing goes through all stages",
        hint: "The artifact (Docker image tagged with commit SHA) is built once in CI, then deployed through staging and production. What's tested is what ships."
      },
      {
        id: "feedback-loop",
        label: "You can explain why pipeline speed matters for developer workflow",
        hint: "Fast feedback (3-5 min) means bugs are fixed while the code is fresh. Slow feedback (45 min) means developers have moved on and context-switching is expensive."
      }
    ]}
  />
</ExerciseCard>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "CI/CD Concepts",
      items: [
        { command: "Continuous Integration (CI)", description: "Every push triggers automated build + test. Catches integration bugs immediately." },
        { command: "Continuous Delivery (CD)", description: "Every passing build produces a deployment-ready artifact. Deployment to production is a manual decision." },
        { command: "Continuous Deployment (CD)", description: "Passing builds are automatically deployed to production. Requires high test coverage and monitoring confidence." },
        { command: "Pipeline", description: "Ordered sequence of stages (build → test → deploy). Each stage gates the next — failure stops the pipeline." },
        { command: "Artifact", description: "The output of a build stage. Docker image, compiled binary, or package. Tagged with commit SHA for traceability." },
        { command: "Feedback loop", description: "Time from push to result. Faster loops (3-5 min) change developer behavior — bugs fixed while change is fresh." },
      ]
    },
    {
      title: "Pipeline Stage Purposes",
      items: [
        { command: "Build stage", description: "Install dependencies, compile code, create the artifact. Fails here = nothing to test." },
        { command: "Test stage", description: "Run automated tests (unit, integration, lint). Fails here = artifact not promoted." },
        { command: "Staging deploy", description: "Deploy artifact to production-like environment, run smoke tests against live service." },
        { command: "Production deploy", description: "Deploy the same artifact that passed all previous stages. High confidence because it's already been tested." },
      ]
    },
    {
      title: "Common Artifact Tags",
      items: [
        { command: "ghcr.io/org/app:abc1234", description: "Docker image tagged with commit SHA — traceable to exact code revision", example: "docker pull ghcr.io/myorg/myapp:abc1234" },
        { command: "ghcr.io/org/app:latest", description: "Points to most recent build — convenient but not reproducible for rollbacks" },
        { command: "ghcr.io/org/app:v1.2.3", description: "Semantic version tag — used alongside SHA tag for release tracking" },
      ]
    }
  ]}
/>

export const quiz = [
  {
    id: "cicd-q1",
    question: "What does Continuous Integration (CI) do when a developer pushes code?",
    options: [
      "Automatically deploys the code to production",
      "Triggers an automated build and test run in a clean environment",
      "Creates a deployment artifact and waits for manual approval",
      "Merges the code into a staging branch for review"
    ],
    correctIndex: 1,
    explanation: "Continuous Integration triggers an automated build and test run on every push, running in a clean environment rather than a developer laptop. This catches integration bugs immediately, while the change is still fresh in the developer's mind.",
  },
  {
    id: "cicd-q2",
    question: "What is the key difference between Continuous Delivery and Continuous Deployment?",
    options: [
      "Continuous Delivery runs tests automatically; Continuous Deployment does not",
      "Continuous Delivery deploys to staging only; Continuous Deployment deploys to production",
      "Continuous Delivery requires a manual decision to deploy to production; Continuous Deployment deploys automatically",
      "Continuous Delivery produces Docker images; Continuous Deployment produces compiled binaries"
    ],
    correctIndex: 2,
    explanation: "In Continuous Delivery, every passing build produces a deployment-ready artifact but deploying to production is still a manual decision. Continuous Deployment removes that human step — if the build and tests pass, the artifact is automatically deployed to production.",
  },
  {
    id: "cicd-q3",
    question: "In a CI/CD pipeline, what happens when a stage fails?",
    options: [
      "The pipeline retries the failed stage up to three times before stopping",
      "The pipeline skips the failed stage and continues to the next one",
      "The pipeline stops and notifies — no broken code advances to later stages",
      "The pipeline marks the failure as a warning and completes all remaining stages"
    ],
    correctIndex: 2,
    explanation: "Each pipeline stage gates the next — if a stage fails, the pipeline stops immediately and reports the failure. This prevents broken code from advancing to later stages such as staging or production deployment.",
  },
  {
    id: "cicd-q4",
    question: "What is a pipeline artifact?",
    options: [
      "A log file generated when a pipeline stage fails",
      "The output of the Build stage — a Docker image, binary, or package — that travels through all pipeline stages",
      "A test report that documents which tests passed and failed",
      "A YAML configuration file that defines the pipeline structure"
    ],
    correctIndex: 1,
    explanation: "An artifact is the concrete output of the Build stage — a Docker image, compiled binary, or release package. Artifacts travel through pipeline stages rather than being rebuilt at each step, which guarantees that exactly what was tested is exactly what gets deployed.",
  },
  {
    id: "cicd-q5",
    question: "Why does the lesson say a Docker image tag should include the commit SHA?",
    options: [
      "Because Docker Hub requires SHA tags for all images",
      "Because the SHA makes the tag shorter and easier to type",
      "Because it allows you to trace any running container back to the exact commit that produced it",
      "Because the SHA prevents the image from being overwritten by a newer build"
    ],
    correctIndex: 2,
    explanation: "Tagging a Docker image with the commit SHA (for example, ghcr.io/myorg/myapp:a1b2c3d) creates a traceable link between a running container and the exact code revision that was built into it. This is critical for incident investigation and rollback.",
  },
  {
    id: "cicd-q6",
    question: "Which problem does CI/CD directly solve by making integrations small and frequent?",
    options: [
      "Works-on-my-machine issues caused by different software versions",
      "Integration hell caused by developers merging large, long-lived branches all at once",
      "Missing environment variables that differ between laptop and server",
      "Infrequent deployments that create large, high-risk release events"
    ],
    correctIndex: 1,
    explanation: "Integration hell occurs when developers work in isolation for days or weeks and then merge large code changes simultaneously, producing cascading conflicts and bugs. CI requires integrating changes multiple times per day against a known-good baseline, keeping each merge small and the scope of any conflict tiny.",
  },
  {
    id: "cicd-q7",
    question: "Why does pipeline speed directly affect developer behavior?",
    options: [
      "Faster pipelines are cheaper to run on CI infrastructure",
      "Slow pipelines block the deployment system and prevent merges",
      "Fast feedback (3-5 minutes) means developers fix bugs while the change is still in working memory",
      "Pipeline speed determines how many builds can run in parallel"
    ],
    correctIndex: 2,
    explanation: "When a pipeline reports results in 3-5 minutes, developers are still mentally focused on the change and can fix bugs immediately. A 45-minute pipeline means the developer has moved on to other work; they must context-switch back to code they wrote nearly an hour ago, making debugging slower and more expensive.",
  },
  {
    id: "cicd-q8",
    question: "In the pipeline stage sequence described in this lesson, what does the Staging Deploy stage do?",
    options: [
      "Runs the automated test suite against the source code",
      "Compiles the code and installs dependencies to create the artifact",
      "Deploys the artifact to a production-like environment and runs smoke tests against the live service",
      "Sends a notification to the developer that the build passed"
    ],
    correctIndex: 2,
    explanation: "The Staging Deploy stage deploys the artifact to a staging environment — a copy of production that is not customer-facing — and runs smoke tests against the live staging service. This validates the artifact in a real environment before it reaches production.",
  },
]
