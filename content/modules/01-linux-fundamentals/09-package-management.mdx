---
title: "Package Management"
description: "apt, repositories, dependencies — installing and managing software on Linux"
module: "Linux Fundamentals"
moduleSlug: "01-linux-fundamentals"
lessonSlug: "09-package-management"
order: 9
difficulty: "Intermediate"
estimatedMinutes: 20
prerequisites: ["01-linux-fundamentals/06-shell-fundamentals"]
tags: ["apt", "dpkg", "packages", "repositories", "dependencies"]
---

## Overview

A Docker build failed because `apt-get install` couldn't find a package. Was the package renamed? Is the repository missing? Is the index stale? Understanding how repositories, package versions, and dependency resolution work tells you whether to add a PPA, pin a version, run `apt-get update` first, or look for an alternative package. The difference between a 10-second fix and an hour of confusion is understanding the two-layer architecture of Linux package management.

## How It Works

### What is a Package

A package is an archive containing:
- **Binaries**: the compiled executables (e.g., `/usr/bin/curl`)
- **Libraries**: shared `.so` files the binaries depend on
- **Configuration files**: defaults in `/etc/` or `/usr/share/`
- **Metadata**: name, version, description, maintainer, and — critically — the **dependency list** (other packages this one requires)

On Debian-based systems (Ubuntu, Debian, Kali), packages use the `.deb` format. Each `.deb` is essentially a tar archive with a standardized metadata structure.

### Two-Layer Architecture: dpkg and apt

Debian-based Linux uses two layers of package management that solve different problems:

**dpkg (low level):** Installs, removes, and queries individual `.deb` files on disk. It does NOT fetch packages from the internet and does NOT resolve dependencies. If you give it a `.deb` with unsatisfied dependencies, it will fail and tell you what's missing.

**apt (high level):** Knows about repositories (catalogs of packages on the internet), resolves dependencies automatically, and calls `dpkg` to do the actual installation. Think of `dpkg` as manually placing a file; `apt` is an app store that figures out everything you need first.

```
You type: apt-get install nginx
         ↓
apt:     fetch package index → resolve dependencies → download .deb files
         ↓
dpkg:    install each .deb file onto the filesystem
```

### Repositories: Where Packages Come From

`apt` fetches packages from **repositories** — servers that host curated collections of packages. The repository list lives at `/etc/apt/sources.list` and in files under `/etc/apt/sources.list.d/`.

A single repository entry looks like:

```
deb http://archive.ubuntu.com/ubuntu jammy main restricted universe
```

Breaking this apart:

| Component | Value | Meaning |
|-----------|-------|---------|
| `deb` | `deb` | Binary packages (use `deb-src` for source code) |
| URL | `http://archive.ubuntu.com/ubuntu` | The repository server |
| Codename | `jammy` | Ubuntu 22.04 LTS release name |
| Sections | `main restricted universe` | Which package categories to include |

**Repository sections:**

| Section | Contents | License status |
|---------|----------|----------------|
| `main` | Canonical-supported open-source software | Free and open-source |
| `restricted` | Drivers and firmware with restricted licenses | Not fully open-source |
| `universe` | Community-maintained open-source packages | Free, but unsupported |
| `multiverse` | Software with non-free licenses (codecs, etc.) | Non-free |

### apt-get update: Fetching the Index

Running `apt-get install` without first running `apt-get update` is one of the most common causes of "package not found" errors. Here's why:

`apt-get update` downloads the **package index** — a metadata file from each repository listing every available package, version, and download URL. It does NOT install anything. It just updates apt's local catalog of what's available.

<Callout type="warning" title="Always Run apt-get update Before apt-get install in Dockerfiles">
Docker caches each RUN layer. If you have:

```dockerfile
RUN apt-get update
RUN apt-get install -y curl
```

Docker will cache the first layer and never re-run `apt-get update` in subsequent builds. Over time, the cached index becomes stale and packages "disappear" (because the index no longer matches the repository). The correct pattern is:

```dockerfile
RUN apt-get update && apt-get install -y curl
```

A single RUN layer means the index is always fresh when the install runs. This is the official Docker best practice.
</Callout>

### apt-get install: Dependency Resolution

When you run `apt-get install nginx`, apt performs these steps:

1. Looks up `nginx` in the local package index (from the last `apt-get update`)
2. Reads nginx's dependency list from the metadata
3. Recursively resolves all dependencies (nginx depends on libssl, which depends on libc, etc.)
4. Downloads all required `.deb` files that aren't already installed
5. Calls `dpkg` to install each `.deb` in dependency order

The `-y` flag skips the "Do you want to continue? [Y/n]" prompt — essential for scripts and Dockerfiles:

```bash
apt-get install -y nginx curl git
```

<Callout type="tip" title="--no-install-recommends Keeps Docker Images Small">
By default, `apt-get install` also installs "recommended" packages — related software that isn't strictly required. In a Docker container, these are often unnecessary. Adding `--no-install-recommends` can significantly reduce image size:

```bash
apt-get install -y --no-install-recommends nginx
```

For minimal containers (alpine-based Dockerfiles, or slim variants like `ubuntu:22.04`), this flag is especially important.
</Callout>

### Exploring Packages: apt-cache

Before installing, you often want to search for the right package name or understand what it contains:

```bash
# Search for packages by keyword
apt-cache search nginx

# Show detailed info about a package
apt-cache show nginx

# List what packages a package depends on
apt-cache depends nginx

# List packages that depend on this package
apt-cache rdepends libc6

# List available versions of a package
apt-cache policy nginx
```

`apt-cache` reads from the local index (built by `apt-get update`). It never makes network requests.

### dpkg: Low-Level Package Queries

Once packages are installed, `dpkg` is your tool for inspecting them:

```bash
# List all installed packages (long list — pipe through less or grep)
dpkg -l
dpkg -l | grep nginx           # find specific package

# Check if a specific package is installed
dpkg -s nginx

# List all files installed by a package
dpkg -L nginx

# Find which package owns a specific file
dpkg -S /usr/bin/curl

# Install a .deb file directly (no dependency resolution)
dpkg -i mypackage.deb
```

`dpkg -L` is particularly useful when you need to know where a package put its binaries, config files, or library files.

### Version Pinning

In production environments, reproducibility matters. Installing the latest version of a package every time can cause unexpected behavior changes. Version pinning ensures every deployment gets exactly the same package version:

```bash
# Install a specific version
apt-get install -y nginx=1.18.0-0ubuntu1.4

# List available versions to find the exact version string
apt-cache policy nginx
```

For more complex pinning (like "always prefer packages from a specific repository"), use `/etc/apt/preferences`. This is advanced but important for maintaining stable server configurations.

### Removing Packages

```bash
# Remove a package (keeps config files in /etc/)
apt-get remove nginx

# Remove a package AND its config files (purge)
apt-get purge nginx

# Remove orphaned dependencies (packages nothing else depends on)
apt-get autoremove

# Combined: remove package + config + clean up orphans
apt-get purge nginx && apt-get autoremove
```

The difference between `remove` and `purge` matters when re-installing: `purge` gives you a completely clean slate; `remove` preserves your configuration so reinstallation picks up where you left off.

<Callout type="deep-dive" title="How the Dependency Resolver Works">
When `apt-get install package` runs, apt uses a constraint-satisfaction algorithm (in the `apt` library, derived from the original "boolean satisfiability" approach) to find a consistent set of package versions that satisfies all dependencies simultaneously.

The algorithm:

1. Start with the requested package and its stated dependencies
2. For each dependency, find candidate versions in the index
3. Recursively expand: each candidate may introduce new dependencies
4. Check for conflicts: two packages cannot both be installed if they conflict (e.g., `apache2` and `nginx` both want port 80 — but they don't actually conflict at the package level, only at runtime)
5. Apply version constraints: if package A needs `libssl >= 1.1.1` and package B needs `libssl < 1.1.0`, there is no satisfying solution — apt reports a conflict

When apt says "The following packages have unmet dependencies," it means the constraint solver found no valid assignment. Common causes: stale index (run `apt-get update`), conflicting requirements, or a package only available in a repository not in `sources.list`.
</Callout>

**Forward reference:** In the Docker module (Module 3), you'll see why `apt-get update && apt-get install -y` must be in the same RUN command. Docker layer caching makes a separate `apt-get update` step permanently stale after the first build — the install layer sees the old (cached) index and fails to find new packages.

<TerminalBlock
  title="bash — package management workflow"
  lines={[
    { type: 'comment', content: 'Update index, then install curl and check what files it installs' },
    { type: 'command', content: 'apt-get update -qq && apt-get install -y --no-install-recommends curl' },
    { type: 'output', content: 'Reading package lists...' },
    { type: 'output', content: 'Building dependency tree...' },
    { type: 'output', content: 'The following NEW packages will be installed: curl libcurl4' },
    { type: 'output', content: 'Need to get 194 kB of archives.' },
    { type: 'command', content: 'dpkg -L curl' },
    { type: 'output', content: '/usr/bin/curl' },
    { type: 'output', content: '/usr/share/doc/curl/changelog.Debian.gz' },
    { type: 'output', content: '/usr/share/man/man1/curl.1.gz' },
    { type: 'comment', content: 'Check what curl depends on' },
    { type: 'command', content: 'apt-cache depends curl' },
    { type: 'output', content: 'curl' },
    { type: 'output', content: '  Depends: libc6' },
    { type: 'output', content: '  Depends: libcurl4 (= 7.81.0-1ubuntu1.20)' },
    { type: 'output', content: '  Depends: zlib1g' },
  ]}
/>

## Hands-On Exercise

<ExerciseCard
  title="Set Up a Development Server Package Environment"
  difficulty="Intermediate"
  scenario="You are a DevOps engineer provisioning a new development server. You need to install specific tools, verify what was installed, understand the dependency graph, and practice the common troubleshooting pattern of running apt-get update before installs — because the image was built weeks ago and has a stale package index."
  objective="By the end of this exercise, you will be able to update the package index, search for and install packages, inspect what files a package installs, examine package dependencies, and explain the difference between apt-get remove and purge."
  steps={[
    {
      step: 1,
      description: "Start a fresh Ubuntu container to simulate a stale server environment.",
      command: "docker run -it --rm ubuntu:22.04 /bin/bash"
    },
    {
      step: 2,
      description: "First, try installing tree WITHOUT running apt-get update first. Observe the output.",
      command: "apt-get install -y tree"
    },
    {
      step: 3,
      description: "Now run apt-get update and then install tree. Note the difference.",
      command: "apt-get update && apt-get install -y tree"
    },
    {
      step: 4,
      description: "Verify tree installed successfully and run it.",
      command: "tree /etc/apt"
    },
    {
      step: 5,
      description: "Use dpkg -L to see every file the tree package installed on this system.",
      command: "dpkg -L tree"
    },
    {
      step: 6,
      description: "Use apt-cache show to read the full metadata for tree.",
      command: "apt-cache show tree"
    },
    {
      step: 7,
      description: "Show the dependency chain for curl.",
      command: "apt-cache depends curl"
    },
    {
      step: 8,
      description: "Show the sources.list file to understand which repositories are configured.",
      command: "cat /etc/apt/sources.list"
    },
    {
      step: 9,
      description: "Remove tree with apt-get remove, then check if the binary is still present.",
      command: "apt-get remove -y tree && which tree"
    }
  ]}
>
  <ScenarioQuestion
    question="You tried apt-get install tree without running apt-get update first and it failed. In a Dockerfile, why must apt-get update and apt-get install be in the same RUN instruction instead of separate RUN layers?"
    answer="Docker caches each RUN layer as an image layer. If apt-get update and apt-get install are in separate RUN instructions, Docker caches the apt-get update result as its own layer. On subsequent builds, Docker sees that the apt-get update layer has not changed and skips re-running it — the local package index is never refreshed. Over time (days to weeks), the cached index becomes stale: packages get new versions, old versions are removed from repositories, and new package names appear. When apt-get install runs against the stale index, it either cannot find the package or installs an outdated version. Combining them in a single RUN instruction means the index is always fresh when the install runs, because both commands execute together or not at all — there is no cached intermediate layer to skip."
  />
  <ScenarioQuestion
    question="After running dpkg -L tree, you see it installed files in /usr/bin/, /usr/share/doc/, and /usr/share/man/. You then ran apt-get remove tree and the binary disappeared. What files, if any, did apt-get remove leave behind, and when would you use apt-get purge instead?"
    answer="apt-get remove uninstalls the binary and library files but deliberately preserves the package's configuration files (files in /etc/) and the documentation in /usr/share/doc/. For the tree package specifically, there are no configuration files in /etc/, so remove and purge produce the same result — but for packages like nginx or postgresql that install configuration in /etc/, remove keeps those files intact. Use apt-get purge when you want a completely clean slate, as if the package was never installed — useful when reinstalling a service that was previously misconfigured, or when you want to guarantee no stale configuration affects a fresh install. Note that purge only removes the package's own config files; it does not remove files the application created itself at runtime (like database data in /var/lib/postgresql/)."
  />
  <VerificationChecklist
    title="Verify Your Work"
    items={[
      {
        id: "pkg-01",
        label: "You installed tree using apt-get after running apt-get update",
        hint: "Run: apt-get update && apt-get install -y tree && tree --version"
      },
      {
        id: "pkg-02",
        label: "You can list files installed by the tree package using dpkg -L",
        hint: "Run: dpkg -L tree — you should see /usr/bin/tree and documentation files"
      },
      {
        id: "pkg-03",
        label: "You can show the dependencies of the curl package",
        hint: "Run: apt-cache depends curl — lists libc6, libcurl4, and others"
      },
      {
        id: "pkg-04",
        label: "You can explain the difference between apt-get remove and apt-get purge",
        hint: "remove keeps config files; purge deletes everything including /etc/ config files"
      },
      {
        id: "pkg-05",
        label: "You can read /etc/apt/sources.list and identify the repository URL and codename",
        hint: "Run: cat /etc/apt/sources.list — look for 'deb http://...' lines with the Ubuntu codename"
      }
    ]}
  />
</ExerciseCard>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "apt-get",
      items: [
        { command: "apt-get update", description: "Fetch package index from all configured repositories" },
        { command: "apt-get install -y pkg", description: "Install package (non-interactive)", example: "apt-get install -y nginx curl" },
        { command: "apt-get install -y pkg=version", description: "Install specific version", example: "apt-get install -y nginx=1.18.0-0ubuntu1.4" },
        { command: "apt-get install -y --no-install-recommends pkg", description: "Install without recommended extras (keep images small)" },
        { command: "apt-get remove pkg", description: "Uninstall package (preserve config files)" },
        { command: "apt-get purge pkg", description: "Uninstall package and delete all config files" },
        { command: "apt-get autoremove", description: "Remove orphaned dependencies no longer needed" }
      ]
    },
    {
      title: "apt-cache",
      items: [
        { command: "apt-cache search keyword", description: "Search for packages by name or description" },
        { command: "apt-cache show pkg", description: "Display full metadata for a package" },
        { command: "apt-cache depends pkg", description: "List packages this package depends on" },
        { command: "apt-cache rdepends pkg", description: "List packages that depend on this package" },
        { command: "apt-cache policy pkg", description: "Show available versions and priorities" }
      ]
    },
    {
      title: "dpkg",
      items: [
        { command: "dpkg -l", description: "List all installed packages" },
        { command: "dpkg -l | grep pkg", description: "Check if a package is installed" },
        { command: "dpkg -s pkg", description: "Show status and metadata of an installed package" },
        { command: "dpkg -L pkg", description: "List all files installed by a package" },
        { command: "dpkg -S /path/to/file", description: "Find which package owns a file" },
        { command: "dpkg -i package.deb", description: "Install a .deb file directly (no dependency resolution)" }
      ]
    },
    {
      title: "Key Files",
      items: [
        { command: "/etc/apt/sources.list", description: "Main repository configuration file" },
        { command: "/etc/apt/sources.list.d/", description: "Additional repository configs (one file per repo)" },
        { command: "/var/cache/apt/archives/", description: "Downloaded .deb package cache" },
        { command: "/var/lib/apt/lists/", description: "Package index files downloaded by apt-get update" }
      ]
  ]}
/>

export const quiz = [
  {
    id: 'pkg-q1',
    question: 'A Dockerfile has two separate RUN layers: "RUN apt-get update" and "RUN apt-get install -y curl". The build works today but fails two weeks later with "Unable to locate package curl". What is the most likely cause?',
    options: [
      'curl was removed from Ubuntu\'s repositories permanently',
      'Docker cached the apt-get update layer; the install layer ran against a stale cached index that no longer matches the current repository',
      'apt-get install requires the -y flag, which was not specified',
      'The Ubuntu base image was updated and is incompatible with the existing RUN layers',
    ],
    correctIndex: 1,
    explanation: 'Docker caches each RUN layer. Once apt-get update is cached, Docker never re-runs it in subsequent builds. Over weeks, the cached package index becomes stale while the actual repository changes. The fix is to combine them in a single RUN: "RUN apt-get update && apt-get install -y curl" so both execute together and the index is always fresh.',
  },
  {
    id: 'pkg-q2',
    question: 'What is the functional difference between dpkg and apt?',
    options: [
      'dpkg installs from repositories over the internet; apt installs from local .deb files only',
      'dpkg is low-level and installs individual .deb files without resolving dependencies; apt knows about repositories and resolves dependencies automatically',
      'dpkg is for Debian systems; apt is for Ubuntu systems specifically',
      'dpkg installs system packages; apt installs user-space applications only',
    ],
    correctIndex: 1,
    explanation: 'dpkg operates on individual .deb archive files already on disk. It installs them but cannot fetch packages from the internet and does not resolve dependencies — if a required library is missing, dpkg fails and tells you what is needed. apt is a higher-level tool that queries repositories, resolves the full dependency tree, downloads everything needed, then calls dpkg to install each package in order.',
  },
  {
    id: 'pkg-q3',
    question: 'You want to find out which package owns the file /usr/bin/curl. Which command gives you this information?',
    options: [
      'apt-cache search curl',
      'apt-get show curl',
      'dpkg -S /usr/bin/curl',
      'dpkg -L curl',
    ],
    correctIndex: 2,
    explanation: 'dpkg -S (search) takes a file path and prints the package that installed it. dpkg -L (list) does the reverse: given a package name, it lists all files that package installed. apt-cache search searches package descriptions by keyword and apt-get show is not a valid command (use apt-cache show).',
  },
  {
    id: 'pkg-q4',
    question: 'You remove the nginx package with "apt-get remove nginx" and then reinstall it. Your old configuration in /etc/nginx/ is still present. Why, and when would you use "apt-get purge" instead?',
    options: [
      'apt-get remove does not actually uninstall anything; only purge removes the package',
      'apt-get remove uninstalls binaries but deliberately preserves config files in /etc/; use purge when you want a completely clean slate with no configuration artifacts',
      'The configuration was preserved because /etc/ is a read-only filesystem',
      'apt-get remove only works for packages in the "main" repository; use purge for all repositories',
    ],
    correctIndex: 1,
    explanation: 'apt-get remove uninstalls a package\'s binaries and libraries but intentionally keeps configuration files in /etc/. This makes reinstallation pick up your previous settings automatically. apt-get purge additionally deletes all configuration files, giving you a fresh installation as if the package had never been installed. Use purge when you need to reset a misconfigured service to defaults.',
  },
  {
    id: 'pkg-q5',
    question: 'What does apt-cache search do, and when should you use it instead of apt-get install?',
    options: [
      'apt-cache search downloads and installs the best matching package',
      'apt-cache search queries the local package index to find packages by name or description, without installing anything',
      'apt-cache search checks the internet for the latest package versions',
      'apt-cache search lists only the packages currently installed on the system',
    ],
    correctIndex: 1,
    explanation: 'apt-cache search reads only from the local package index (built by apt-get update) and never makes network requests or installs anything. Use it to discover the correct package name before installing — for example, searching "json parser" might reveal several candidate packages. It is the exploration tool; apt-get install is the action tool.',
  },
  {
    id: 'pkg-q6',
    question: 'A production deployment script installs packages without version pinning. Two months later a deployment installs a different package version with a breaking API change. What command would you use to install a specific version, and where do you find the available version strings?',
    options: [
      'apt-get install nginx@1.18.0; use apt-cache search to list versions',
      'apt-get install nginx=1.18.0-0ubuntu1.4; use apt-cache policy nginx to list available versions and their exact strings',
      'apt-get install --pin nginx=1.18.0; use dpkg -l to list installed versions',
      'dpkg -i nginx_1.18.0.deb; use apt-get versions nginx to find available versions',
    ],
    correctIndex: 1,
    explanation: 'Version pinning uses the = separator: apt-get install nginx=1.18.0-0ubuntu1.4. The exact version string (including the package revision suffix) must match precisely. apt-cache policy nginx displays all available versions with their exact strings and which repository each comes from, making it the right tool to find the pinning string to use.',
  },
  {
    id: 'pkg-q7',
    question: 'The apt dependency resolver reports "The following packages have unmet dependencies" and refuses to install a package. What are the two most common causes of this error?',
    options: [
      'The package file is corrupted, and the disk is full',
      'The stale local package index (fix: run apt-get update), or conflicting version requirements between two packages that cannot be satisfied simultaneously',
      'The package requires root privileges, and the user has insufficient permissions',
      'The package is already installed in a different version, and /etc/apt/sources.list is empty',
    ],
    correctIndex: 1,
    explanation: 'The two most common causes are: (1) a stale index where the local catalog says a dependency exists but the repository has moved on — run apt-get update to refresh; (2) genuinely conflicting requirements where package A needs libssl >= 1.1.1 but package B needs libssl < 1.1.0, making no valid solution possible. The error message lists the specific missing or conflicting packages to help diagnose which case you have.',
  },
  {
    id: 'pkg-q8',
    question: 'What does "apt-get install -y --no-install-recommends nginx" do differently compared to "apt-get install -y nginx", and when is it beneficial?',
    options: [
      'It skips the package index check, making installation faster',
      'It omits recommended (non-required) packages that would otherwise also be installed, reducing the total footprint',
      'It installs nginx without any dependencies, which may break functionality',
      'It suppresses all output so the install runs silently in the background',
    ],
    correctIndex: 1,
    explanation: 'By default, apt also installs "Recommended" packages — related software that is not strictly required but commonly useful. In Docker containers or minimal servers, these recommendations often add significant size without benefit. --no-install-recommends skips them, installing only strictly declared dependencies, which can substantially reduce container image size.',
  },
]
