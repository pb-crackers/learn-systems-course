---
title: "Log Aggregation: Centralizing Logs from Every Service"
description: "From scattered log files to centralized search — how Loki and ELK collect, store, and query logs at scale"
module: "Monitoring & Observability"
moduleSlug: "08-monitoring"
lessonSlug: "04-log-aggregation"
order: 4
difficulty: "Intermediate"
estimatedMinutes: 25
prerequisites: ["08-monitoring/03-grafana"]
tags: ["logs", "loki", "promtail", "elk", "logql", "centralized-logging", "log-aggregation"]
---

## Overview

In the Linux Fundamentals module, you read log files with `cat`, `grep`, and `tail`. You could watch a single file in real time with `tail -f /var/log/nginx/access.log`. That works when you have one server.

When you have 50 containers across 10 hosts, `tail -f` breaks down immediately. Which host? Which container? Logs from the frontend, API, database, and cache service are scattered across the filesystem of every machine. If the API crashes at 3 AM, you need to find the error quickly — not SSH into ten hosts and search logs manually.

Centralized logging solves this: collect logs from every container and host, ship them to a single storage system, and make them searchable from one place.

## How It Works

The centralized logging pipeline has four stages:

```
Collect → Ship → Store → Query

[App containers] → [Log agent (Promtail/Logstash)] → [Storage (Loki/Elasticsearch)] → [UI (Grafana/Kibana)]
```

Each stage has distinct responsibilities:
- **Collect**: A log agent runs on each host, tails log files or reads from the Docker socket
- **Ship**: The agent sends log lines (with labels/tags) to a central store via HTTP or a binary protocol
- **Store**: The central store indexes and compresses logs for fast retrieval
- **Query**: A query UI lets you search, filter, and visualize logs across all hosts and services

### The ELK Stack

**ELK** stands for Elasticsearch, Logstash, and Kibana — the most established centralized logging stack.

- **Elasticsearch**: Stores and indexes logs. Every word in every log line is indexed for full-text search. This makes queries like "find all logs containing the string 'NullPointerException'" extremely fast.
- **Logstash**: Ingests, transforms, and routes logs. A pipeline with input (read from file, TCP, HTTP), filter (parse, transform, enrich), and output (send to Elasticsearch) stages.
- **Kibana**: Visualizes logs stored in Elasticsearch. Provides a search UI, dashboards, and the KQL query language.

**ELK strengths:** Powerful full-text search, mature ecosystem with 10+ years of production use, extensive plugin ecosystem for parsing structured and unstructured logs.

**ELK tradeoffs:** Elasticsearch requires significant resources — a minimum of 2 GB heap (often 4-8 GB in production), complex JVM tuning, and expertise to operate at scale. A three-node Elasticsearch cluster for high availability is a substantial infrastructure commitment.

ELK is a conceptual reference for this course. For local learning labs, the resource requirements make ELK impractical on a developer laptop.

### Loki + Promtail + Grafana

Loki is Grafana Labs' lightweight alternative to Elasticsearch, designed specifically for log aggregation. It makes a different architectural trade-off: **Loki indexes labels, not log content.**

**The Loki model:**

1. **Promtail** runs on each host as a DaemonSet (Kubernetes) or Docker container. It tails log files and Docker container logs, attaches labels (job name, container name, host), and ships log lines to Loki.

2. **Loki** stores logs indexed only by label set. The labels `{job="app", container="web", host="node-1"}` form the index. The actual log content is compressed and stored but NOT word-indexed.

3. To query logs, you first select a log stream by labels, then Loki greps through that stream for text matches:
   ```
   {job="app"} |= "error"
   ```
   This means: "Find all log lines from the `job=app` stream that contain the string 'error'."

**LogQL** is Loki's query language. It starts with a log stream selector in `{}` and optionally chains filter expressions:

```logql
# Select all logs from the app container
{container=~"monitoring-app.*"}

# Filter to only error lines
{container=~"monitoring-app.*"} |= "error"

# Count log volume per minute
count_over_time({container=~"monitoring-app.*"}[5m])

# Parse JSON logs and filter by field
{container=~"monitoring-app.*"} | json | level="error"
```

**Loki strengths:** Lightweight (no JVM, no full-text index), integrates with Grafana you already have, uses the same label model as Prometheus (labels on log streams mirror metric labels), 10x less storage than Elasticsearch for the same log volume.

**Loki tradeoffs:** No full-text index means "find any log containing 'NullPointerException' across all services" requires selecting a broad stream first, then scanning. For narrow, label-targeted queries this is fast. For broad text searches across unlabeled data, it is slower than Elasticsearch.

### Loki Configuration

The Loki server configuration for a local development setup:

```yaml
# loki/loki-config.yml
auth_enabled: false

server:
  http_listen_port: 3100

ingester:
  lifecycler:
    ring:
      kvstore:
        store: inmemory
      replication_factor: 1

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /loki/tsdb-index
    cache_location: /loki/tsdb-cache
  filesystem:
    directory: /loki/chunks

limits_config:
  reject_old_samples: true
  reject_old_samples_max_age: 168h
```

Configuration sections:
- `auth_enabled: false` — single-tenant mode for development (no tenant ID required in requests)
- `ingester.lifecycler.ring.kvstore.store: inmemory` — for development only; production uses consul or etcd for distributed coordination
- `schema_config` — defines the storage schema: TSDB store (the current recommended index type), filesystem storage for chunk data
- `storage_config.filesystem.directory` — where log chunks are stored on disk
- `limits_config.reject_old_samples_max_age: 168h` — reject log lines older than 7 days (prevents accidentally ingesting old historical logs)

### Promtail Configuration

Promtail is the log shipping agent that collects logs and sends them to Loki:

```yaml
# promtail/promtail-config.yml
server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: system
    static_configs:
      - targets:
          - localhost
        labels:
          job: varlogs
          __path__: /var/log/*log

  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        target_label: container
```

Promtail connects to the Docker socket to discover running containers and tail their logs. The `relabel_configs` section extracts the container name from Docker metadata and sets it as the `container` label on log lines. This is how `{container=~"monitoring-app.*"}` in a LogQL query finds your app's logs.

<Callout type="deep-dive" title="Loki's Key Design Decision: Index Labels, Not Content">
Elasticsearch indexes every word in every log line — this is called an inverted index. If you have 1 billion log lines, Elasticsearch creates a mapping from every word to the log lines containing it. This makes text search fast but requires enormous storage and RAM for the index itself.

Loki only indexes the label set: `{job="app", container="web", host="node-1"}`. The actual log content is stored compressed but not indexed. To search for a word, Loki first selects the matching label stream, then scans through only the relevant compressed chunks.

Result: Loki uses roughly 10x less storage than Elasticsearch for the same log volume. The trade-off is that broad text searches across unlabeled data are slower. But in well-structured environments where logs are properly labeled by service, Loki queries are fast because the label selection narrows the data significantly before the text scan begins.

This is why the Prometheus and Loki label models are intentionally consistent — a metric with `{job="app", instance="web-1"}` and logs with `{job="app", container="web-1"}` use the same label vocabulary, making correlation between metrics and logs natural.
</Callout>

<Callout type="tip" title="Starting the Monitoring Stack with Loki">
The Docker Compose monitoring stack in `docker/monitoring/` includes a Loki + Promtail configuration. Start the full stack including log aggregation using the Loki profile:

```bash
docker compose --profile loki up -d
```

Once running, add Loki as a data source in Grafana (Settings > Data Sources > Add > Loki, URL: `http://loki:3100`). Then go to Explore, select the Loki data source, and write LogQL queries to see your container logs.
</Callout>

## Hands-On Exercise

<ExerciseCard
  title="Set Up Centralized Logging with Loki"
  difficulty="Intermediate"
  scenario="Your Docker Compose application generates logs from multiple containers. Instead of running docker logs on each container individually, set up centralized logging so all logs are searchable from one place in Grafana."
  objective="By the end of this exercise, you will be able to start a Loki stack, write LogQL queries to filter logs by container, and use text filtering to find error lines."
  steps={[
    {
      step: 1,
      description: "Navigate to the monitoring directory and start the monitoring stack with the Loki profile enabled.",
      command: "cd docker/monitoring && docker compose --profile loki up -d"
    },
    {
      step: 2,
      description: "Wait for Loki to start and become healthy. Loki takes a few seconds to initialize its storage.",
      command: "sleep 15 && curl -s http://localhost:3100/ready && echo 'Loki is ready'"
    },
    {
      step: 3,
      description: "Open Grafana at localhost:3001. Navigate to Settings (gear icon) > Data Sources > Add data source. Select Loki.",
      command: "open http://localhost:3001"
    },
    {
      step: 4,
      description: "In the Loki data source config, set URL to http://loki:3100 (the Docker service name, not localhost). Click Save & Test.",
      command: "echo 'In Grafana: URL field = http://loki:3100  (Docker service name)'"
    },
    {
      step: 5,
      description: "Navigate to Explore (compass icon) in the left sidebar. Select the Loki data source from the dropdown.",
      command: "echo 'In Grafana: Explore > select Loki data source'"
    },
    {
      step: 6,
      description: "Query 1: Select all app container logs using a container label selector.",
      command: `echo 'LogQL: {container=~"monitoring-app.*"}  → Shows all log lines from the app container'`
    },
    {
      step: 7,
      description: "Generate some app traffic so there are recent log lines to query.",
      command: "for i in $(seq 1 20); do curl -s http://localhost:3000/ > /dev/null; done && echo 'Traffic generated'"
    },
    {
      step: 8,
      description: "Query 2: Filter app logs to only lines containing 'GET' (HTTP request log lines).",
      command: `echo 'LogQL: {container=~"monitoring-app.*"} |= "GET"  → |= is the text filter operator in LogQL'`
    },
    {
      step: 9,
      description: "Query 3: Count log volume over 5-minute windows to see ingestion rate.",
      command: `echo 'LogQL: count_over_time({container=~"monitoring-app.*"}[5m])  → Returns log line count per 5-minute window'`
    },
    {
      step: 10,
      description: "Compare Loki label-first querying to ELK text-first querying. If this were Elasticsearch, you could query {message: error} across all logs simultaneously. With Loki, you first pick a label stream, then filter text within it.",
      command: `echo 'Loki pattern: {container=~"monitoring-app.*"} |= "error"  vs ELK: message:error across all indices'`
    }
  ]}
>
  <ScenarioQuestion
    question={`Your Prometheus alert fires showing high error rate on the /api/orders route. You switch to Loki to find the specific error. You write the LogQL query {container=~"monitoring-app.*"} |= "error" but it returns no results. Your colleague uses Elasticsearch and finds the error in seconds with just message:error. Explain Loki's label-first querying model and why your query might return nothing even when errors exist.`}
    answer="Loki indexes labels, not log content. When you query {container=~'monitoring-app.*'} |= 'error', Loki first uses the label selector to identify which log stream to scan, then searches through those compressed log chunks for the text 'error'. If no logs appear, the label selector is the most likely culprit — the actual container name might differ from what the regex expects. Run docker ps to find the exact container name and adjust the label value. Elasticsearch avoids this problem entirely because it indexes every word across all logs, so message:error matches without knowing anything about the source. The Loki trade-off is 10x less storage and compute in exchange for requiring accurate label selectors as the starting point for every query."
  />
  <ScenarioQuestion
    question="Your team is deciding between Loki and Elasticsearch for centralized logging on a Kubernetes cluster where every service already has consistent job and namespace labels from Prometheus. The primary use case is filtering error logs by service after a metric alert fires. Which system is better suited to this use case, and what would make the other system a better choice?"
    answer="Loki is better suited here. Your team already uses Prometheus labels consistently across services, and Loki's label model mirrors Prometheus exactly — the same job and namespace labels that identify a metric time-series also identify a log stream. When an alert fires on a specific job, you switch to Loki, select that job's log stream, and filter for errors with |= 'error'. The narrow label-targeted query pattern is exactly what Loki is optimized for, and you avoid Elasticsearch's JVM overhead and complex cluster management. Elasticsearch would be the better choice if your logs are unstructured (no consistent labels), if you need full-text search across all services simultaneously without knowing which service to target, or if your compliance requirements mandate long-term log retention with fast arbitrary text search across the full corpus."
  />
  <VerificationChecklist
    title="Verify Your Work"
    items={[
      {
        id: "loki-connects",
        label: "Loki data source connects in Grafana (Save & Test shows green checkmark)",
        hint: "The URL must be http://loki:3100 (Docker service name). Using localhost:3100 will fail because Grafana's backend cannot reach localhost:3100 inside the Docker network."
      },
      {
        id: "logql-stream",
        label: `LogQL query {container=~"monitoring-app.*"} returns app container log lines`,
        hint: "If no logs appear, check that Promtail is running (docker compose ps) and has had time to collect logs. Also verify the container name matches — run: docker ps | grep monitoring to see actual container names."
      },
      {
        id: "text-filter",
        label: `Text filter |= "GET" correctly narrows log results to lines containing 'GET'`,
        hint: `The |= operator is Loki's contains filter. It scans the selected log stream for the literal string. Case-sensitive: |= "get" will not match 'GET'.`
      },
      {
        id: "label-query-model",
        label: "You can explain why Loki requires a label selector before text filtering",
        hint: "Loki indexes labels, not text. Without a label selector, Loki has no way to narrow down which log chunks to scan. The label selector is mandatory — it is how Loki avoids scanning all stored data for every query."
      }
    ]}
  />
</ExerciseCard>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "Log Aggregation Stacks",
      items: [
        { command: "ELK (Elasticsearch + Logstash + Kibana)", description: "Full-text indexed logs — powerful search but resource-heavy (2+ GB heap). Industry standard for large-scale deployments.", example: "Query: GET /logs/_search?q=NullPointerException" },
        { command: "Loki + Promtail + Grafana", description: "Label-indexed logs — lightweight (no JVM), integrates with Grafana/Prometheus label model. 10x less storage than ELK.", example: "Query: {job='app'} |= 'error'" },
        { command: "Fluentd / Fluent Bit", description: "Log collectors/forwarders — ship logs from containers to any backend (ELK, Loki, Splunk, CloudWatch)", example: "Alternative to Promtail; more plugin ecosystem" },
      ]
    },
    {
      title: "LogQL",
      items: [
        { command: `{label="value"}`, description: "Log stream selector — select logs by label. Required as first component of every LogQL query.", example: `{job="app", container="web"}` },
        { command: `|= "string"`, description: "Text filter — include lines containing the string (case-sensitive)", example: `{job="app"} |= "error"` },
        { command: `!= "string"`, description: "Inverse text filter — exclude lines containing the string", example: `{job="app"} != "healthcheck"` },
        { command: "| json", description: "Parse JSON log lines into structured fields for filtering", example: `{job="app"} | json | level="error"` },
        { command: "count_over_time([5m])", description: "Count log lines per time window — useful for log volume dashboards", example: `count_over_time({job="app"}[5m])` },
      ]
    },
    {
      title: "Loki vs Elasticsearch",
      items: [
        { command: "Loki index", description: "Indexes only labels — fast for label-targeted queries, uses 10x less storage", example: "{job='app', host='node-1'} |= 'error'" },
        { command: "Elasticsearch index", description: "Full-text inverted index — fast for any text search, but expensive (RAM, disk, CPU)", example: "GET /logs/_search?q=error" },
        { command: "When to use Loki", description: "Services with consistent labeling (Kubernetes, Docker Compose), Prometheus-based monitoring stacks, resource-constrained environments", example: "" },
        { command: "When to use ELK", description: "Large log volumes requiring full-text search across unstructured log data, compliance/audit log retention, existing ELK infrastructure", example: "" },
      ]
    }
  ]}
/>

export const quiz = [
  {
    id: "logagg-q1",
    question: "What is the fundamental architectural difference between Loki and Elasticsearch for log storage?",
    options: ["Loki stores logs in memory; Elasticsearch uses disk", "Loki indexes only label metadata, not log content; Elasticsearch creates a full-text inverted index of every word in every log line", "Elasticsearch uses pull-based ingestion; Loki uses push-based ingestion", "Loki is designed for application logs only; Elasticsearch handles infrastructure logs"],
    correctIndex: 1,
    explanation: "Loki indexes only the label set attached to log streams, storing log content in compressed chunks without word-indexing it. Elasticsearch creates an inverted index mapping every word to every log line containing it. This makes Elasticsearch faster for arbitrary text search but uses roughly 10x more storage.",
  },
  {
    id: "logagg-q2",
    question: "You write the LogQL query {container=\"app\"} |= \"error\" but it returns no results. Logs exist from the app container. What is the most likely cause?",
    options: ["LogQL does not support the |= operator for text filtering", "The actual container name does not match the label value in the selector — run docker ps to verify the container name", "Loki does not index log content so text filters always return empty", "The error text must be capitalized: |= \"Error\""],
    correctIndex: 1,
    explanation: "Loki queries start by selecting a log stream by its label set. If the container label value does not exactly match the actual container name as discovered by Promtail, the stream selector finds nothing and there is no data for the text filter to scan. Verify the container name with docker ps.",
  },
  {
    id: "logagg-q3",
    question: "Your services already use consistent job and namespace labels from Prometheus for metrics. A new team member proposes adding Elasticsearch for log aggregation. Given your existing label infrastructure, which system is better suited, and why?",
    options: ["Elasticsearch, because full-text search is always preferable to label-based querying", "Loki, because its label model mirrors Prometheus labels exactly — the same labels identifying metric time-series also identify log streams, enabling natural correlation without duplicating label infrastructure", "Neither — use a separate logging database with its own schema", "Elasticsearch for production; Loki for development only"],
    correctIndex: 1,
    explanation: "Loki's label model was deliberately designed to be consistent with Prometheus. When you already use job and namespace labels on metrics, those same labels applied to log streams let you correlate a firing alert with its log stream directly. This label alignment is the primary architectural advantage of Loki in Prometheus-based stacks.",
  },
  {
    id: "logagg-q4",
    question: "Promtail connects to the Docker socket in its configuration. What does this enable?",
    options: ["Promtail can restart containers when log errors are detected", "Promtail automatically discovers running containers and tails their logs, attaching the container name as a label without manual configuration for each container", "Promtail reads container resource metrics from Docker and ships them to Loki", "Promtail uses the Docker socket to push logs to Loki in binary format"],
    correctIndex: 1,
    explanation: "By connecting to the Docker socket via docker_sd_configs, Promtail discovers all running containers automatically and tails their stdout/stderr logs. The relabel_configs extract container metadata like the container name and attach it as a label, enabling {container=\"app\"} queries in LogQL.",
  },
  {
    id: "logagg-q5",
    question: "The centralized logging pipeline has four stages in order. Which sequence is correct?",
    options: ["Store → Collect → Ship → Query", "Collect → Ship → Store → Query", "Ship → Collect → Query → Store", "Query → Store → Collect → Ship"],
    correctIndex: 1,
    explanation: "The correct pipeline sequence is Collect (log agent tails log files or Docker socket), Ship (agent sends labeled log lines to the central store), Store (central store indexes and compresses logs), Query (UI lets you search across all hosts and services). Each stage has a distinct responsibility.",
  },
  {
    id: "logagg-q6",
    question: "When would Elasticsearch be a better choice than Loki for log aggregation?",
    options: ["When you already use Prometheus for metrics and your services have consistent labeling", "When you need full-text search across unstructured log data without knowing which service to target first, or when compliance requires long-term retention with arbitrary text search", "When resource constraints are a concern, since Elasticsearch uses less memory than Loki", "When your logs are structured JSON, since Loki cannot parse JSON"],
    correctIndex: 1,
    explanation: "Elasticsearch is preferable when logs are unstructured (no consistent labels to filter on) or when your query pattern requires searching across all logs simultaneously by content rather than by label first. It is also used for compliance and audit log retention scenarios requiring fast arbitrary text search over large corpora.",
  },
  {
    id: "logagg-q7",
    question: "What does the LogQL query count_over_time({container=~\"monitoring-app.*\"}[5m]) return?",
    options: ["The total number of containers matching the selector", "The count of log lines from the matching streams in each 5-minute window", "The rate of errors per second from the app container", "The number of unique label combinations in the selected stream"],
    correctIndex: 1,
    explanation: "count_over_time is a LogQL metric query that returns how many log lines were produced in each [5m] window. This is useful for log volume dashboards — you can chart ingestion rate over time and spot when a service starts producing significantly more log output than usual.",
  },
  {
    id: "logagg-q8",
    question: "What does auth_enabled: false mean in the Loki server configuration?",
    options: ["Loki will reject all unauthenticated ingestion requests", "Loki runs in single-tenant mode — no tenant ID is required in requests, appropriate for local development", "Grafana cannot query Loki without setting up OAuth", "Promtail will not be able to connect to Loki"],
    correctIndex: 1,
    explanation: "auth_enabled: false enables single-tenant mode, where no tenant ID header is required in requests. This is suitable for local development. In multi-tenant production deployments, auth_enabled: true requires each request to include an X-Scope-OrgID header identifying the tenant.",
  },
]
