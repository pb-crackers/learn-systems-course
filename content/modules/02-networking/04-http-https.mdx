---
title: "HTTP and HTTPS: How the Web Communicates"
description: "The request/response cycle that powers every web application — methods, headers, status codes, and how TLS encrypts it all"
module: "Networking Foundations"
moduleSlug: "02-networking"
lessonSlug: "04-http-https"
order: 4
difficulty: "Foundation"
estimatedMinutes: 23
prerequisites: ["02-networking/02-tcp-ip-stack"]
tags: ["http", "https", "tls", "curl", "headers", "status-codes", "request-response"]
---

## Overview

Your deployment succeeded but users are getting 502 Bad Gateway errors. Understanding HTTP tells you the request reached the load balancer but the backend didn't respond correctly — the status code is the first clue. HTTP (Hypertext Transfer Protocol) is the application-layer protocol that defines exactly how clients and servers exchange messages. HTTPS adds TLS encryption on top, so those messages can't be read or tampered with in transit.

This lesson explains the complete request/response cycle, what each status code category means, how TLS protects your traffic, and how `curl` gives you full visibility into every layer of an HTTP exchange.

## How It Works

### HTTP Request/Response Cycle

HTTP is a request/response protocol. The client sends a request; the server returns a response. Both are text-based messages with a structured format: a start line, headers, a blank line, and an optional body.

```
HTTP Request                         HTTP Response
────────────────────────────────     ────────────────────────────────
GET /api/users HTTP/1.1              HTTP/1.1 200 OK
Host: api.example.com                Content-Type: application/json
Authorization: Bearer abc123         Content-Length: 47
Accept: application/json             Cache-Control: no-store
                                     Date: Thu, 19 Mar 2026 10:00:00 GMT
(no body — GET has no body)
                                     {"users": [{"id": 1, "name": "Alice"}]}
────────────────────────────────     ────────────────────────────────
start-line: method + path + version  start-line: version + status-code + reason
headers: key: value pairs            headers: key: value pairs
blank line (separator)               blank line (separator)
body (optional)                      body (the actual response content)
```

Each HTTP exchange happens over a TCP connection. The client opens a TCP connection to the server's IP on port 80 (HTTP) or 443 (HTTPS), sends the request as text, and reads the response. HTTP/1.1 can reuse the same TCP connection for multiple requests (persistent connections). HTTP/2 goes further by multiplexing multiple requests over a single connection.

### HTTP Methods

Methods define the intended action on the resource. The method is the first word of the request start-line.

| Method | Meaning | Has Body? | Idempotent? |
|--------|---------|-----------|-------------|
| **GET** | Read a resource | No | Yes — repeating returns same result |
| **POST** | Create a new resource | Yes | No — each call creates a new item |
| **PUT** | Replace a resource entirely | Yes | Yes — replacing with same data has same result |
| **PATCH** | Update a resource partially | Yes | No — partial updates can compound |
| **DELETE** | Remove a resource | No | Yes — deleting what's already gone is a no-op |

**Idempotency** matters for reliability: if a network timeout causes you to retry a request, idempotent methods are safe to retry. POST is not — retrying "create order" creates two orders.

### HTTP Headers

Headers are key-value pairs that carry metadata about the request or response. They are separate from the body.

**Common request headers:**

| Header | Purpose | Example |
|--------|---------|---------|
| `Host` | Which virtual host to route to (required in HTTP/1.1) | `Host: api.example.com` |
| `Content-Type` | Format of the request body | `Content-Type: application/json` |
| `Authorization` | Authentication credentials | `Authorization: Bearer eyJhb...` |
| `User-Agent` | Client identifier | `User-Agent: curl/8.5.0` |
| `Accept` | Response formats the client can handle | `Accept: application/json` |

**Common response headers:**

| Header | Purpose | Example |
|--------|---------|---------|
| `Content-Type` | Format of the response body | `Content-Type: text/html; charset=utf-8` |
| `Content-Length` | Size of the body in bytes | `Content-Length: 1024` |
| `Cache-Control` | Caching directives | `Cache-Control: max-age=3600` |
| `Location` | Redirect destination (with 3xx) | `Location: https://example.com/new-path` |
| `Set-Cookie` | Set a cookie on the client | `Set-Cookie: session=abc; HttpOnly` |

`curl -v` shows every header exchanged. The `>` lines are your request headers; the `<` lines are the server's response headers:

<TerminalBlock
  title="curl -v showing full header exchange"
  lines={[
    { type: 'command', content: 'curl -v http://httpbin.org/get 2>&1 | head -30' },
    { type: 'output', content: '* Trying 54.208.105.16:80...' },
    { type: 'output', content: '* Connected to httpbin.org (54.208.105.16) port 80' },
    { type: 'output', content: '> GET /get HTTP/1.1' },
    { type: 'output', content: '> Host: httpbin.org' },
    { type: 'output', content: '> User-Agent: curl/8.5.0' },
    { type: 'output', content: '> Accept: */*' },
    { type: 'output', content: '>' },
    { type: 'output', content: '< HTTP/1.1 200 OK' },
    { type: 'output', content: '< Content-Type: application/json' },
    { type: 'output', content: '< Content-Length: 268' },
    { type: 'output', content: '< Access-Control-Allow-Origin: *' },
    { type: 'output', content: '<' },
    { type: 'output', content: '{ ... JSON body ... }' },
  ]}
/>

### Status Codes

The three-digit status code in every response tells you what happened. The first digit is the category:

| Category | Range | Meaning |
|----------|-------|---------|
| **2xx** | 200–299 | Success — the request was received, understood, and accepted |
| **3xx** | 300–399 | Redirection — further action needed to complete the request |
| **4xx** | 400–499 | Client error — the request was malformed or unauthorized |
| **5xx** | 500–599 | Server error — the server failed to fulfill a valid request |

**The codes you'll see most:**

| Code | Name | What It Means |
|------|------|--------------|
| 200 | OK | Standard success response |
| 201 | Created | A new resource was created (POST success) |
| 204 | No Content | Success, but no body to return (DELETE success) |
| 301 | Moved Permanently | Resource has a new permanent URL; update your links |
| 302 | Found | Temporary redirect; keep using the original URL |
| 304 | Not Modified | Cache is still valid; no body sent |
| 400 | Bad Request | The request is malformed — usually a client bug |
| 401 | Unauthorized | Not authenticated — provide credentials |
| 403 | Forbidden | Authenticated but not authorized for this resource |
| 404 | Not Found | The resource doesn't exist at this URL |
| 500 | Internal Server Error | Generic server crash — check server logs |
| 502 | Bad Gateway | Proxy reached the backend but got a bad response |
| 503 | Service Unavailable | Server is down or overloaded |
| 504 | Gateway Timeout | Proxy reached the backend but it didn't respond in time |

<Callout type="tip" title="502 vs 504: Different Problems">
Most debugging starts with the status code. A **502** means the proxy reached the server but got a bad response — the backend is up but crashing. A **504** means the proxy couldn't get a response at all within the timeout — the backend may be down, overloaded, or the network between them is broken. Same symptom (users can't load the page), different causes, different fixes.
</Callout>

### HTTPS and TLS

HTTP sends everything as plaintext — headers, cookies, request bodies, passwords. Anyone between the client and server (a coffee shop router, an ISP, a compromised CDN node) can read or modify the traffic. HTTPS adds TLS (Transport Layer Security) to encrypt and authenticate the connection.

**The TLS handshake** happens before any HTTP data is exchanged. It establishes encrypted communication and verifies the server's identity:

```
Client                                        Server
  │                                              │
  │──── ClientHello ─────────────────────────►  │
  │     (TLS version, cipher suites,             │
  │      random bytes)                           │
  │                                              │
  │  ◄─── ServerHello ───────────────────────── │
  │        (chosen cipher suite,                 │
  │         random bytes, session ID)            │
  │                                              │
  │  ◄─── Certificate ───────────────────────── │
  │        (server's public key +                │
  │         identity signed by a CA)             │
  │                                              │
  │  ◄─── ServerHelloDone ───────────────────── │
  │                                              │
  │──── ClientKeyExchange ──────────────────►  │
  │     (pre-master secret encrypted            │
  │      with server's public key)              │
  │                                              │
  │──── ChangeCipherSpec ───────────────────►  │
  │──── Finished (encrypted) ───────────────►  │
  │                                              │
  │  ◄─── ChangeCipherSpec ──────────────────── │
  │  ◄─── Finished (encrypted) ─────────────── │
  │                                              │
  │  ═══════ Encrypted HTTP traffic ═══════════ │
```

After the handshake, all data is encrypted using a **symmetric key** that both sides derived from the exchange. The server's certificate (signed by a Certificate Authority) proves it is who it claims to be — your browser has a built-in list of trusted CAs.

<Callout type="deep-dive" title="Why Two Types of Encryption?">
The TLS handshake uses **asymmetric encryption** (public/private key pairs) to securely exchange a shared secret. Asymmetric encryption is mathematically expensive — encrypting large amounts of data with RSA or ECDH is slow. Once both sides have the shared secret, they switch to **symmetric encryption** (AES-GCM) for the actual data. Symmetric encryption is orders of magnitude faster. The handshake is the expensive part; the data transfer is fast. This hybrid approach is why TLS overhead is measured in milliseconds, not seconds.
</Callout>

**Certificates and Certificate Authorities (CAs):** A certificate binds a public key to a domain name and is signed by a CA that your browser trusts. If the certificate isn't signed by a trusted CA (a self-signed certificate), browsers and curl warn you because there's no third-party verification that you're talking to who you think you are.

### curl: The DevOps Swiss Army Knife

`curl` is the primary tool for testing and debugging HTTP. It speaks every HTTP variant, shows you exactly what was sent and received, and works from any shell.

```bash
# Just get the response body
curl http://api.example.com/health

# Verbose — see headers on both sides
curl -v http://api.example.com/health

# Headers only (HEAD request)
curl -I http://api.example.com/

# POST with JSON body
curl -X POST \
     -H 'Content-Type: application/json' \
     -d '{"name": "alice"}' \
     http://api.example.com/users

# Follow redirects automatically
curl -L http://example.com/

# Skip TLS certificate verification (for self-signed certs)
curl -k https://self-signed.example.com/

# Print just the HTTP status code
curl -s -o /dev/null -w '%{http_code}\n' http://api.example.com/health

# Save response to a file
curl -o response.json http://api.example.com/data

# Set a custom header
curl -H 'Authorization: Bearer mytoken' http://api.example.com/secure
```

<Callout type="warning" title="curl -k Skips Authentication">
The `-k` flag tells curl to ignore TLS certificate errors, including invalid or self-signed certificates. This is necessary for lab environments using self-signed certs, but it disables a critical security check — if you use `-k` against a real server, you cannot verify you're talking to the intended server. An attacker doing a MITM attack could present any certificate and you'd never know. Use `-k` only in trusted lab environments, never in scripts that run against production.
</Callout>

## Hands-On Exercise

<ExerciseCard
  title="Debug an HTTP/HTTPS Deployment"
  difficulty="Foundation"
  scenario="You're debugging a web application deployment and need to verify the server is responding correctly, inspect response headers, and confirm HTTPS is working. The lab has an nginx server with both HTTP (port 80) and HTTPS (port 443) using a self-signed certificate."
  objective="By the end of this exercise, you will be able to use curl to inspect HTTP headers, diagnose status codes, and verify TLS negotiation with a self-signed certificate."
  annotated={true}
  steps={[
    {
      step: 1,
      description: "Start the lab environment. Two containers will start: webserver (nginx) and client (your shell).",
      command: "docker compose -f docker/networking/04-http/compose.yml up -d",
      annotations: [
        { token: "docker", description: "Runs the Docker CLI for managing containers and multi-container applications." },
        { token: "compose", description: "Invokes the Docker Compose subcommand for managing applications defined in a Compose file." },
        { token: "-f", description: "Specifies a non-default Compose file path to use for this command." },
        { token: "docker/networking/04-http/compose.yml", description: "Points to the HTTP lab Compose file defining the nginx webserver and client containers." },
        { token: "up", description: "Creates and starts all services defined in the Compose file." },
        { token: "-d", description: "Runs the containers in detached mode, returning the shell prompt immediately." }
      ]
    },
    {
      step: 2,
      description: "Open a shell in the client container.",
      command: "docker compose -f docker/networking/04-http/compose.yml exec client bash",
      annotations: [
        { token: "docker", description: "Runs the Docker CLI to interact with running containers." },
        { token: "compose", description: "Invokes the Docker Compose subcommand to target services in the specified project." },
        { token: "-f", description: "Specifies the Compose file to identify which project's services to target." },
        { token: "docker/networking/04-http/compose.yml", description: "Points to the HTTP lab Compose file so exec targets the correct project." },
        { token: "exec", description: "Runs a command inside an already-running container." },
        { token: "client", description: "Specifies the service name to run the command in — the client container with curl installed." },
        { token: "bash", description: "Starts an interactive Bash shell inside the client container." }
      ]
    },
    {
      step: 3,
      description: "Make a basic HTTP request and check the status code.",
      command: "curl -s -o /dev/null -w '%{http_code}\\n' http://webserver/",
      annotations: [
        { token: "curl", description: "Transfers data from or to a server using HTTP, HTTPS, or other protocols." },
        { token: "-s", description: "Silent mode — suppresses progress meters and error messages from curl itself." },
        { token: "-o", description: "Writes the response body to a file instead of stdout." },
        { token: "/dev/null", description: "Discards the response body by redirecting it to the null device." },
        { token: "-w", description: "Prints a formatted string after the transfer using curl variables like http_code." },
        { token: "'%{http_code}\\n'", description: "Formats the output to print only the HTTP status code followed by a newline." },
        { token: "http://webserver/", description: "Specifies the URL to request — the nginx webserver container's root path over HTTP." }
      ]
    },
    {
      step: 4,
      description: "Get the response headers only (HEAD request) and save them to a file for inspection. Create the directory first.",
      command: "mkdir -p /tmp/exercise && curl -I http://webserver/ | tee /tmp/exercise/headers.txt",
      annotations: [
        { token: "mkdir", description: "Creates one or more directories." },
        { token: "-p", description: "Creates parent directories as needed and does not error if the directory already exists." },
        { token: "/tmp/exercise", description: "Specifies the directory path to create for storing exercise output files." },
        { token: "&&", description: "Runs the next command only if mkdir succeeds." },
        { token: "curl", description: "Transfers data from a server — here used to send a HEAD request." },
        { token: "-I", description: "Sends an HTTP HEAD request, receiving response headers without a response body." },
        { token: "http://webserver/", description: "Specifies the URL to send the HEAD request to." },
        { token: "tee", description: "Reads from stdin and writes the data to both stdout and a file simultaneously." },
        { token: "/tmp/exercise/headers.txt", description: "Specifies the file path where the response headers are saved for inspection." }
      ]
    },
    {
      step: 5,
      description: "Use verbose mode to see the full TLS negotiation for the HTTPS endpoint. The -k flag is needed for the self-signed certificate.",
      command: "curl -vk https://webserver/ 2>&1 | head -40",
      annotations: [
        { token: "curl", description: "Transfers data from a server — here used with verbose output to inspect TLS negotiation." },
        { token: "-v", description: "Verbose mode — prints request headers, response headers, and TLS handshake details." },
        { token: "-k", description: "Skips TLS certificate verification, required here because the lab uses a self-signed certificate." },
        { token: "https://webserver/", description: "Specifies the HTTPS URL to connect to, triggering a TLS handshake before the HTTP request." },
        { token: "2>&1", description: "Redirects stderr (where curl writes verbose info) to stdout so it can be piped." },
        { token: "head", description: "Prints only the first N lines of input." },
        { token: "-40", description: "Limits output to the first 40 lines, showing the TLS handshake details without truncating them." }
      ]
    },
    {
      step: 6,
      description: "Send a POST request with a JSON body to the API endpoint. Observe the Content-Type header requirement.",
      command: "curl -X POST -H 'Content-Type: application/json' -d '{\"test\":\"data\"}' http://webserver/api",
      annotations: [
        { token: "curl", description: "Transfers data to a server — here used to send an HTTP POST request with a JSON body." },
        { token: "-X", description: "Specifies the HTTP method to use instead of the default GET." },
        { token: "POST", description: "Sets the request method to POST, used for creating resources or sending data to the server." },
        { token: "-H", description: "Adds a custom header to the request." },
        { token: "'Content-Type: application/json'", description: "Sets the Content-Type header to tell the server the request body is JSON-formatted." },
        { token: "-d", description: "Specifies the data to send as the request body." },
        { token: "'{\"test\":\"data\"}'", description: "Provides a minimal JSON object as the POST body to test the API endpoint." },
        { token: "http://webserver/api", description: "Specifies the API endpoint URL to send the POST request to." }
      ]
    },
    {
      step: 7,
      description: "Use curl to print just the HTTP status code from the HTTPS endpoint (skipping cert verification).",
      command: "curl -sk -o /dev/null -w '%{http_code}\\n' https://webserver/",
      annotations: [
        { token: "curl", description: "Transfers data from a server, here used to extract only the HTTP status code." },
        { token: "-s", description: "Silent mode — suppresses progress output from curl." },
        { token: "-k", description: "Skips TLS certificate verification, required for the self-signed certificate in the lab." },
        { token: "-o", description: "Writes the response body to a file rather than stdout." },
        { token: "/dev/null", description: "Discards the response body, keeping only the status code output." },
        { token: "-w", description: "Prints a formatted string after transfer using curl output variables." },
        { token: "'%{http_code}\\n'", description: "Formats output to print only the HTTP status code followed by a newline." },
        { token: "https://webserver/", description: "Specifies the HTTPS URL to request, testing that TLS handshake and response are both successful." }
      ]
    },
    {
      step: 8,
      description: "Run the verification script to confirm your work.",
      command: "bash /verify/04-http.sh",
      annotations: [
        { token: "bash", description: "Runs the Bash shell interpreter to execute the specified script file." },
        { token: "/verify/04-http.sh", description: "Specifies the path to the lab verification script that checks all exercise requirements." }
      ]
    }
  ]}
>
  <ScenarioQuestion
    question="You run curl -s -o /dev/null -w '%{http_code}' http://webserver/ and get 502. Then curl -v http://webserver/ shows the connection is established but the response is '502 Bad Gateway'. What does this status code tell you about where the problem is, and which component would you investigate next?"
    answer="A 502 Bad Gateway means the nginx webserver received your request and successfully proxied it to a backend service, but got an invalid or unreadable response from that backend. The nginx server itself is working correctly — the problem is one layer deeper, in the application server or API service that nginx is forwarding to. The next investigation step is to look at the backend service directly: check if it is running with ss -tlnp on the app server container, review its logs with docker compose logs app-server, and try connecting directly to the backend port with curl to bypass nginx and see the raw response. A 502 with no incoming requests in the backend logs would confirm the backend has crashed or is not listening on the expected port."
  />
  <ScenarioQuestion
    question="curl -vk https://webserver/ shows a successful TLS handshake with the lab's self-signed certificate. Your team asks why you need -k in the lab but production HTTPS endpoints work without it. What is the difference between a self-signed certificate and a CA-signed certificate, and what security guarantee does -k remove?"
    answer="A CA-signed certificate is issued by a Certificate Authority that is already in your operating system's trusted root store — a list of organizations whose signatures browsers and curl trust by default. When curl connects to a production server, it receives the certificate, checks its signature against the trusted CAs, and verifies the certificate's domain matches the hostname you requested. A self-signed certificate is signed by the server itself, with no trusted CA in the chain. curl cannot verify the signature against any trusted authority, so it rejects the certificate unless you force it to skip with -k. The security guarantee -k removes is server identity verification: without certificate validation, you cannot confirm you are talking to the intended server rather than an attacker performing a man-in-the-middle attack who presents their own certificate. In a lab with a controlled network, this is acceptable. Against production systems, it is a serious security risk."
  />
  <VerificationChecklist
    title="Verify Your Work"
    items={[
      {
        id: "http-200",
        label: "HTTP server on port 80 returns 200 OK",
        hint: "Run: curl -s -o /dev/null -w '%{http_code}' http://webserver/ — should print 200"
      },
      {
        id: "headers-captured",
        label: "Response headers captured to /tmp/exercise/headers.txt",
        hint: "Run: curl -I http://webserver/ > /tmp/exercise/headers.txt — then cat the file to verify"
      },
      {
        id: "https-works",
        label: "HTTPS endpoint on port 443 returns 200 (TLS handshake completed)",
        hint: "Run: curl -sk -o /dev/null -w '%{http_code}' https://webserver/ — should print 200"
      }
    ]}
  />
</ExerciseCard>

<TerminalBlock
  title="Tear down the lab when done"
  lines={[
    { type: 'comment', content: 'Exit the container shell first' },
    { type: 'command', content: 'exit' },
    { type: 'command', content: 'docker compose -f docker/networking/04-http/compose.yml down' },
    { type: 'output', content: '[+] Running 3/3' },
    { type: 'output', content: ' ✔ Container 04-http-client-1    Removed' },
    { type: 'output', content: ' ✔ Container 04-http-webserver-1 Removed' },
    { type: 'output', content: ' ✔ Network 04-http_lab-net       Removed' },
  ]}
/>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "HTTP Methods",
      items: [
        { command: "GET", description: "Read a resource. Safe and idempotent. No request body.", example: "GET /api/users HTTP/1.1" },
        { command: "POST", description: "Create a new resource. Not idempotent — each call creates a new item.", example: "POST /api/users HTTP/1.1" },
        { command: "PUT", description: "Replace a resource entirely. Idempotent.", example: "PUT /api/users/42 HTTP/1.1" },
        { command: "PATCH", description: "Partially update a resource.", example: "PATCH /api/users/42 HTTP/1.1" },
        { command: "DELETE", description: "Remove a resource. Idempotent.", example: "DELETE /api/users/42 HTTP/1.1" },
      ]
    },
    {
      title: "Common Status Codes",
      items: [
        { command: "200 OK", description: "Standard success. Body contains the resource." },
        { command: "201 Created", description: "Resource created (POST success). Location header has new URL." },
        { command: "301 Moved Permanently", description: "Permanent redirect. Update your bookmarks/links." },
        { command: "400 Bad Request", description: "Malformed request — fix the client." },
        { command: "401 Unauthorized", description: "No credentials provided — authenticate first." },
        { command: "403 Forbidden", description: "Authenticated but not allowed — check permissions." },
        { command: "404 Not Found", description: "Resource does not exist at this URL." },
        { command: "502 Bad Gateway", description: "Proxy reached backend but got invalid response — backend is up but failing." },
        { command: "503 Service Unavailable", description: "Server down or overloaded." },
        { command: "504 Gateway Timeout", description: "Proxy timed out waiting for backend — backend may be down or slow." },
      ]
    },
    {
      title: "curl Command Reference",
      items: [
        { command: "curl -v <url>", description: "Verbose — show request and response headers", example: "curl -v http://api.example.com/" },
        { command: "curl -I <url>", description: "HEAD request — response headers only, no body", example: "curl -I https://example.com/" },
        { command: "curl -s -o /dev/null -w '%{http_code}'", description: "Print just the HTTP status code", example: "curl -s -o /dev/null -w '%{http_code}' http://example.com/" },
        { command: "curl -X POST -d '<data>'", description: "Send POST request with body data", example: "curl -X POST -d '{\"key\":\"val\"}' http://api.example.com/" },
        { command: "curl -H '<header>'", description: "Add a custom request header", example: "curl -H 'Content-Type: application/json' ..." },
        { command: "curl -k <url>", description: "Skip TLS certificate verification (for self-signed certs, lab only)", example: "curl -k https://localhost:8443/" },
        { command: "curl -L <url>", description: "Follow redirects automatically", example: "curl -L http://example.com/" },
        { command: "curl -o <file>", description: "Save response body to a file", example: "curl -o output.json http://api.example.com/data" },
      ]
    },
    {
      title: "TLS Handshake Summary",
      items: [
        { command: "ClientHello", description: "Client announces TLS version and supported cipher suites" },
        { command: "ServerHello + Certificate", description: "Server picks cipher suite, sends its certificate (public key + CA signature)" },
        { command: "Key Exchange", description: "Client uses server's public key to share a secret; both sides derive a symmetric key" },
        { command: "Finished", description: "Both sides confirm the handshake; encrypted data transfer begins" },
        { command: "Symmetric encryption", description: "All HTTP data is encrypted with AES-GCM after handshake (fast)" },
        { command: "Certificate Authority", description: "Trusted third party that signs certificates; browser has a built-in list of trusted CAs" },
      ]
    }
  ]}
/>

export const quiz = [
  {
    id: 'http-q1',
    question: 'What does a 502 Bad Gateway status code mean in an HTTP response?',
    options: ['The client sent a malformed request', 'The server received a valid request but failed internally', 'The proxy reached the backend but received an invalid or bad response from it', 'The client is not authenticated and must provide credentials'],
    correctIndex: 2,
    explanation: 'A 502 means the proxy (like nginx) successfully connected to the backend service but the backend returned an invalid or unreadable response. The proxy itself is working. This is different from 504, where the proxy times out waiting for the backend. A 502 means the backend is up but crashing or misbehaving.',
  },
  {
    id: 'http-q2',
    question: 'Which HTTP method is considered idempotent and safe for reading a resource without side effects?',
    options: ['POST', 'PATCH', 'PUT', 'GET'],
    correctIndex: 3,
    explanation: 'GET is both safe (no side effects) and idempotent (repeating it returns the same result). This is why browsers safely retry GET requests on network errors. POST is not idempotent — retrying a POST could create duplicate resources. PUT is idempotent but not safe since it modifies a resource.',
  },
  {
    id: 'http-q3',
    question: 'What is the purpose of the Host header in an HTTP/1.1 request?',
    options: ['It specifies the IP address of the server for routing', 'It identifies which virtual host on the server should handle the request', 'It carries the client authentication token', 'It tells the server which HTTP version the client supports'],
    correctIndex: 1,
    explanation: 'The Host header is required in HTTP/1.1 because a single IP address can serve many different websites (virtual hosting). The server uses the Host header value to determine which site or backend to route the request to. Without it the server would not know which of many hosted domains the client is requesting.',
  },
  {
    id: 'http-q4',
    question: 'What does a 401 Unauthorized status code indicate, and how does it differ from 403?',
    options: ['401 means no credentials were provided; 403 means credentials were provided but access is denied', '401 means the server crashed; 403 means the request was malformed', '401 means the resource was not found; 403 means the method is not allowed', '401 means too many requests; 403 means the service is unavailable'],
    correctIndex: 0,
    explanation: '401 Unauthorized means the client is not authenticated — it has not provided credentials or the credentials are invalid. 403 Forbidden means the client is authenticated but does not have permission to access the resource. The distinction is important: 401 requires authentication, 403 requires authorization.',
  },
  {
    id: 'http-q5',
    question: 'Why does the TLS handshake use asymmetric encryption to set up the session, then switch to symmetric encryption for data transfer?',
    options: ['Asymmetric encryption is faster for bulk data; symmetric is used only for key exchange because it is more secure', 'Asymmetric encryption is slow and computationally expensive; symmetric encryption (AES-GCM) is orders of magnitude faster for bulk data', 'Symmetric encryption cannot be used over untrusted networks; asymmetric must be used throughout', 'The switch happens because symmetric keys expire after the handshake completes'],
    correctIndex: 1,
    explanation: 'Asymmetric cryptography (RSA, ECDH) uses complex mathematical operations that are too slow for encrypting large data volumes. It is used only to securely establish a shared secret without transmitting it. Once both sides derive the same symmetric key, they switch to AES-GCM which is orders of magnitude faster, making TLS overhead measured in milliseconds rather than seconds.',
  },
  {
    id: 'http-q6',
    question: 'What curl flag displays the full TLS handshake details and request/response headers?',
    options: ['-s', '-I', '-v', '-k'],
    correctIndex: 2,
    explanation: 'The -v (verbose) flag tells curl to print all connection details including TLS negotiation, request headers sent, and response headers received. The > lines show outgoing request headers and the < lines show incoming response headers. This is the primary tool for debugging HTTP and HTTPS issues.',
  },
  {
    id: 'http-q7',
    question: 'A 304 Not Modified response means what in the context of HTTP caching?',
    options: ['The server could not find the resource', 'The client cached version is still valid and no body is returned — the client can use its cached copy', 'The resource has moved permanently to a new URL', 'The server partially modified the resource in response to a PATCH request'],
    correctIndex: 1,
    explanation: 'When a client sends a conditional request (using If-None-Match or If-Modified-Since headers), a 304 tells the client its cached copy is still current. The response has no body — the client simply continues using its local cache. This saves bandwidth and speeds up repeated page loads for resources that have not changed.',
  },
  {
    id: 'http-q8',
    question: 'What security guarantee does the -k flag remove when using curl?',
    options: ['It disables data encryption, sending traffic in plaintext', 'It skips TLS certificate verification, removing the ability to confirm you are talking to the intended server', 'It disables HTTP authentication header encoding', 'It allows the server to read the local filesystem'],
    correctIndex: 1,
    explanation: 'The -k flag tells curl to accept any TLS certificate, including self-signed or invalid ones. This removes server identity verification — without a valid certificate chain, you cannot confirm you are talking to the intended server rather than an attacker performing a man-in-the-middle attack. Data is still encrypted, but its destination is unverified.',
  },
  {
    id: 'http-q9',
    question: 'Which HTTP method should be used to create a new resource on a server?',
    options: ['GET', 'PUT', 'POST', 'PATCH'],
    correctIndex: 2,
    explanation: 'POST is used to create a new resource. The server determines the resource URL and typically returns 201 Created with a Location header pointing to the new resource. POST is not idempotent — calling it twice creates two resources. PUT replaces an existing resource at a URL the client specifies, and is idempotent.',
  },
  {
    id: 'http-q10',
    question: 'What does the Cache-Control: max-age=3600 response header instruct the client to do?',
    options: ['Refresh the resource every 3600 milliseconds', 'Cache the response and consider it fresh for 3600 seconds before re-requesting', 'Only cache the response if it is under 3600 bytes', 'Retry the request after 3600 seconds if the initial response failed'],
    correctIndex: 1,
    explanation: 'max-age=3600 tells the client to cache the response and serve it from cache for up to 3600 seconds (1 hour) without re-requesting from the server. This reduces server load and speeds up repeat visits. After the max-age expires, the client must revalidate with the server, potentially getting a 304 Not Modified if the content has not changed.',
  },
]
