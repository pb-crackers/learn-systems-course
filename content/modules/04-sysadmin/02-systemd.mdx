---
title: "systemd: The Modern Linux Init System"
description: "Unit files, service lifecycle, targets, and journald — how Linux starts and manages services"
module: "System Administration"
moduleSlug: "04-sysadmin"
lessonSlug: "02-systemd"
order: 2
difficulty: "Foundation"
estimatedMinutes: 30
prerequisites: ["01-linux-fundamentals/05-processes", "04-sysadmin/01-user-management"]
tags: ["systemd", "systemctl", "journald", "units", "services", "targets", "init"]
---

## Overview

Your Node.js API is crashing under load. Every time it crashes, someone has to SSH in and manually restart it. The correct fix is not a shell script in a while loop — it is a systemd service unit that automatically restarts the process, logs output to the journal, and starts on boot. Understanding systemd is the difference between a fragile process and a managed service.

systemd is PID 1 — the first process the kernel starts. Everything else on the system is a child or descendant of systemd. It manages service dependencies, parallelizes boot, and provides structured logging. This lesson explains how it works before showing you the commands.

<Callout type="warning" title="Practicing systemd in Docker Containers">
In this course we practice systemd inside Docker containers using a special systemd-enabled image (`jrei/systemd-ubuntu:22.04`). This image requires the `--privileged` flag and specific `--tmpfs` mounts so systemd can manage cgroups correctly inside the container. In production, you manage systemd on real Linux hosts — the container gives you a safe sandbox to practice the commands without risking a real system.

On Apple Silicon Macs, add `--platform linux/amd64` to the docker run command — the image is amd64-only and runs via Rosetta 2 emulation.
</Callout>

## How It Works

### What PID 1 Does

When the Linux kernel finishes booting, it needs to hand control to user space. It does this by executing one process: PID 1. That process is responsible for starting everything else — every service, every daemon, every user session. If PID 1 dies, the kernel panics.

Before systemd, this was handled by **SysV init** — a set of shell scripts in `/etc/init.d/` that ran sequentially. Sequential startup was slow: each service waited for the previous one to finish. Dependency management was manual and fragile. A broken init script could hang the entire boot process.

**systemd** replaced this with:
- **Parallel startup**: services with no dependencies start simultaneously
- **Declarative dependency management**: you describe what depends on what, systemd calculates the order
- **Socket activation**: systemd can listen on a port and start a service on demand when the first connection arrives
- **Unified logging**: all service output goes to journald (structured binary log), accessible via `journalctl`
- **Automatic restart**: services can be configured to restart on failure with configurable backoff

### Unit File Anatomy

systemd manages resources through **unit files** — declarative configuration files describing what to start, how to start it, and what it depends on. Unit files live in:

- `/lib/systemd/system/` — installed by packages (do not edit)
- `/etc/systemd/system/` — your custom units and overrides

A service unit file has three sections:

```ini
# /etc/systemd/system/hello.service
[Unit]
Description=Hello World Service
After=network.target
Wants=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/hello.sh
Restart=on-failure
RestartSec=5s
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**`[Unit]` section** — metadata and dependencies:

| Directive | Meaning |
|-----------|---------|
| `Description=` | Human-readable name shown in `systemctl status` |
| `After=` | Start this unit AFTER these units are active (ordering only) |
| `Requires=` | Hard dependency — if the required unit fails, this unit fails too |
| `Wants=` | Soft dependency — if the wanted unit fails, this unit still starts |

**`[Service]` section** — how to run the service:

| Directive | Meaning |
|-----------|---------|
| `Type=simple` | Process stays in foreground (default for most services) |
| `Type=forking` | Process daemonizes — forks and parent exits |
| `Type=oneshot` | Process runs once and exits (scripts, setup tasks) |
| `Type=notify` | Process notifies systemd when it is ready via `sd_notify()` |
| `ExecStart=` | Command to start the service (must be an absolute path) |
| `ExecStop=` | Command to stop it gracefully (default: SIGTERM) |
| `Restart=on-failure` | Restart only if the process exits with non-zero status |
| `Restart=always` | Restart regardless of exit status |
| `RestartSec=5s` | Wait this long before restarting |
| `StandardOutput=journal` | Send stdout to journald |
| `StandardError=journal` | Send stderr to journald |

**`[Install]` section** — when to start this service:

| Directive | Meaning |
|-----------|---------|
| `WantedBy=multi-user.target` | Enable this service as part of normal multi-user boot |
| `WantedBy=graphical.target` | Enable for graphical desktop boot |

### Targets: Replacing Runlevels

SysV init had numbered runlevels (0=halt, 1=single-user, 3=multi-user, 5=graphical, 6=reboot). systemd replaces these with **targets** — named groups of units that represent a system state:

| Target | SysV Equivalent | Meaning |
|--------|----------------|---------|
| `poweroff.target` | runlevel 0 | Shutdown |
| `rescue.target` | runlevel 1 | Single-user rescue mode |
| `multi-user.target` | runlevel 3 | Multi-user without GUI |
| `graphical.target` | runlevel 5 | Multi-user with GUI |
| `reboot.target` | runlevel 6 | Reboot |

When you "enable" a service with `WantedBy=multi-user.target`, systemd creates a symlink in `/etc/systemd/system/multi-user.target.wants/` pointing to your unit file. When the system reaches `multi-user.target`, it starts all units in that directory.

### Service Lifecycle

A unit moves through states:

```
loaded
  └─ inactive (not running)
       └─ activating (starting up — ExecStartPre, ExecStart running)
            └─ active (running normally)
                 └─ deactivating (stopping — ExecStop running)
                      └─ inactive
                           └─ failed (exited with error, not configured to restart)
```

`systemctl status` shows the current state plus recent journal entries for that unit.

### journald: Structured Logging

journald is systemd's logging daemon. Unlike traditional syslog (which wrote text files), journald stores **structured binary logs** with indexed metadata: timestamp, unit name, PID, priority level, hostname, and the message. This enables fast queries by any of those fields.

journald stores logs in `/run/log/journal/` (volatile, cleared on reboot) or `/var/log/journal/` (persistent, if that directory exists). Access logs with `journalctl`:

```bash
journalctl -u hello.service          # logs for specific unit
journalctl -u hello.service -f       # follow (like tail -f)
journalctl -b                        # logs since current boot
journalctl -b -1                     # logs from the previous boot
journalctl --since "1 hour ago"      # time-based filter
journalctl --since "09:00" --until "10:00"
journalctl -p err                    # only errors and above
journalctl -p err..crit              # range of priorities
journalctl -u nginx.service --since "2026-01-01" -p warning
```

Priority levels (from lowest to highest): `debug`, `info`, `notice`, `warning`, `err`, `crit`, `alert`, `emerg`.

## Hands-On Exercise

<ExerciseCard
  title="Create a Self-Healing Service with systemd"
  difficulty="Foundation"
  scenario="Your team deployed a long-running script that needs to run continuously on a server. After the last system reboot, it didn't start automatically, and when it crashed due to a transient error, nobody noticed for hours. You need to create a proper systemd service that starts on boot and automatically restarts on failure."
  objective="By the end of this exercise, you will be able to write a unit file, register it with systemd, control it with systemctl, and use journalctl to read its output."
  annotated={true}
  steps={[
    {
      step: 1,
      description: "Start the systemd-enabled lab container. The --privileged and --tmpfs flags are required for systemd to function correctly inside a container.",
      command: "docker run -d --name sysadmin-lab --privileged --tmpfs /tmp --tmpfs /run --tmpfs /run/lock -v /sys/fs/cgroup:/sys/fs/cgroup:ro jrei/systemd-ubuntu:22.04",
      annotations: [
        { token: "docker", description: "Runs the Docker CLI, which communicates with the Docker daemon to manage images and containers." },
        { token: "run", description: "Creates and starts a new container from the specified image." },
        { token: "-d", description: "Runs the container in detached mode so it keeps running in the background after the command returns." },
        { token: "--name", description: "Assigns a human-readable name to the container so you can reference it by name instead of a random ID." },
        { token: "sysadmin-lab", description: "The container name to assign — used in later commands like docker exec to identify this specific container." },
        { token: "--privileged", description: "Grants the container extended permissions including full access to host devices and kernel features required by systemd." },
        { token: "--tmpfs", description: "Mounts a temporary in-memory filesystem at the specified path, required for systemd to write runtime state." },
        { token: "/tmp", description: "Path for the first tmpfs mount — systemd uses this directory for temporary files during service startup." },
        { token: "--tmpfs", description: "Mounts a second temporary in-memory filesystem, required for systemd PID files and socket files." },
        { token: "/run", description: "Path for the second tmpfs mount — systemd stores runtime state, sockets, and PID files here." },
        { token: "--tmpfs", description: "Mounts a third temporary in-memory filesystem for systemd lock files." },
        { token: "/run/lock", description: "Path for the third tmpfs mount — systemd uses this directory for lock files to serialize access to resources." },
        { token: "-v", description: "Binds a host directory into the container, making the host path accessible at the container path." },
        { token: "/sys/fs/cgroup:/sys/fs/cgroup:ro", description: "Mounts the host cgroup filesystem into the container as read-only so systemd can manage process resource limits." },
        { token: "jrei/systemd-ubuntu:22.04", description: "Specifies the image to use — a systemd-enabled Ubuntu 22.04 image that includes all required init system components." }
      ]
    },
    {
      step: 2,
      description: "Wait a moment for systemd to finish initializing, then open a shell in the running container.",
      command: "docker exec -it sysadmin-lab bash",
      annotations: [
        { token: "docker", description: "Runs the Docker CLI to communicate with the Docker daemon." },
        { token: "exec", description: "Runs a command inside an already-running container without creating a new container." },
        { token: "-it", description: "Allocates a pseudo-TTY and keeps stdin open so you can interact with the shell session." },
        { token: "sysadmin-lab", description: "The name of the running container to execute the command inside." },
        { token: "bash", description: "The command to run inside the container — opens an interactive Bash shell." }
      ]
    },
    {
      step: 3,
      description: "Create the script that the service will run. The -e flag on echo prevents literal backslash-n interpretation issues. This script loops forever, printing a message every 5 seconds.",
      command: "cat > /usr/local/bin/hello.sh << 'EOF'\n#!/bin/bash\nwhile true; do\n  echo \"[$(date '+%Y-%m-%d %H:%M:%S')] Hello from systemd service\"\n  sleep 5\ndone\nEOF\nchmod +x /usr/local/bin/hello.sh",
      annotations: [
        { token: "cat", description: "Reads from standard input and writes to standard output — used here to write a heredoc to a file via redirection." },
        { token: ">", description: "Redirects stdout to the specified file path, creating the file if it does not exist or overwriting it if it does." },
        { token: "/usr/local/bin/hello.sh", description: "The destination path for the script file — /usr/local/bin is in the default PATH so the script can be called by name." },
        { token: "chmod", description: "Changes the file permission bits to control who can read, write, or execute the file." },
        { token: "+x", description: "Adds the execute permission bit for owner, group, and others so the script can be run as a program." },
        { token: "/usr/local/bin/hello.sh", description: "The script file to make executable — required before systemd can call it as the ExecStart command." }
      ]
    },
    {
      step: 4,
      description: "Write the systemd unit file. Type=simple means the process stays in the foreground. Restart=on-failure means it restarts only if it exits with a non-zero status.",
      command: "cat > /etc/systemd/system/hello.service << 'EOF'\n[Unit]\nDescription=Hello World Service\nAfter=network.target\n\n[Service]\nType=simple\nExecStart=/usr/local/bin/hello.sh\nRestart=on-failure\nRestartSec=5s\nStandardOutput=journal\nStandardError=journal\n\n[Install]\nWantedBy=multi-user.target\nEOF",
      annotations: [
        { token: "cat", description: "Reads from standard input and writes to standard output — used here to write a heredoc into the unit file via redirection." },
        { token: ">", description: "Redirects stdout to the specified file path, creating or overwriting the unit file." },
        { token: "/etc/systemd/system/hello.service", description: "The unit file path in the local override directory — systemd reads unit files from here during daemon-reload." }
      ]
    },
    {
      step: 5,
      description: "Tell systemd to reload its unit file configuration. This must be done every time you create or modify a unit file — otherwise systemd still has the old (or absent) version in memory.",
      command: "systemctl daemon-reload",
      annotations: [
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "daemon-reload", description: "Instructs systemd to re-read all unit files from disk, picking up any new or modified service definitions." }
      ]
    },
    {
      step: 6,
      description: "Start the service and check its status immediately. Active: active (running) confirms it started successfully.",
      command: "systemctl start hello.service && systemctl status hello.service",
      annotations: [
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "start", description: "Activates the specified unit immediately, launching the process defined in ExecStart." },
        { token: "hello.service", description: "The name of the unit to start — systemd looks up this name in its loaded unit file table." },
        { token: "&&", description: "Runs the next command only if the previous command exited with success, so status is checked only after a successful start." },
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "status", description: "Displays the current state of the unit including active/inactive status, PID, recent journal output, and last exit code." },
        { token: "hello.service", description: "The unit to inspect — shows whether it reached active (running) state after the start command." }
      ]
    },
    {
      step: 7,
      description: "Enable the service so it starts automatically on the next boot. This creates a symlink in /etc/systemd/system/multi-user.target.wants/.",
      command: "systemctl enable hello.service",
      annotations: [
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "enable", description: "Installs the unit for automatic startup by creating a symlink in the WantedBy target directory." },
        { token: "hello.service", description: "The unit to enable — systemd will start this service automatically when the system reaches multi-user.target." }
      ]
    },
    {
      step: 8,
      description: "Read the service's log output using journalctl. The -f flag follows new entries as they arrive (like tail -f). Press Ctrl+C to stop following.",
      command: "journalctl -u hello.service -f",
      annotations: [
        { token: "journalctl", description: "Queries and displays entries from the systemd journal, the structured binary log store." },
        { token: "-u", description: "Filters journal output to show only entries from the specified systemd unit." },
        { token: "hello.service", description: "The unit name to filter by — shows only log entries produced by this service." },
        { token: "-f", description: "Follows the journal in real time, printing new entries as they arrive (similar to tail -f on a log file)." }
      ]
    },
    {
      step: 9,
      description: "Stop the service and verify it stopped. Active: inactive (dead) is the expected state.",
      command: "systemctl stop hello.service && systemctl status hello.service",
      annotations: [
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "stop", description: "Deactivates the specified unit, sending SIGTERM to the process and waiting for it to exit." },
        { token: "hello.service", description: "The unit to stop — systemd sends the stop signal to the running process for this service." },
        { token: "&&", description: "Runs the next command only if the previous command exited with success, so status is checked only after a successful stop." },
        { token: "systemctl", description: "The primary command-line tool for managing systemd units, services, and system state." },
        { token: "status", description: "Displays the current state of the unit — should show inactive (dead) after a successful stop." },
        { token: "hello.service", description: "The unit to inspect — confirms the service has transitioned to the inactive state." }
      ]
    },
    {
      step: 10,
      description: "Clean up the container when done.",
      command: "exit",
      annotations: [
        { token: "exit", description: "Ends the current shell session, returning control to the host terminal and closing the container exec connection." }
      ]
    }
  ]}
>
  <ScenarioQuestion
    question="Your hello.service crashed and systemd restarted it, but 10 minutes later it crashed again. You need to find out what error it logged just before it crashed. What journalctl command would show you only the error-level messages from hello.service across all restarts?"
    answer="Run journalctl -u hello.service -p err to filter the service journal to error priority and above. This shows only the err, crit, alert, and emerg messages from every run of the service, across all restarts, without the noise of normal info output. If you want to narrow it to the last restart only, combine it with --since to specify a time window. The -p flag maps directly to syslog priority levels, so -p err means priority 3 and below (lower number = more severe)."
  />
  <ScenarioQuestion
    question="You modified the ExecStart path in hello.service but after running systemctl restart hello.service the service is still launching the old script path. You confirmed the unit file on disk has the new path. What did you forget, and what is the correct sequence of commands to apply the change?"
    answer="You forgot to run systemctl daemon-reload before restarting. systemd loads unit files into memory when it first reads them — modifying the file on disk does not update the in-memory configuration. systemctl restart uses the cached in-memory version. The correct sequence is: systemctl daemon-reload (re-reads unit files from disk), then systemctl restart hello.service (stops and starts using the updated configuration). Skipping daemon-reload is a common mistake that causes confusing behavior where your edits appear to have no effect."
  />
  <VerificationChecklist
    title="Verify Your Work"
    items={[
      {
        id: "service-file",
        label: "Unit file exists at /etc/systemd/system/hello.service",
        hint: "Run: cat /etc/systemd/system/hello.service — should show the [Unit], [Service], and [Install] sections"
      },
      {
        id: "service-active",
        label: "systemctl status hello.service shows Active: active (running)",
        hint: "Run: systemctl is-active hello.service — should print 'active'"
      },
      {
        id: "service-enabled",
        label: "The service is enabled (will start on boot)",
        hint: "Run: systemctl is-enabled hello.service — should print 'enabled'"
      },
      {
        id: "journal-output",
        label: "journalctl shows timestamped output from the service",
        hint: "Run: journalctl -u hello.service --no-pager | tail -10 — should show lines like '[2026-...] Hello from systemd service'"
      }
    ]}
  />
</ExerciseCard>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "systemctl — Service Control",
      items: [
        { command: "systemctl start <unit>", description: "Start a unit immediately", example: "systemctl start nginx" },
        { command: "systemctl stop <unit>", description: "Stop a unit (sends SIGTERM, then SIGKILL)", example: "systemctl stop nginx" },
        { command: "systemctl restart <unit>", description: "Stop and start a unit", example: "systemctl restart nginx" },
        { command: "systemctl reload <unit>", description: "Send SIGHUP to reload config without restart", example: "systemctl reload nginx" },
        { command: "systemctl status <unit>", description: "Show current state, recent logs, and PID", example: "systemctl status sshd" },
        { command: "systemctl enable <unit>", description: "Enable unit to start on boot (creates symlink)", example: "systemctl enable myapp" },
        { command: "systemctl disable <unit>", description: "Disable unit from starting on boot", example: "systemctl disable myapp" },
        { command: "systemctl daemon-reload", description: "Reload unit file configurations from disk — required after editing unit files", example: "systemctl daemon-reload" },
        { command: "systemctl is-active <unit>", description: "Returns 'active' or 'inactive' (exit 0 or 1)", example: "systemctl is-active nginx" },
        { command: "systemctl is-enabled <unit>", description: "Returns 'enabled' or 'disabled'", example: "systemctl is-enabled nginx" },
        { command: "systemctl list-units --type=service", description: "List all loaded service units and their state", example: "systemctl list-units --type=service --state=running" },
      ]
    },
    {
      title: "journalctl — Log Queries",
      items: [
        { command: "journalctl -u <unit>", description: "Show all logs for a specific unit", example: "journalctl -u nginx.service" },
        { command: "journalctl -u <unit> -f", description: "Follow logs for a unit in real time", example: "journalctl -u myapp.service -f" },
        { command: "journalctl -b", description: "Logs from the current boot only", example: "journalctl -b" },
        { command: "journalctl -p err", description: "Only log entries at error priority or above", example: "journalctl -p err -b" },
        { command: "journalctl --since '1 hour ago'", description: "Logs from the last hour", example: "journalctl --since '2 hours ago' --until '1 hour ago'" },
        { command: "journalctl -n 50", description: "Show the last 50 log entries", example: "journalctl -n 50 -u nginx" },
      ]
    },
    {
      title: "Unit File Directives",
      items: [
        { command: "Type=simple", description: "Process stays in foreground — the most common type for modern services" },
        { command: "Type=oneshot", description: "Run once and exit — for scripts and setup tasks" },
        { command: "Restart=on-failure", description: "Restart only when process exits with non-zero status" },
        { command: "Restart=always", description: "Restart regardless of how the process exits" },
        { command: "After=network.target", description: "Start after network is up — ordering dependency (not hard)" },
        { command: "WantedBy=multi-user.target", description: "Enable as part of normal boot — soft dependency from target to unit" },
      ]
    }
  ]}
/>

export const quiz = [
  {
    id: "sysd-q1",
    question: "What is PID 1 in a Linux system, and what happens if it dies?",
    options: [
      "PID 1 is the first user login session; if it dies, that user is logged out",
      "PID 1 is systemd; if it dies, the kernel panics",
      "PID 1 is the init script runner; if it dies, the boot process pauses until it restarts",
      "PID 1 is the cron daemon; if it dies, scheduled tasks stop running"
    ],
    correctIndex: 1,
    explanation: "PID 1 is the first process the kernel starts in user space — in modern Linux this is systemd. Every other process on the system is a child or descendant of PID 1. If PID 1 dies, the kernel has no process to hand off to and triggers a kernel panic, which crashes the system.",
  },
  {
    id: "sysd-q2",
    question: "What is the purpose of 'systemctl daemon-reload'?",
    options: [
      "It restarts all running services to pick up configuration changes",
      "It re-reads unit files from disk so in-memory configuration matches the file system",
      "It reloads the kernel modules required by systemd",
      "It resets failed services back to an inactive state"
    ],
    correctIndex: 1,
    explanation: "systemd loads unit files into memory when it first reads them. Modifying a unit file on disk does not automatically update systemd's in-memory configuration. systemctl daemon-reload instructs systemd to re-read all unit files from disk, making any new or modified unit definitions available. Forgetting this step causes confusing behavior where edits appear to have no effect.",
  },
  {
    id: "sysd-q3",
    question: "What does 'systemctl enable myapp.service' do, and what does it NOT do?",
    options: [
      "It starts the service immediately and marks it to start on future boots",
      "It creates a symlink for boot startup but does NOT start the service now",
      "It installs the service binary and creates the unit file automatically",
      "It sets the service as high-priority and adds it to the default target"
    ],
    correctIndex: 1,
    explanation: "systemctl enable creates a symlink in the WantedBy target directory so the service starts automatically on the next boot. It does NOT start the service immediately. To both enable and start a service in one command, use systemctl enable --now myapp.service.",
  },
  {
    id: "sysd-q4",
    question: "What is the difference between 'After=network.target' and 'Requires=network.target' in a unit file's [Unit] section?",
    options: [
      "After= starts the service in parallel with network; Requires= waits for network first",
      "After= is only for ordering (starts after network is active); Requires= is a hard dependency that fails the service if network fails",
      "They are equivalent — both ensure the service does not start before network is up",
      "Requires= is deprecated; After= is the modern replacement for all dependency types"
    ],
    correctIndex: 1,
    explanation: "After= controls ordering only — the service starts after network.target is active, but if network.target fails, this service still attempts to start. Requires= is a hard dependency — if the required unit fails or cannot be activated, this unit will also fail. Wants= is a soft dependency that starts the required unit but does not fail if it is unavailable.",
  },
  {
    id: "sysd-q5",
    question: "A unit file has 'Restart=on-failure'. Under which condition will systemd NOT automatically restart the service?",
    options: [
      "The service process is killed with SIGKILL",
      "The service exits with a non-zero exit code",
      "The service exits with exit code 0 (success)",
      "The service crashes due to a segmentation fault"
    ],
    correctIndex: 2,
    explanation: "Restart=on-failure restarts the service only when it exits with a non-zero exit status, is killed by a signal (except SIGTERM from systemctl stop), or hits a timeout. If the process exits cleanly with code 0, systemd considers the job done and does not restart it. Use Restart=always to restart regardless of exit status.",
  },
  {
    id: "sysd-q6",
    question: "Where should you place custom service unit files that you create, and why?",
    options: [
      "/lib/systemd/system/ — this directory is scanned first and has highest priority",
      "/etc/systemd/system/ — this is for admin-created units and overrides; package units go in /lib/systemd/system/",
      "/usr/local/lib/systemd/ — this directory is for locally installed services",
      "/run/systemd/system/ — this directory persists unit files across reboots"
    ],
    correctIndex: 1,
    explanation: "/etc/systemd/system/ is the correct location for custom and admin-created unit files. /lib/systemd/system/ is reserved for units installed by distribution packages and should not be edited directly, as package upgrades can overwrite changes. Files in /etc/systemd/system/ take precedence over /lib/systemd/system/ and survive package updates.",
  },
  {
    id: "sysd-q7",
    question: "What journalctl command shows only error-level messages from a specific service?",
    options: [
      "journalctl -u myapp.service --level=error",
      "journalctl -u myapp.service -p err",
      "journalctl -u myapp.service --filter=err",
      "journalctl -u myapp.service -s error"
    ],
    correctIndex: 1,
    explanation: "The -p flag filters journal output by syslog priority level. -p err shows entries at error priority and above: err, crit, alert, and emerg. The -u flag scopes the output to a specific unit. Lower priority numbers are more severe — err is priority 3, so -p err shows priorities 0 through 3.",
  },
  {
    id: "sysd-q8",
    question: "What does 'WantedBy=multi-user.target' in the [Install] section of a unit file cause to happen when 'systemctl enable' is run?",
    options: [
      "systemd adds the unit to the kernel boot table so it starts before user space",
      "systemd creates a symlink in /etc/systemd/system/multi-user.target.wants/ pointing to the unit file",
      "systemd modifies /etc/fstab to ensure the service dependencies are mounted first",
      "systemd sends SIGHUP to multi-user.target to incorporate the new unit immediately"
    ],
    correctIndex: 1,
    explanation: "When you enable a service with WantedBy=multi-user.target, systemd creates a symlink in /etc/systemd/system/multi-user.target.wants/ that points to your unit file. When the system reaches multi-user.target during boot, systemd starts all units symlinked in that target's wants directory — which now includes your service.",
  },
  {
    id: "sysd-q9",
    question: "What is the key advantage of systemd's 'Type=oneshot' versus 'Type=simple' for a backup script?",
    options: [
      "Type=oneshot allows the script to run with root privileges without a password",
      "Type=simple keeps the process in the foreground; Type=oneshot is for scripts that run once and exit",
      "Type=oneshot provides better logging than Type=simple",
      "Type=simple requires a network connection; Type=oneshot works offline"
    ],
    correctIndex: 1,
    explanation: "Type=simple expects the service process to stay running in the foreground — it is for long-running daemons. Type=oneshot is designed for processes that complete their work and exit, like backup scripts or setup tasks. With Type=oneshot, systemd correctly considers the service 'done' after the process exits with 0, rather than treating a clean exit as a failure.",
  },
  {
    id: "sysd-q10",
    question: "Where does journald store logs by default, and what must you do to make logs persist across reboots?",
    options: [
      "Logs are stored in /var/log/journal/ by default and persist automatically",
      "Logs are stored in /run/log/journal/ (volatile tmpfs) by default; creating /var/log/journal/ enables persistence",
      "Logs are stored in /tmp/journal/ and must be manually archived to persist",
      "Logs are stored in /etc/journal/ and persist by default unless the directory is deleted"
    ],
    correctIndex: 1,
    explanation: "By default, journald stores logs in /run/log/journal/ which is a tmpfs — a volatile in-memory filesystem that is cleared on every reboot. To make logs persist across reboots, you must create the /var/log/journal/ directory, run systemd-tmpfiles --create --prefix /var/log/journal, and restart systemd-journald. Once this directory exists, journald will use it for persistent storage.",
  },
]
