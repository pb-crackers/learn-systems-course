---
title: "Disk Management: Block Devices, Filesystems, and Mounts"
description: "How block devices, partitions, filesystems, and mount points work — before fdisk or mount commands"
module: "System Administration"
moduleSlug: "04-sysadmin"
lessonSlug: "04-disk-management"
order: 4
difficulty: "Intermediate"
estimatedMinutes: 30
prerequisites: ["01-linux-fundamentals/03-linux-filesystem", "04-sysadmin/01-user-management"]
tags: ["disk", "fdisk", "mount", "fstab", "lvm", "df", "du", "block-devices", "filesystems"]
---

## Overview

A production database is running slow. `df -h` shows the data volume at 97% capacity. You need to add storage, partition it, format it, and mount it without taking down the database. Do this wrong — a bad `/etc/fstab` entry — and the server will not boot. Understanding block devices, filesystems, and mount points from first principles is the difference between a clean maintenance window and an emergency recovery.

## How It Works

### Block Devices: The Kernel's Disk Abstraction

The Linux kernel presents every storage device — hard drive, SSD, USB drive, cloud volume — as a **block device** file in `/dev/`. A block device is a file that you can read from and write to in fixed-size blocks (typically 512 bytes or 4KB).

```
/dev/sda        # first SCSI/SATA disk
/dev/sdb        # second disk
/dev/vda        # first virtio disk (cloud VMs, KVM)
/dev/nvme0n1    # first NVMe SSD
/dev/sda1       # first partition on /dev/sda
/dev/sda2       # second partition on /dev/sda
```

<Callout type="deep-dive" title="Everything Is a File — Including Disks">
In the Linux Fundamentals lesson on the filesystem, you learned that everything in Linux is a file — including devices. `/dev/sda` is a file that represents a disk. When you run `fdisk /dev/sda`, you are reading and writing that file's contents — which happen to be the partition table. When you run `dd if=/dev/zero of=/dev/sda`, you are overwriting the raw disk data byte-by-byte. The file abstraction makes every disk operation composable with standard Unix tools.
</Callout>

Use `lsblk` to see the block device tree without modifying anything:

```bash
$ lsblk
NAME   MAJ:MIN RM  SIZE RO TYPE MOUNTPOINT
sda      8:0    0   20G  0 disk
├─sda1   8:1    0   19G  0 part /
└─sda2   8:2    0    1G  0 part [SWAP]
sdb      8:16   0  100G  0 disk    ← unpartitioned second disk
```

### Partitions: Dividing a Disk

A **partition** is a logically isolated region of a disk, defined by a start sector and end sector in the partition table. The partition table lives in the first sector(s) of the disk.

Two partition table formats exist:

| Format | Max disk size | Max partitions | Notes |
|--------|--------------|----------------|-------|
| MBR (Master Boot Record) | 2TB | 4 primary (or 3 primary + extended) | Legacy, 1980s design |
| GPT (GUID Partition Table) | 9.4ZB | 128 by default | Modern, required for disks > 2TB and UEFI boot |

For new systems, always use GPT unless there is a specific reason to use MBR.

`fdisk` is the classic partition editor (interactive or scriptable). `parted` and `gdisk` are alternatives that handle GPT better. Basic `fdisk` workflow:

```bash
fdisk /dev/sdb       # open interactive partition editor
# Inside fdisk:
# g  — create new GPT partition table (destroys all data)
# n  — add new partition
# p  — print current partition table
# w  — write changes to disk and exit
# q  — quit WITHOUT writing
```

### Filesystems: Structure on a Partition

A raw partition is just a sequence of blocks — no directories, no files, no names. A **filesystem** provides the structure: a way to organize blocks into files and directories, track free space, store metadata (permissions, timestamps, ownership).

Creating a filesystem writes its metadata structures to the partition:

```bash
mkfs.ext4 /dev/sdb1        # create ext4 filesystem on first partition of sdb
mkfs.xfs /dev/sdb1         # create XFS filesystem
mkfs.vfat /dev/sdb1        # create FAT32 (for USB drives, cross-platform)
```

**ext4** is the standard Linux filesystem (Ubuntu default). **XFS** is used for large-scale deployments (databases, large files). Once created, the filesystem type is recorded in the partition and can be detected automatically.

### The Mount Operation

A filesystem becomes accessible by **mounting** it — attaching it at a directory in the existing filesystem tree. The directory it attaches to is called the **mount point**:

```bash
mkdir /mnt/data              # create the mount point directory
mount /dev/sdb1 /mnt/data    # attach the filesystem there
```

After mounting, everything in `/mnt/data/` reads from and writes to `/dev/sdb1`. The mount point directory acts as the entry point. Files written to `/mnt/data/` are stored on the `sdb1` partition.

```bash
umount /mnt/data     # detach the filesystem (must not be in use)
mount | grep data    # list currently mounted filesystems (shows all mounts)
```

### /etc/fstab: Persistent Mount Configuration

`mount` mounts a filesystem for the current boot session only. To make it persistent across reboots, add it to `/etc/fstab`. The kernel reads this file at boot and mounts every entry.

`/etc/fstab` has six whitespace-separated fields:

```
# Device          Mount point   FS type   Options         Dump  Pass
UUID=a1b2c3d4...  /mnt/data     ext4      defaults,nofail  0     2
/dev/sdb1         /mnt/backup   xfs       defaults         0     0
tmpfs             /tmp          tmpfs     defaults,noatime 0     0
```

| Field | Values | Meaning |
|-------|--------|---------|
| Device | UUID=... or /dev/sdX | What to mount. Prefer UUID — device names change. |
| Mount point | /mnt/data | Where to attach it in the directory tree |
| FS type | ext4, xfs, tmpfs, nfs | Filesystem type |
| Options | defaults, ro, noatime, nofail | Mount options (comma-separated) |
| Dump | 0 or 1 | Whether `dump` backup tool includes this filesystem (0 = skip) |
| Pass | 0, 1, or 2 | `fsck` check order at boot (0=skip, 1=root first, 2=others) |

**Important options:**
- `defaults` — standard options (rw, suid, dev, exec, auto, nouser, async)
- `nofail` — do not fail boot if this device is absent (use for non-critical mounts)
- `noatime` — do not update access timestamps on reads (performance optimization)
- `ro` — mount read-only

**Why UUIDs instead of device names:** Device names (`/dev/sda`, `/dev/sdb`) are assigned by the kernel in the order devices are detected at boot. If you add or remove a disk, the order can change — `/dev/sdb` might become `/dev/sda` on next boot. UUIDs are stable identifiers written into the filesystem at `mkfs` time. Get the UUID with `blkid /dev/sdb1`.

<Callout type="warning" title="Test fstab Before Rebooting">
Never add a new `/etc/fstab` entry without testing it first. A bad fstab entry causes the system to drop into emergency mode on the next boot — and recovering from emergency mode requires console access or a rescue disk.

Test procedure:
1. Add the fstab entry
2. Run `mount -a` — this mounts all unmounted fstab entries and will fail immediately if there is a syntax error or bad device
3. Only reboot after `mount -a` succeeds

Also use the `nofail` option for non-critical mounts (external drives, secondary data volumes). With `nofail`, if the device is absent at boot (disconnected drive, unattached cloud volume), the system boots normally instead of entering emergency mode.
</Callout>

### df vs du: Two Views of Storage Usage

These tools answer fundamentally different questions:

**`df` (disk free)** — reports at the **filesystem level**: how much space is allocated to the filesystem vs available:

```bash
$ df -h
Filesystem      Size  Used Avail Use% Mounted on
/dev/sda1        19G   12G  6.5G  65% /
/dev/sdb1        99G   45G   49G  48% /mnt/data
tmpfs           2.0G     0  2.0G   0% /dev/shm
```

**`du` (disk usage)** — reports at the **directory level**: how much space the files in a directory tree are actually using:

```bash
$ du -sh /var/log/*
4.0K    /var/log/apt
48M     /var/log/journal
2.4M    /var/log/syslog
512K    /var/log/nginx
```

Use `df -h` to identify which filesystem is full. Use `du -sh /some/directory/*` to find what is consuming the space within that filesystem.

### LVM Basics (Concept Overview)

**LVM (Logical Volume Manager)** adds a virtualization layer between physical disks and filesystems:

```
Physical disks (PV) → Volume Group (VG) → Logical Volumes (LV) → Filesystems
   /dev/sdb           vg-data               lv-app    lv-db
   /dev/sdc                                  100G       200G
```

LVM allows:
- **Online resize**: grow a logical volume and filesystem without unmounting
- **Snapshots**: point-in-time copy of a volume for backups
- **Spanning**: one logical volume can span multiple physical disks

Full LVM administration (vgcreate, lvcreate, lvextend, pvmove) is a topic for advanced system administration. The key concept to understand now: when you see `/dev/mapper/ubuntu--vg-ubuntu--lv` as a root device, you are on an LVM system — the `vg-data` volume group contains `lv-ubuntu` which is the actual logical volume your filesystem lives on.

## Hands-On Exercise

<ExerciseCard
  title="Investigate Disk Usage and Set Up a Data Partition"
  difficulty="Intermediate"
  scenario="Your server's root filesystem is at 80% capacity. Your application logs are growing rapidly. You need to identify what is consuming the space, then create and mount a dedicated partition for application data using a loopback device to simulate a real disk."
  objective="By the end of this exercise, you will be able to use df and du to diagnose storage issues, create a filesystem on a loopback device, mount it, and write a correct fstab entry."
  steps={[
    {
      step: 1,
      description: "Launch the lab container with privileged mode (required for losetup to create loopback devices).",
      command: "docker run --rm -it --privileged ubuntu:22.04 bash"
    },
    {
      step: 2,
      description: "Install the tools needed for this exercise: util-linux (losetup, fdisk), e2fsprogs (mkfs.ext4), and procps (for reference).",
      command: "apt-get update -qq && apt-get install -y util-linux e2fsprogs fdisk 2>/dev/null; echo 'Tools ready'"
    },
    {
      step: 3,
      description: "Check the current disk layout and filesystem usage. df -h shows human-readable sizes.",
      command: "df -h && echo '---' && lsblk"
    },
    {
      step: 4,
      description: "Identify the top disk consumers in /var. du -sh shows the size of each item, sorted by size with sort -rh.",
      command: "du -sh /var/* 2>/dev/null | sort -rh | head -10"
    },
    {
      step: 5,
      description: "Create a 512MB file to simulate a new disk. truncate creates a sparse file — it appears to be 512MB but does not actually consume that much space until written.",
      command: "truncate -s 512M /tmp/disk.img && ls -lh /tmp/disk.img"
    },
    {
      step: 6,
      description: "Associate the file with a loopback block device. This makes /dev/loop0 behave exactly like a real disk — you can partition it and create filesystems on it.",
      command: "losetup /dev/loop0 /tmp/disk.img && losetup -l"
    },
    {
      step: 7,
      description: "Create an ext4 filesystem directly on the loopback device. In a real scenario you would partition first with fdisk, but for a single-use data volume, creating a filesystem on the whole device is acceptable.",
      command: "mkfs.ext4 /dev/loop0 && echo 'Filesystem created'"
    },
    {
      step: 8,
      description: "Create the mount point directory and mount the new filesystem.",
      command: "mkdir -p /mnt/appdata && mount /dev/loop0 /mnt/appdata && df -h /mnt/appdata"
    },
    {
      step: 9,
      description: "Verify the mount by writing a test file and checking it persists.",
      command: "echo 'Application data here' > /mnt/appdata/test.txt && cat /mnt/appdata/test.txt && ls -la /mnt/appdata/"
    },
    {
      step: 10,
      description: "Get the UUID of the new filesystem. In production you would use this UUID in /etc/fstab instead of the device name.",
      command: "blkid /dev/loop0"
    },
    {
      step: 11,
      description: "Write the fstab entry. Using nofail ensures the system boots even if this device is absent (important for non-root volumes).",
      command: "echo '/dev/loop0  /mnt/appdata  ext4  defaults,nofail  0  2' >> /etc/fstab && tail -3 /etc/fstab"
    },
    {
      step: 12,
      description: "Test the fstab entry by unmounting and remounting with mount -a (simulating what happens at boot).",
      command: "umount /mnt/appdata && mount -a && df -h /mnt/appdata && cat /mnt/appdata/test.txt"
    }
  ]}
>
  <ScenarioQuestion
    question="After mounting /mnt/appdata, you write files there. Then the system reboots and /mnt/appdata is empty — the mount point directory exists but the filesystem is not mounted. You open /etc/fstab and confirm the entry looks correct. What is the most likely cause, and how would you diagnose it?"
    answer="The most likely cause is a missing nofail option combined with a device that is temporarily unavailable at boot — but with nofail in place, the symptom would be different. A more common cause is using a device name like /dev/loop0 that does not persist across reboots (loopback devices are not re-created automatically after reboot). In production, fstab entries should use the UUID obtained from blkid, not the device path — UUIDs are stable even if the device enumeration order changes. To diagnose, check journalctl -b for mount errors, verify the device exists with lsblk, and confirm the fstab UUID matches blkid output."
  />
  <ScenarioQuestion
    question="Your server's root filesystem is at 92% capacity. df -h shows / at 92%, but du -sh /* shows the top-level directories only consume 40GB of the 50GB total. How do you explain the gap, and what tool would reveal what is actually consuming the remaining space?"
    answer="The gap usually means deleted files that are still held open by running processes. When a process opens a file, the filesystem does not release the blocks even after the file is deleted — the space is only freed when the process closes its file handle. This is common with application logs that were deleted but the application is still writing to the file descriptor. To find these, run lsof | grep deleted — it will show all files that have been deleted but still have open handles. To reclaim the space without restarting the process, you can truncate the file to zero bytes: truncate -s 0 /proc/PID/fd/FD where FD is the file descriptor number from lsof output."
  />
  <VerificationChecklist
    title="Verify Your Work"
    items={[
      {
        id: "df-shows-mount",
        label: "df -h shows /mnt/appdata as a mounted 512MB filesystem",
        hint: "Run: df -h /mnt/appdata — should show /dev/loop0 with ~476M size (512MB - filesystem overhead)"
      },
      {
        id: "fstab-entry",
        label: "/etc/fstab contains the /mnt/appdata entry with nofail option",
        hint: "Run: grep appdata /etc/fstab — should show the /dev/loop0 entry with defaults,nofail options"
      },
      {
        id: "mount-a-works",
        label: "mount -a succeeds after umount (simulates boot behavior)",
        hint: "Run: umount /mnt/appdata && mount -a — should complete without errors. If mount -a fails, there is an error in your fstab entry."
      },
      {
        id: "data-persists",
        label: "Data written to /mnt/appdata persists after umount and remount",
        hint: "Run: cat /mnt/appdata/test.txt — should show your test content after remounting"
      }
    ]}
  />
</ExerciseCard>

## Quick Reference

<QuickReference
  sections={[
    {
      title: "Disk Investigation",
      items: [
        { command: "lsblk", description: "List block devices in a tree — shows disks, partitions, and mount points", example: "lsblk" },
        { command: "blkid /dev/sdb1", description: "Show UUID, filesystem type, and label of a partition", example: "blkid /dev/sda1" },
        { command: "df -h", description: "Filesystem-level disk usage (human-readable sizes)", example: "df -h" },
        { command: "du -sh /path/*", description: "Directory-level disk usage — find what is consuming space", example: "du -sh /var/log/* | sort -rh | head -20" },
        { command: "mount | grep <device>", description: "Show all active mounts or mounts for a specific device", example: "mount | grep sdb" },
      ]
    },
    {
      title: "Partition and Format",
      items: [
        { command: "fdisk /dev/sdb", description: "Interactive partition editor — g creates GPT table, n adds partition, w writes and exits", example: "fdisk /dev/sdb" },
        { command: "mkfs.ext4 /dev/sdb1", description: "Create ext4 filesystem on a partition", example: "mkfs.ext4 -L data /dev/sdb1" },
        { command: "mkfs.xfs /dev/sdb1", description: "Create XFS filesystem (preferred for large data volumes)", example: "mkfs.xfs /dev/sdb1" },
        { command: "e2label /dev/sdb1 data", description: "Set a label on an ext4 filesystem (use in fstab as LABEL=data)", example: "e2label /dev/sdb1 appdata" },
      ]
    },
    {
      title: "Mount Operations",
      items: [
        { command: "mount /dev/sdb1 /mnt/data", description: "Mount a filesystem at a directory", example: "mount /dev/sdb1 /mnt/data" },
        { command: "mount -a", description: "Mount all entries in /etc/fstab that are not yet mounted — test fstab entries with this before rebooting", example: "mount -a" },
        { command: "umount /mnt/data", description: "Unmount a filesystem (fails if any process has files open there)", example: "umount /mnt/data" },
        { command: "fuser /mnt/data", description: "Find processes with files open on a mount (useful when umount says 'device is busy')", example: "fuser -vm /mnt/data" },
      ]
    },
    {
      title: "Loopback Devices (Lab Use)",
      items: [
        { command: "truncate -s 512M /tmp/disk.img", description: "Create a sparse 512MB file to use as a virtual disk", example: "truncate -s 1G /tmp/bigdisk.img" },
        { command: "losetup /dev/loop0 /tmp/disk.img", description: "Associate a file with a loopback block device", example: "losetup /dev/loop0 /tmp/disk.img" },
        { command: "losetup -d /dev/loop0", description: "Detach (release) a loopback device", example: "losetup -d /dev/loop0" },
        { command: "losetup -l", description: "List all active loopback devices and their associated files", example: "losetup -l" },
      ]
    }
  ]}
/>

export const quiz = [
  {
    id: "disk-q1",
    question: "A server has /dev/sda and /dev/sdb listed in lsblk output. You add a third disk but on the next boot /dev/sdb becomes /dev/sdc. Which /etc/fstab entry strategy survives this change without breaking the mount?",
    options: [
      "Use /dev/sdb in fstab — the kernel always preserves device names after the first boot",
      "Use UUID=<value> from blkid — UUIDs are written into the filesystem and remain stable regardless of device enumeration order",
      "Use LABEL=data in fstab — labels are maintained by the kernel when device names change",
      "Use /dev/disk/by-path/ — path-based names are stable because they reflect the physical bus address"
    ],
    correctIndex: 1,
    explanation: "Device names like /dev/sdb are assigned by the kernel based on the order devices are detected at boot, which can change when disks are added or removed. UUIDs are written into the filesystem metadata by mkfs and never change regardless of how the device is enumerated. Using UUID= in fstab ensures the correct filesystem is always mounted even after hardware changes.",
  },
  {
    id: "disk-q2",
    question: "What is the critical step to take before rebooting after adding a new /etc/fstab entry?",
    options: [
      "Run fsck on the new device to verify filesystem integrity before adding the entry",
      "Run mount -a to test that the fstab entry mounts successfully before rebooting",
      "Run blkid to confirm the device UUID matches the fstab entry",
      "Run lsblk to verify the partition is visible to the kernel"
    ],
    correctIndex: 1,
    explanation: "A bad fstab entry causes the system to drop into emergency mode on the next boot, requiring console access or a rescue disk to fix. Running mount -a mounts all unmounted fstab entries and immediately fails if there is a syntax error or missing device — giving you a chance to fix it while the system is still running. This is the mandatory test before any reboot after editing fstab.",
  },
  {
    id: "disk-q3",
    question: "What is the fundamental difference between what 'df -h' and 'du -sh /path' report?",
    options: [
      "df reports real-time usage while du reports a cached snapshot from the last scan",
      "df reports filesystem-level space allocation; du reports directory-level space consumed by actual files",
      "df includes swap space in its totals while du reports only file data",
      "df operates on mounted filesystems; du operates on block devices directly"
    ],
    correctIndex: 1,
    explanation: "df answers the filesystem-level question: of the total space allocated to this mounted filesystem, how much is used versus available. du answers the directory-level question: how much space do the files in this directory tree actually occupy. When df shows a filesystem near full but du totals seem lower, the gap typically indicates deleted files still held open by running processes.",
  },
  {
    id: "disk-q4",
    question: "You run 'mkfs.ext4 /dev/sdb1'. What does this command do and what irreversible consequence does it have?",
    options: [
      "It checks the ext4 filesystem on /dev/sdb1 for errors and repairs them",
      "It writes filesystem metadata structures to /dev/sdb1, destroying all existing data on that partition",
      "It expands an existing ext4 filesystem on /dev/sdb1 to use any newly available space",
      "It creates a filesystem image file named ext4 in the /dev/sdb1 directory"
    ],
    correctIndex: 1,
    explanation: "mkfs.ext4 (make filesystem) writes the ext4 metadata structures — superblock, inode tables, and block group descriptors — to the specified partition, overwriting whatever was there before. This is a destructive operation: all data on the partition is permanently lost. You should always double-check that you are targeting the correct device before running any mkfs command.",
  },
  {
    id: "disk-q5",
    question: "What is the correct sequence of commands to make a new disk partition available at /mnt/data?",
    options: [
      "mkfs.ext4 /dev/sdb1 → mount /dev/sdb1 /mnt/data → mkdir /mnt/data",
      "mkdir /mnt/data → mkfs.ext4 /dev/sdb1 → mount /dev/sdb1 /mnt/data",
      "fdisk /dev/sdb → mkfs.ext4 /dev/sdb1 → mkdir /mnt/data → mount /dev/sdb1 /mnt/data",
      "mount /dev/sdb1 /mnt/data → mkfs.ext4 /dev/sdb1 → mkdir /mnt/data"
    ],
    correctIndex: 2,
    explanation: "The full sequence is: first partition the disk with fdisk to create /dev/sdb1, then format the partition with mkfs.ext4 to create a filesystem, then create the mount point directory with mkdir, and finally mount the filesystem. The mount point directory must exist before mount can attach the filesystem to it.",
  },
  {
    id: "disk-q6",
    question: "What does the 'nofail' mount option in /etc/fstab do, and when is it especially important?",
    options: [
      "It prevents the filesystem from reporting errors — useful for temporary scratch disks",
      "It allows the boot process to continue even if this mount fails, instead of entering emergency mode",
      "It mounts the filesystem in read-only mode if the device fails its integrity check",
      "It disables the fsck check at boot for this filesystem, speeding up boot time"
    ],
    correctIndex: 1,
    explanation: "Without nofail, a missing device in fstab causes the boot process to drop into emergency mode. With nofail, the system boots normally even if the device is absent or the mount fails. This is especially important for secondary data volumes, external drives, and cloud-attached storage that might not always be present — you do not want a detached data volume to prevent the entire system from booting.",
  },
  {
    id: "disk-q7",
    question: "The lesson mentions two partition table formats. Which should you use for a new disk larger than 2TB?",
    options: [
      "MBR (Master Boot Record), because it has broader hardware compatibility",
      "GPT (GUID Partition Table), because MBR has a 2TB disk size limit",
      "LVM, which replaces partition tables on disks larger than 2TB",
      "ext4, which includes its own partition table for large disks"
    ],
    correctIndex: 1,
    explanation: "MBR (Master Boot Record) uses 32-bit addressing and can only address disks up to 2TB. GPT (GUID Partition Table) uses 64-bit addressing and supports disks up to 9.4 zettabytes with up to 128 partitions by default. For any disk larger than 2TB — or any new system using UEFI boot — you must use GPT. fdisk uses the 'g' command to create a new GPT partition table.",
  },
  {
    id: "disk-q8",
    question: "What is LVM's key advantage over traditional fixed partitions for a production database server?",
    options: [
      "LVM encrypts the data at rest, which traditional partitions cannot do",
      "LVM allows online resize of logical volumes without unmounting, enabling storage growth without downtime",
      "LVM provides faster I/O throughput by striping data across multiple disks automatically",
      "LVM automatically compresses data to use disk space more efficiently"
    ],
    correctIndex: 1,
    explanation: "LVM (Logical Volume Manager) interposes a virtualization layer between physical disks and filesystems. Its primary operational advantage is online resize — you can extend a logical volume and its filesystem while the database is running and the filesystem is mounted. With traditional fixed partitions, growing storage requires unmounting the filesystem, which means taking the database offline.",
  },
]
