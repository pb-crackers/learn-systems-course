---
phase: 15-content-authoring
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - content/modules/06-iac/01-iac-concepts.mdx
  - content/modules/06-iac/02-hcl-basics.mdx
  - content/modules/06-iac/03-terraform-state.mdx
  - content/modules/06-iac/04-modules.mdx
  - content/modules/06-iac/05-cheat-sheet.mdx
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "All 5 Infrastructure as Code lessons export a quiz array with 7-10 questions each"
    - "Every question has id, question, 4 options, correctIndex 0-3, and 2-3 sentence explanation"
    - "Foundation lessons test recall, Intermediate lessons test application"
    - "Questions are scoped to their specific lesson content only"
  artifacts:
    - path: "content/modules/06-iac/01-iac-concepts.mdx"
      provides: "7-10 quiz questions"
      contains: "export const quiz"
    - path: "content/modules/06-iac/02-hcl-basics.mdx"
      provides: "7-10 quiz questions"
      contains: "export const quiz"
    - path: "content/modules/06-iac/03-terraform-state.mdx"
      provides: "7-10 quiz questions"
      contains: "export const quiz"
    - path: "content/modules/06-iac/04-modules.mdx"
      provides: "7-10 quiz questions"
      contains: "export const quiz"
    - path: "content/modules/06-iac/05-cheat-sheet.mdx"
      provides: "7-10 quiz questions"
      contains: "export const quiz"
  key_links:
    - from: "content/modules/06-iac/*.mdx"
      to: "getLessonContent in lib/mdx.ts"
      via: "export const quiz named export extracted by Array.isArray(mod.quiz)"
      pattern: "export const quiz"
---

<objective>
Author 7-10 multiple-choice quiz questions for all 5 lessons in Module 06: Infrastructure as Code.

Purpose: Populate the IaC module with quiz content covering IaC concepts, HCL syntax, Terraform state, and modules.
Output: 5 MDX files updated with `export const quiz = [...]` blocks appended.
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-authoring/15-RESEARCH.md
@types/quiz.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author quizzes for all 5 Infrastructure as Code lessons</name>
  <files>
    content/modules/06-iac/01-iac-concepts.mdx
    content/modules/06-iac/02-hcl-basics.mdx
    content/modules/06-iac/03-terraform-state.mdx
    content/modules/06-iac/04-modules.mdx
    content/modules/06-iac/05-cheat-sheet.mdx
  </files>
  <read_first>
    content/modules/06-iac/01-iac-concepts.mdx
    content/modules/06-iac/02-hcl-basics.mdx
    content/modules/06-iac/03-terraform-state.mdx
    content/modules/06-iac/04-modules.mdx
    content/modules/06-iac/05-cheat-sheet.mdx
    types/quiz.ts
  </read_first>
  <action>
For each of the 5 lesson files in Module 06, read the full lesson content and append a quiz block after the last JSX closing tag.

**Quiz format (MUST match exactly):**
```
export const quiz = [
  {
    id: '{prefix}-q1',
    question: 'Question text here?',
    options: ['Option A', 'Option B', 'Option C', 'Option D'],
    correctIndex: 2,
    explanation: 'Two to three sentences explaining WHY the correct answer is right. Reinforce the mechanism.',
  },
]
```

**ID prefixes per lesson:**
- 01-iac-concepts: `iac`
- 02-hcl-basics: `hcl`
- 03-terraform-state: `tfstate`
- 04-modules: `tfmod`
- 05-cheat-sheet: `iac-ref`

**Quality standards (MANDATORY):**
- 7-10 questions per quiz
- Foundation lessons (01-02, 05): test basic recall — "What does X do?", "Which block type does Y?"
- Intermediate lessons (03-04): test application — "Given situation X, what would happen?", "Which approach achieves Y?"
- Distractors must be plausible (common misconceptions, adjacent concepts) — NOT obviously absurd
- Explanations must be 2-3 sentences explaining WHY the correct answer is right — reinforce the mechanism
- Questions must ONLY test content from that specific lesson
- Every correct answer must be findable in the lesson content
- Use double-quoted strings throughout — avoid apostrophes in strings
- correctIndex must be 0-3 and must point to the actually correct option
- Options must be exactly 4 strings

**CRITICAL: Read each lesson file fully before writing questions.**
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && for f in content/modules/06-iac/*.mdx; do echo "=== $(basename $f) ==="; grep -c "export const quiz" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files contain exactly one `export const quiz = [` line
    - grep -c "id:" on each quiz block shows 7-10 question objects per file
    - No file contains apostrophes inside single-quoted strings
  </acceptance_criteria>
  <done>All 5 Infrastructure as Code lessons have 7-10 quiz questions each, with proper IDs, 4 options, valid correctIndex, and mechanism-explaining explanations.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Module 06 quiz integrity</name>
  <files></files>
  <read_first></read_first>
  <action>
Run verification commands to confirm all Module 06 quizzes are properly formed:

1. Count `export const quiz` occurrences — must be exactly 5
2. Count question IDs per file — each must have 7-10
3. Run `npx tsc --noEmit 2>&1 | grep -v "__tests__\|\.test\.\|\.spec\." | grep "error TS"` — must produce zero output
4. Run `npx vitest run` — all existing tests must still pass

If any issues are found, fix them before completing this task.
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && echo "--- Quiz export count ---" && grep -rl "export const quiz" content/modules/06-iac/ | wc -l && echo "--- TSC non-test errors ---" && npx tsc --noEmit 2>&1 | grep -v "__tests__\|\.test\.\|\.spec\." | grep "error TS" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "export const quiz" content/modules/06-iac/ | wc -l` outputs 5
    - `npx tsc --noEmit 2>&1 | grep -v "__tests__\|\.test\.\|\.spec\." | grep "error TS" | wc -l` outputs 0
  </acceptance_criteria>
  <done>Module 06 has 5 lesson files each with a valid quiz export, zero new TypeScript errors, and all existing tests passing.</done>
</task>

</tasks>

<verification>
- All 5 MDX files in `content/modules/06-iac/` contain `export const quiz = [`
- Each quiz has 7-10 questions with valid structure
- `tsc --noEmit` produces zero non-test errors
</verification>

<success_criteria>
Module 06: Infrastructure as Code has complete quiz coverage — 5 lessons, 35-50 total questions, all type-safe and content-accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-authoring/15-06-SUMMARY.md`
</output>
