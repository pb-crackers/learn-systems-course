---
phase: 05-system-administration-ci-cd
plan: "04"
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - content/modules/05-cicd/05-cheat-sheet.mdx
  - lib/__tests__/modules.test.ts
autonomous: true
requirements: [CICD-05, CICD-06]

must_haves:
  truths:
    - "Learner can reference a complete GitHub Actions workflow YAML for the progressive Docker app (lint -> test -> build -> push)"
    - "Learner can reference the CI/CD cheat sheet with GitHub Actions syntax and pipeline patterns"
    - "CI/CD module has 5 lessons in Vitest assertion"
  artifacts:
    - path: "content/modules/05-cicd/05-cheat-sheet.mdx"
      provides: "CI/CD cheat sheet with 4 QuickReference sections + pipeline exercise"
      contains: "moduleSlug: \"05-cicd\""
    - path: "lib/__tests__/modules.test.ts"
      provides: "Updated test with cicd lesson count assertion"
      contains: "05-cicd"
  key_links:
    - from: "content/modules/05-cicd/05-cheat-sheet.mdx"
      to: "content/modules/05-cicd/03-building-testing.mdx"
      via: "References the complete pipeline YAML from lesson 3"
      pattern: "building-testing"
---

<objective>
Write the CI/CD cheat sheet (which also serves as the CICD-05 exercise landing page with the complete pipeline YAML) and update the Vitest module test with the cicd lesson count.

Purpose: The cheat sheet provides quick reference for GitHub Actions syntax. The CICD-05 exercise requirement (hands-on pipeline building) is satisfied through the complete workflow YAML embedded in lessons 02 and 03 plus the cheat sheet's pipeline reference. The test update ensures lesson count integrity.
Output: CI/CD cheat sheet MDX, updated test assertions
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-system-administration-ci-cd/05-CONTEXT.md
@.planning/phases/05-system-administration-ci-cd/05-RESEARCH.md
@.planning/phases/05-system-administration-ci-cd/05-03-SUMMARY.md
@content/modules/03-docker/08-cheat-sheet.mdx (cheat sheet pattern reference)
@lib/__tests__/modules.test.ts

<interfaces>
From RESEARCH.md (CICD-05 exercise design):
- Complete YAML workflow for docker/app/ — lint -> test -> build -> push to ghcr.io
- Learner copies into their own repo
- VerificationChecklist items learner checks manually

From RESEARCH.md (CICD-06 cheat sheet):
- QuickReference component per lesson topic (4 components for CICD-01-04)
- GitHub Actions YAML syntax reference items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CI/CD cheat sheet with pipeline exercise reference</name>
  <files>content/modules/05-cicd/05-cheat-sheet.mdx</files>
  <read_first>
    - content/modules/03-docker/08-cheat-sheet.mdx (cheat sheet MDX pattern)
    - content/modules/05-cicd/03-building-testing.mdx (complete pipeline YAML to reference)
    - .planning/phases/05-system-administration-ci-cd/05-RESEARCH.md (CICD-05 exercise requirements, CICD-06 cheat sheet structure)
  </read_first>
  <action>
**content/modules/05-cicd/05-cheat-sheet.mdx (CICD-05 + CICD-06)**
Frontmatter: title "CI/CD Cheat Sheet and Pipeline Reference", module "CI/CD Pipelines", moduleSlug "05-cicd", lessonSlug "05-cheat-sheet", order 5, difficulty "Foundation", estimatedMinutes 10, prerequisites [], tags ["cheat-sheet", "cicd", "github-actions", "reference", "pipeline"].

Content structure:

**Pipeline Exercise Section (CICD-05):**
Before the QuickReference sections, include a section titled "## Your Pipeline Exercise" with:
- Brief intro: "This is the complete CI/CD pipeline for the Node.js app you built in the Docker module. Copy this workflow into your own GitHub repository to see it run."
- The complete production-quality pipeline YAML from RESEARCH.md in a CodeBlock (lint -> test -> build-push with actions/checkout@v6, docker/setup-buildx-action@v4, docker/login-action@v4, docker/build-push-action@v7)
- ExerciseCard difficulty="Intermediate", scenario "Put your Docker knowledge to work in a real CI/CD pipeline. Create a GitHub repository, push the progressive Docker app, and add this workflow to automate everything." Steps: create a new GitHub repo, copy docker/app/ files, add the workflow YAML to .github/workflows/ci.yml, push to main, watch the Actions tab for pipeline execution.
- VerificationChecklist items: workflow triggers on push, lint job passes, test job passes, Docker image pushed to ghcr.io (visible in repo Packages tab). Note: this requires a GitHub account with Actions enabled.
- Callout type="tip": "You do not need to set up any secrets for ghcr.io — the GITHUB_TOKEN is provided automatically by GitHub Actions with the permissions block in the workflow."

**Quick Reference Sections (CICD-06):**
4 QuickReference components, one per lesson topic:

1. CI/CD Concepts — CI definition, CD (delivery vs deployment), pipeline stages, artifact concept, feedback loop value
2. GitHub Actions — .github/workflows/ location, on: triggers (push/pull_request/workflow_dispatch/schedule), jobs.*.runs-on, steps uses vs run, ${{ secrets.NAME }}, ${{ github.* }} context, GITHUB_TOKEN, needs for job dependencies
3. Building & Testing — actions/checkout@v6, docker/setup-buildx-action@v4, docker/login-action@v4, docker/build-push-action@v7, cache-from/cache-to: type=gha, matrix strategy syntax, npm ci/npm test/npm run lint
4. Deployment Strategies — Blue/green (instant switch, double cost), Rolling (gradual, both versions live), Canary (traffic split, safest), comparison table summary
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npx vitest run lib/__tests__/mdx.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/05-cicd/05-cheat-sheet.mdx exists and contains 'moduleSlug: "05-cicd"'
    - 05-cheat-sheet.mdx contains 4 QuickReference components
    - 05-cheat-sheet.mdx contains ExerciseCard for the pipeline exercise (CICD-05)
    - 05-cheat-sheet.mdx contains "actions/checkout@v6" and "docker/build-push-action@v7" in the pipeline YAML
    - 05-cheat-sheet.mdx contains VerificationChecklist
  </acceptance_criteria>
  <done>CI/CD cheat sheet with pipeline exercise and 4 QuickReference sections. Complete pipeline YAML with current action versions.</done>
</task>

<task type="auto">
  <name>Task 2: Update Vitest assertion for CI/CD module lesson count</name>
  <files>lib/__tests__/modules.test.ts</files>
  <read_first>
    - lib/__tests__/modules.test.ts (current test file — must read before modifying)
  </read_first>
  <action>
Add one new test case in the getAllLessonPaths describe block of lib/__tests__/modules.test.ts:

```typescript
it('cicd module has 5 lessons', () => {
  const paths = getAllLessonPaths()
  const cicdPaths = paths.filter(p => p.moduleSlug === '05-cicd')
  expect(cicdPaths).toHaveLength(5)
})
```

This mirrors the existing docker (9 lessons) and sysadmin (7 lessons, added in plan 05-02) assertions.

Do NOT modify any existing tests.
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npx vitest run lib/__tests__/modules.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/__tests__/modules.test.ts contains "05-cicd" and "toHaveLength(5)"
    - npx vitest run passes (all tests green including sysadmin and cicd count assertions)
  </acceptance_criteria>
  <done>Vitest confirms 5 CI/CD lessons. All module tests pass including sysadmin (7) and cicd (5) counts.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (full suite including sysadmin and cicd assertions)
- `ls content/modules/05-cicd/*.mdx | wc -l` returns 5
- `ls content/modules/04-sysadmin/*.mdx | wc -l` returns 7
- Complete pipeline YAML uses current action versions (v6, v4, v7)
</verification>

<success_criteria>
CI/CD module complete: 5 MDX files with cheat sheet + pipeline exercise. Vitest confirms both module lesson counts (sysadmin=7, cicd=5). Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-system-administration-ci-cd/05-04-SUMMARY.md`
</output>
