---
phase: 04-docker-foundation-capstone
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/modules/03-docker/01-what-are-containers.mdx
  - content/modules/03-docker/02-docker-images.mdx
  - content/modules/03-docker/03-docker-containers.mdx
  - docker/app/app.js
  - docker/app/package.json
  - docker/app/Dockerfile.basic
  - docker/app/.dockerignore
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "Learner can explain namespaces, cgroups, and overlay FS as the mechanisms behind containers, connecting back to Linux Fundamentals lessons"
    - "Learner can write a Dockerfile with FROM, COPY, RUN, CMD, EXPOSE and build an image from the progressive Node.js app"
    - "Learner can run a container with --name, --rm, -p, -e flags, exec into it, view logs, and set resource limits"
  artifacts:
    - path: "content/modules/03-docker/01-what-are-containers.mdx"
      provides: "Container internals lesson with namespace/cgroup/overlay FS deep-dive"
      contains: "moduleSlug: \"03-docker\""
    - path: "content/modules/03-docker/02-docker-images.mdx"
      provides: "Docker images lesson with Dockerfile, layers, build process"
      contains: "moduleSlug: \"03-docker\""
    - path: "content/modules/03-docker/03-docker-containers.mdx"
      provides: "Container lifecycle lesson with exec, logs, resource limits"
      contains: "moduleSlug: \"03-docker\""
    - path: "docker/app/app.js"
      provides: "Progressive exercise Node.js app (Express, GET / and GET /health)"
    - path: "docker/app/Dockerfile.basic"
      provides: "Single-stage Dockerfile for DOC-02 exercises"
  key_links:
    - from: "content/modules/03-docker/01-what-are-containers.mdx"
      to: "01-linux-fundamentals/05-processes"
      via: "prerequisites frontmatter and Callout deep-dive referencing PID namespaces"
      pattern: "prerequisites:.*01-linux-fundamentals/05-processes"
    - from: "content/modules/03-docker/02-docker-images.mdx"
      to: "docker/app/Dockerfile.basic"
      via: "TerminalBlock referencing docker build -f docker/app/Dockerfile.basic"
      pattern: "docker/app/Dockerfile"
---

<objective>
Create the first 3 Docker module lessons (container internals, images, containers) plus the progressive Node.js exercise app that learners Dockerize across the module.

Purpose: Establishes the mechanism-first foundation — learners understand what containers ARE (Linux kernel features) before using Docker CLI. The progressive app provides a coherent artifact that grows in complexity through subsequent lessons.
Output: 3 MDX lesson files in content/modules/03-docker/, progressive app source in docker/app/
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-foundation-capstone/04-CONTEXT.md
@.planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
@content/modules/index.ts
@content/modules/02-networking/01-how-networks-work.mdx (pattern reference for MDX structure)
@content/modules/01-linux-fundamentals/00-template.mdx (canonical lesson template)

<interfaces>
From content/modules/index.ts:
- Module slug: '03-docker', title: 'Docker & Containerization', accentColor: 'docker', order: 3

From types/content.ts (LessonFrontmatter):
- title, description, module, moduleSlug, lessonSlug, order, difficulty, estimatedMinutes, prerequisites, tags

Components available (from components/content/):
- CodeBlock (auto-mapped from pre in mdx-components.tsx)
- TerminalBlock (command/output pairs)
- ExerciseCard (with difficulty, scenario, objective, steps props)
- VerificationChecklist (pass/fail items with hint)
- Callout (type: tip | warning | deep-dive)
- QuickReference (cheat sheet tables)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progressive app source files and DOC-01 container internals lesson</name>
  <files>
    docker/app/app.js
    docker/app/package.json
    docker/app/.dockerignore
    docker/app/Dockerfile.basic
    content/modules/03-docker/01-what-are-containers.mdx
  </files>
  <read_first>
    content/modules/index.ts
    content/modules/02-networking/01-how-networks-work.mdx
    content/modules/01-linux-fundamentals/00-template.mdx
    .planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
  </read_first>
  <action>
    1. Create `docker/app/` directory.

    2. Create `docker/app/app.js` — minimal Express server (~15 lines):
       - `GET /` returns JSON `{ message: "Hello from the Docker app!", version: "1.0.0" }`
       - `GET /health` returns JSON `{ status: "healthy", uptime: process.uptime() }`
       - Listens on `process.env.PORT || 3000`
       - This is the progressive app used from DOC-02 through DOC-07

    3. Create `docker/app/package.json`:
       - name: "docker-exercise-app", version: "1.0.0"
       - main: "app.js"
       - scripts: `{ "start": "node app.js" }`
       - dependencies: `{ "express": "^4.21.0" }`
       - Do NOT include a lockfile — learners run `npm install` inside the container during DOC-02

    4. Create `docker/app/.dockerignore`:
       - node_modules, npm-debug.log, .git, .env

    5. Create `docker/app/Dockerfile.basic` — single-stage Dockerfile for DOC-02 exercises:
       - `FROM node:20` (NOT alpine — intentionally uses the full ~1.1GB image for DOC-07 before/after comparison)
       - `WORKDIR /app`
       - `COPY package.json .`
       - `RUN npm install`
       - `COPY . .`
       - `EXPOSE 3000`
       - `CMD ["node", "app.js"]` (NOT npm start — avoids npm as PID 1)
       - Add comments explaining each instruction for learning purposes

    6. Create `content/modules/03-docker/` directory.

    7. Create `content/modules/03-docker/01-what-are-containers.mdx` (DOC-01):
       - Frontmatter: moduleSlug: "03-docker", lessonSlug: "01-what-are-containers", order: 1, difficulty: "Foundation", estimatedMinutes: 22, prerequisites: ["01-linux-fundamentals/05-processes", "01-linux-fundamentals/03-linux-filesystem"], module: "Docker & Containerization"
       - ## Overview: "Your team lead says 'just Docker it' — but what IS a container?" scenario. Containers are NOT lightweight VMs. They are isolated Linux processes using kernel features the learner already knows.
       - ## How It Works: Deep mechanism-first sections covering:
         a) **Linux Namespaces**: PID, NET, MNT, UTS, IPC, USER — each gets its own subsection. Callout type="deep-dive" connecting PID namespaces to LNX-05 (processes lesson). Show the clone() syscall with CLONE_NEWPID flag.
         b) **Control Groups (cgroups)**: CPU shares, memory limits, I/O throttling. Explain cgroups v2 hierarchy. Show `/sys/fs/cgroup/` structure.
         c) **Union/Overlay Filesystem**: Layers concept — lower (read-only) + upper (writable) + merged view. Connect to LNX-03 (filesystem lesson). ASCII diagram showing overlay mount structure.
         d) **Putting It Together**: How `docker run` calls clone() with namespace flags, sets up cgroup limits, mounts overlay FS, then execs the container process. ASCII diagram of the full sequence.
       - ## Hands-On Exercise: ExerciseCard with difficulty="Foundation", scenario about inspecting container internals from the host:
         Steps: (1) Run `docker run -d --name mybox ubuntu:22.04 sleep infinity`, (2) Find host PID via `docker inspect`, (3) List namespace links in `/proc/<PID>/ns/`, (4) Compare PID inside vs outside container, (5) Inspect cgroup limits, (6) Clean up with `docker rm -f`
         VerificationChecklist items: namespace links visible, PID differs inside/outside, cgroup directory exists
       - ## Quick Reference: QuickReference with container internals concepts (namespace types table, cgroup subsystems, overlay FS layers)
       - Use TerminalBlock for all command demonstrations with truncated container IDs and "Your output will show a different container ID" comments
       - Callout type="tip" bridging from Phase 3 networking: "In the Networking module you learned about network interfaces — each container gets its own network namespace with its own interfaces, routing table, and iptables rules"
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - docker/app/app.js exists and contains "express" and "GET /" and "/health"
    - docker/app/package.json exists and contains "docker-exercise-app"
    - docker/app/Dockerfile.basic exists and contains "FROM node:20" and "CMD"
    - docker/app/.dockerignore exists and contains "node_modules"
    - content/modules/03-docker/01-what-are-containers.mdx exists
    - 01-what-are-containers.mdx contains 'moduleSlug: "03-docker"'
    - 01-what-are-containers.mdx contains "namespaces" and "cgroups" and "overlay"
    - 01-what-are-containers.mdx contains "01-linux-fundamentals/05-processes" in prerequisites
    - 01-what-are-containers.mdx contains "ExerciseCard" and "VerificationChecklist"
    - npm run build completes without error
  </acceptance_criteria>
  <done>Progressive app source files exist in docker/app/ with a working Express app, single-stage Dockerfile, and .dockerignore. DOC-01 lesson explains container internals (namespaces, cgroups, overlay FS) with explicit connections to Linux Fundamentals lessons, includes a hands-on exercise inspecting container internals from the host, and builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: DOC-02 Docker images and DOC-03 container lifecycle lessons</name>
  <files>
    content/modules/03-docker/02-docker-images.mdx
    content/modules/03-docker/03-docker-containers.mdx
  </files>
  <read_first>
    content/modules/03-docker/01-what-are-containers.mdx
    docker/app/Dockerfile.basic
    docker/app/app.js
    .planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
  </read_first>
  <action>
    1. Create `content/modules/03-docker/02-docker-images.mdx` (DOC-02):
       - Frontmatter: moduleSlug: "03-docker", lessonSlug: "02-docker-images", order: 2, difficulty: "Foundation", estimatedMinutes: 25, prerequisites: ["03-docker/01-what-are-containers"], module: "Docker & Containerization"
       - ## Overview: "You need to ship your app — but every developer's machine is different" scenario. Images are the portable, reproducible packaging format.
       - ## How It Works:
         a) **Image Layers**: Each Dockerfile instruction creates a layer. Show `docker image inspect` output with layer IDs. ASCII diagram of layer stacking (base → deps → app code → config). Explain copy-on-write.
         b) **Dockerfile Instructions**: FROM, RUN, COPY, ADD, WORKDIR, ENV, EXPOSE, CMD, ENTRYPOINT — each instruction explained with what layer it produces. Use the progressive app's Dockerfile.basic as the running example. Callout type="warning" about EXPOSE being documentation-only (does NOT publish ports — `-p` does that).
         c) **Build Process**: `docker build` step-by-step trace. Show build output with "Step 1/6: FROM node:20" etc. Explain build context and .dockerignore. Show how changing one line invalidates downstream layers.
         d) **Registries**: Docker Hub, pulling images, tagging. `docker pull`, `docker tag`, image naming convention (`registry/repo:tag`).
       - ## Hands-On Exercise: ExerciseCard difficulty="Foundation", scenario about containerizing the team's Node.js API:
         Steps: (1) Build image from docker/app/Dockerfile.basic: `docker build -t myapp:v1 -f docker/app/Dockerfile.basic docker/app/`, (2) List images and note size, (3) Run the image: `docker run -d -p 3000:3000 --name myapp myapp:v1`, (4) Test with `curl http://localhost:3000/`, (5) Inspect layers with `docker image inspect myapp:v1`, (6) Modify app.js, rebuild, notice which layers are cached vs rebuilt, (7) Clean up
         VerificationChecklist: image built successfully, container responds on port 3000, layer count visible in inspect output
       - ## Quick Reference: QuickReference with Dockerfile instructions table and docker image commands
       - TerminalBlock examples use the progressive app with truncated IDs and "output will differ" comments

    2. Create `content/modules/03-docker/03-docker-containers.mdx` (DOC-03):
       - Frontmatter: moduleSlug: "03-docker", lessonSlug: "03-docker-containers", order: 3, difficulty: "Foundation", estimatedMinutes: 20, prerequisites: ["03-docker/02-docker-images"], module: "Docker & Containerization"
       - ## Overview: "Your container crashed at 3am — what happened?" scenario. Understanding the container lifecycle means knowing how to inspect, debug, and control containers.
       - ## How It Works:
         a) **Container Lifecycle**: Created → Running → Paused → Stopped → Removed. ASCII state diagram. `docker create`, `docker start`, `docker stop` (SIGTERM then SIGKILL after grace period), `docker rm`. Explain graceful shutdown.
         b) **Interactive Access**: `docker exec -it <container> /bin/bash` — get a shell inside a running container. `docker exec` vs `docker attach`. Callout type="warning" about attach connecting to PID 1's stdin (Ctrl+C kills the container).
         c) **Logs and Inspection**: `docker logs`, `docker logs -f` (follow), `docker inspect` for full container metadata. `docker stats` for live resource usage.
         d) **Resource Limits**: `--memory`, `--cpus` flags. Show `docker stats` before and after applying limits. Connect back to cgroups from DOC-01 — "These flags set the cgroup limits you learned about in the previous lesson." Callout type="deep-dive" showing the actual cgroup file that Docker writes to.
       - ## Hands-On Exercise: ExerciseCard difficulty="Foundation", scenario about debugging a misbehaving container in production:
         Steps: (1) Run the progressive app: `docker run -d --name debug-app -p 3000:3000 myapp:v1`, (2) View logs, (3) Exec into container and inspect the running process, (4) Stop and restart with resource limits: `docker run -d --name limited-app --memory=128m --cpus=0.5 -p 3001:3000 myapp:v1`, (5) Monitor with docker stats, (6) Stop gracefully vs force-kill timing difference, (7) Clean up all containers
         VerificationChecklist: container logs visible, exec shell works, resource limits shown in docker stats, graceful stop observed
       - ## Quick Reference: QuickReference with container lifecycle commands and resource limit flags
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/03-docker/02-docker-images.mdx exists
    - 02-docker-images.mdx contains 'moduleSlug: "03-docker"' and 'order: 2'
    - 02-docker-images.mdx contains "Dockerfile" and "layers" and "docker build"
    - 02-docker-images.mdx contains "docker/app/Dockerfile.basic" reference
    - 02-docker-images.mdx contains "ExerciseCard" and "VerificationChecklist"
    - content/modules/03-docker/03-docker-containers.mdx exists
    - 03-docker-containers.mdx contains 'moduleSlug: "03-docker"' and 'order: 3'
    - 03-docker-containers.mdx contains "lifecycle" and "docker exec" and "docker logs"
    - 03-docker-containers.mdx contains "--memory" and "--cpus" and "cgroup"
    - 03-docker-containers.mdx contains "ExerciseCard" and "VerificationChecklist"
    - npm run build completes without error
  </acceptance_criteria>
  <done>DOC-02 covers Docker images (layers, Dockerfile instructions, build process, registries) and DOC-03 covers container lifecycle (exec, logs, resource limits connected to cgroups). Both reference the progressive app, include hands-on exercises, and build successfully.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (MDX compilation, frontmatter validation, static params)
- `npm test` passes (existing tests unaffected)
- All 3 MDX files have correct moduleSlug: "03-docker"
- Progressive app files exist in docker/app/ with valid Dockerfile
- Prerequisite chain: DOC-01 depends on Linux lessons, DOC-02 depends on DOC-01, DOC-03 depends on DOC-02
</verification>

<success_criteria>
- 3 Docker lesson MDX files exist and render in the app
- Progressive Node.js app source exists in docker/app/ with Dockerfile.basic
- Container internals lesson (DOC-01) explicitly connects namespaces/cgroups to Linux Fundamentals
- Images lesson (DOC-02) uses the progressive app as the running example
- Container lifecycle lesson (DOC-03) connects resource limits back to cgroups
- All difficulty labels match: DOC-01 Foundation, DOC-02 Foundation, DOC-03 Foundation
</success_criteria>

<output>
After completion, create `.planning/phases/04-docker-foundation-capstone/04-01-SUMMARY.md`
</output>
