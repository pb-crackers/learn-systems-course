---
phase: 04-docker-foundation-capstone
plan: "03"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - docker/app/verify-images.sh
  - docker/app/verify-containers.sh
  - docker/app/verify-volumes.sh
  - docker/app/verify-networking.sh
  - docker/app/verify-compose.sh
  - docker/app/verify-best-practices.sh
  - docker/app/verify-internals.sh
  - lib/__tests__/modules.test.ts
autonomous: true
requirements: [DOC-08]

must_haves:
  truths:
    - "Learner can run a verify script for each Docker lesson and receive clear PASS/FAIL feedback on whether they completed the exercise correctly"
    - "All 9 Docker lessons are discovered by the filesystem scanner and appear in the sidebar"
  artifacts:
    - path: "docker/app/verify-internals.sh"
      provides: "DOC-01 exercise verification — namespace/cgroup inspection"
      contains: "PASS"
    - path: "docker/app/verify-images.sh"
      provides: "DOC-02 exercise verification — image built, container responds"
      contains: "PASS"
    - path: "docker/app/verify-containers.sh"
      provides: "DOC-03 exercise verification — lifecycle, exec, resource limits"
      contains: "PASS"
    - path: "docker/app/verify-volumes.sh"
      provides: "DOC-04 exercise verification — named volume persistence"
      contains: "PASS"
    - path: "docker/app/verify-networking.sh"
      provides: "DOC-05 exercise verification — custom network, DNS resolution"
      contains: "PASS"
    - path: "docker/app/verify-compose.sh"
      provides: "DOC-06 exercise verification — multi-service Compose stack"
      contains: "PASS"
    - path: "docker/app/verify-best-practices.sh"
      provides: "DOC-07 exercise verification — image size, non-root, healthcheck"
      contains: "PASS"
    - path: "lib/__tests__/modules.test.ts"
      provides: "Integration test asserting 9 Docker lessons discovered"
      contains: "03-docker"
  key_links:
    - from: "docker/app/verify-*.sh"
      to: "docker CLI on host"
      via: "docker commands checking container/image/network/volume state"
      pattern: "docker (ps|images|volume|network|compose)"
---

<objective>
Create exercise verification scripts for all 7 Docker lessons (DOC-01 through DOC-07) and add integration test asserting the Docker module has 9 lessons.

Purpose: Every lesson exercise needs a verify script that gives clear PASS/FAIL feedback. These scripts run on the host machine using Docker CLI to check the learner's work. The integration test ensures the module filesystem scanner discovers all Docker content.
Output: 7 verify scripts in docker/app/, updated modules test
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-foundation-capstone/04-CONTEXT.md
@.planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
@.planning/phases/04-docker-foundation-capstone/04-01-SUMMARY.md
@.planning/phases/04-docker-foundation-capstone/04-02-SUMMARY.md
@lib/__tests__/modules.test.ts

<interfaces>
From docker/linux/verify/*.sh (Phase 2 verify pattern):
- set -euo pipefail at top
- check() function with colored PASS/FAIL printf
- PASS=$((PASS + 1)) and FAIL=$((FAIL + 1)) counters (NOT ((PASS++)) under set -e)
- RESULT: PASS/FAIL summary at end with exit 1 on failure
- Each FAIL message includes diagnostic hint

From 04-RESEARCH.md:
- Docker lesson verify scripts run on HOST (not inside container)
- Use docker CLI commands to inspect state
- Use docker compose exec -T for commands inside containers (in scripts)
- Use || echo "fallback" for commands that may fail under set -e
- Use python3 for JSON parsing (universally available, no jq dependency)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify scripts for DOC-01 through DOC-04</name>
  <files>
    docker/app/verify-internals.sh
    docker/app/verify-images.sh
    docker/app/verify-containers.sh
    docker/app/verify-volumes.sh
  </files>
  <read_first>
    docker/linux/verify/01.sh
    content/modules/03-docker/01-what-are-containers.mdx
    content/modules/03-docker/02-docker-images.mdx
    content/modules/03-docker/03-docker-containers.mdx
    content/modules/03-docker/04-docker-volumes.mdx
    .planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
  </read_first>
  <action>
    All verify scripts follow the Phase 2 pattern (check() function, PASS/FAIL counters, colored output) but run on the HOST using docker CLI. Each script starts with:
    ```
    #!/usr/bin/env bash
    set -euo pipefail
    PASS=0
    FAIL=0
    check() { ... } # same pattern as docker/linux/verify/*.sh
    ```

    1. Create `docker/app/verify-internals.sh` (DOC-01 exercise verification):
       - Check: container named "mybox" is running (`docker ps --filter name=mybox --format "{{.Names}}"`)
       - Check: can retrieve host PID via `docker inspect mybox --format "{{.State.Pid}}"` (PID > 0)
       - Check: namespace links exist in /proc/<PID>/ns/ (at least pid, net, mnt visible)
       - Check: PID inside container differs from host PID (`docker exec -T mybox cat /proc/1/status | grep ^Pid` vs host PID)
       - Each FAIL includes the exact command to run to fix the issue
       - Graceful fallback if mybox doesn't exist: "Run: docker run -d --name mybox ubuntu:22.04 sleep infinity"

    2. Create `docker/app/verify-images.sh` (DOC-02 exercise verification):
       - Check: image "myapp:v1" exists (`docker images myapp:v1 --format "{{.Repository}}:{{.Tag}}"`)
       - Check: container "myapp" is running
       - Check: app responds on port 3000 (`curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` returns 200)
       - Check: can inspect image layers (`docker image inspect myapp:v1 --format "{{len .RootFS.Layers}}"` returns > 0)
       - Graceful fallback if image/container missing

    3. Create `docker/app/verify-containers.sh` (DOC-03 exercise verification):
       - Check: container "debug-app" or "limited-app" is running
       - Check: can exec into container (`docker exec -T <name> echo "exec works"`)
       - Check: logs accessible (`docker logs <name> 2>&1 | head -1` returns non-empty)
       - Check: resource-limited container has memory limit set (`docker inspect limited-app --format "{{.HostConfig.Memory}}"` returns non-zero)
       - Graceful fallback if containers missing

    4. Create `docker/app/verify-volumes.sh` (DOC-04 exercise verification):
       - Check: a named volume exists (`docker volume ls --format "{{.Name}}"` contains a volume name)
       - Check: data persists — write test file to volume, remove container, start new container with same volume, read test file
       - Actually: since verify runs AFTER learner completes exercise, just check that a named volume exists and has data
       - Check: volume is mountable and readable
       - Graceful fallback if no volumes exist

    Make all scripts executable: chmod +x docker/app/verify-*.sh
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && bash -n docker/app/verify-internals.sh && bash -n docker/app/verify-images.sh && bash -n docker/app/verify-containers.sh && bash -n docker/app/verify-volumes.sh && echo "All scripts pass syntax check"</automated>
  </verify>
  <acceptance_criteria>
    - docker/app/verify-internals.sh exists and passes bash -n syntax check
    - docker/app/verify-images.sh exists and passes bash -n syntax check
    - docker/app/verify-containers.sh exists and passes bash -n syntax check
    - docker/app/verify-volumes.sh exists and passes bash -n syntax check
    - All scripts contain "check()" function and "PASS" and "FAIL" counters
    - All scripts use $((PASS + 1)) not ((PASS++))
    - All scripts use docker exec -T (not docker exec without -T) in script context
    - All scripts have executable permission
  </acceptance_criteria>
  <done>4 verify scripts for DOC-01 through DOC-04 exist, pass syntax validation, follow the PASS/FAIL pattern, run on the host using Docker CLI, and include diagnostic hints in FAIL messages.</done>
</task>

<task type="auto">
  <name>Task 2: Verify scripts for DOC-05 through DOC-07 and integration test update</name>
  <files>
    docker/app/verify-networking.sh
    docker/app/verify-compose.sh
    docker/app/verify-best-practices.sh
    lib/__tests__/modules.test.ts
  </files>
  <read_first>
    docker/app/verify-internals.sh
    content/modules/03-docker/05-docker-networking.mdx
    content/modules/03-docker/06-docker-compose.mdx
    content/modules/03-docker/07-dockerfile-best-practices.mdx
    lib/__tests__/modules.test.ts
    .planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
  </read_first>
  <action>
    1. Create `docker/app/verify-networking.sh` (DOC-05 exercise verification):
       - Check: custom network exists (`docker network ls --format "{{.Name}}"` contains "mynet" or similar)
       - Check: at least 2 containers on the network (`docker network inspect <network> --format "{{len .Containers}}"` >= 2, or use python3 JSON parsing)
       - Check: containers can resolve each other by name (docker exec -T <container> ping -c 1 <other_name> succeeds)
       - Check: port mapping works if applicable (curl test)
       - Graceful fallback with hints

    2. Create `docker/app/verify-compose.sh` (DOC-06 exercise verification):
       - Check: compose.yml exists in expected location OR at least one docker compose project is running
       - Check: at least 2 services running (`docker compose ps --format json 2>/dev/null | python3 -c "import sys,json; data=json.load(sys.stdin); print(len(data))"` >= 2, with fallback)
       - Check: healthcheck passing (at least one service shows "healthy" in docker compose ps output)
       - Check: named volume defined (`docker volume ls --format "{{.Name}}"` contains compose-related volume)
       - Use `|| echo "fallback"` pattern for all commands that may fail
       - Graceful fallback if compose not running

    3. Create `docker/app/verify-best-practices.sh` (DOC-07 exercise verification):
       - Check: image "app:optimized" (or similar tag) exists
       - Check: optimized image size under 200MB (`docker images app:optimized --format "{{.Size}}"` — parse to check under 200MB)
       - Check: container runs as non-root (`docker run --rm app:optimized id -u` returns non-zero UID)
       - Check: HEALTHCHECK is defined in image (`docker inspect app:optimized --format "{{.Config.Healthcheck}}"` is non-empty/non-nil)
       - Graceful fallback if image doesn't exist

    Make all scripts executable: chmod +x docker/app/verify-*.sh

    4. Update `lib/__tests__/modules.test.ts`:
       - Add a new test in the `getAllLessonPaths` describe block:
         ```typescript
         it('docker module has 9 lessons', () => {
           const paths = getAllLessonPaths()
           const dockerPaths = paths.filter(p => p.moduleSlug === '03-docker')
           expect(dockerPaths).toHaveLength(9)
         })
         ```
       - Update the stale description 'returns an array (empty in Phase 1)' to 'returns lesson paths for all modules'
       - Keep all existing tests intact
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && bash -n docker/app/verify-networking.sh && bash -n docker/app/verify-compose.sh && bash -n docker/app/verify-best-practices.sh && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - docker/app/verify-networking.sh exists and passes bash -n syntax check
    - docker/app/verify-compose.sh exists and passes bash -n syntax check
    - docker/app/verify-best-practices.sh exists and passes bash -n syntax check
    - All scripts contain "check()" function and "PASS" and "FAIL"
    - All scripts use $((PASS + 1)) not ((PASS++))
    - verify-compose.sh uses docker compose (not docker-compose) and exec -T
    - lib/__tests__/modules.test.ts contains "03-docker" and "toHaveLength(9)"
    - npm test passes with all tests green
  </acceptance_criteria>
  <done>3 verify scripts for DOC-05 through DOC-07 exist and pass syntax validation. Integration test updated to assert 9 Docker module lessons are discovered. All tests pass.</done>
</task>

</tasks>

<verification>
- `bash -n docker/app/verify-*.sh` all pass syntax checks
- `npm test` passes including new Docker module assertion
- All 7 verify scripts follow PASS/FAIL pattern from Phase 2
- No script uses ((PASS++)) or docker-compose (v1) or exec without -T
</verification>

<success_criteria>
- 7 exercise verify scripts exist (one per DOC-01 through DOC-07)
- Each script gives clear PASS/FAIL output with diagnostic hints
- Scripts run on host using Docker CLI
- Integration test confirms 9 Docker lessons discovered by filesystem scanner
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-docker-foundation-capstone/04-03-SUMMARY.md`
</output>
