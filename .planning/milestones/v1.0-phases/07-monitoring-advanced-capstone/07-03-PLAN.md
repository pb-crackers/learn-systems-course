---
phase: 07-monitoring-advanced-capstone
plan: "03"
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - content/modules/08-monitoring/07-advanced-capstone.mdx
  - docker/advanced-capstone/app/app.js
  - docker/advanced-capstone/app/package.json
  - docker/advanced-capstone/app/Dockerfile
  - docker/advanced-capstone/prometheus/prometheus.yml
  - docker/advanced-capstone/prometheus/rules/alerts.yml
  - docker/advanced-capstone/grafana/provisioning/datasources/prometheus.yml
  - docker/advanced-capstone/iac/main.tf
  - docker/advanced-capstone/iac/variables.tf
  - docker/advanced-capstone/iac/outputs.tf
  - docker/advanced-capstone/.github/workflows/ci.yml
  - docker/advanced-capstone/compose.yml
  - docker/advanced-capstone/verify.sh
  - docker/advanced-capstone/README.md
autonomous: true
requirements: [CAP-02]

must_haves:
  truths:
    - "Learner can read the capstone brief and understand they must connect a Dockerized app to CI/CD + IaC + monitoring and then diagnose an intentional memory leak"
    - "Learner can start the capstone stack with docker compose up -d and see the app, Prometheus, Grafana, and Alertmanager all running"
    - "Learner can observe nodejs_heap_used_bytes steadily rising in Prometheus after the app runs for a few minutes"
    - "Learner can run verify.sh and get PASS/FAIL feedback on all capstone components"
  artifacts:
    - path: "content/modules/08-monitoring/07-advanced-capstone.mdx"
      provides: "CAP-02 capstone lesson content"
      contains: "difficulty: \"Challenge\""
    - path: "docker/advanced-capstone/app/app.js"
      provides: "App with intentional memory leak"
      contains: "leakStore"
    - path: "docker/advanced-capstone/compose.yml"
      provides: "Capstone Docker Compose stack"
      contains: "prom/prometheus"
    - path: "docker/advanced-capstone/verify.sh"
      provides: "Capstone verification script"
      contains: "PASS"
    - path: "docker/advanced-capstone/iac/main.tf"
      provides: "OpenTofu IaC config for capstone"
      contains: "kreuzwerker/docker"
    - path: "docker/advanced-capstone/.github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "actions/checkout"
  key_links:
    - from: "docker/advanced-capstone/app/app.js"
      to: "docker/advanced-capstone/prometheus/prometheus.yml"
      via: "Prometheus scrapes app:3000/metrics where the memory leak manifests"
      pattern: "targets:.*app:3000"
    - from: "docker/advanced-capstone/verify.sh"
      to: "docker/advanced-capstone/app/app.js"
      via: "verify.sh checks nodejs_heap_used_bytes metric exists in Prometheus"
      pattern: "nodejs_heap_used_bytes"
    - from: "content/modules/08-monitoring/07-advanced-capstone.mdx"
      to: "docker/advanced-capstone/"
      via: "lesson references capstone directory for all exercise files"
      pattern: "docker/advanced-capstone"
---

<objective>
Build the Advanced Capstone project (CAP-02): a full-stack application with CI/CD pipeline, IaC provisioning, Prometheus/Grafana monitoring, and an intentional memory leak failure scenario the learner must diagnose.

Purpose: The course finale that integrates all 8 modules. The learner starts with provided infrastructure, connects the pieces, and then uses monitoring to diagnose a subtle memory leak. This is the highest-difficulty exercise in the course — no step-by-step guidance, only a brief and verification script.
Output: docker/advanced-capstone/ directory with full project, updated 07-advanced-capstone.mdx with full capstone content
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-monitoring-advanced-capstone/07-CONTEXT.md
@.planning/phases/07-monitoring-advanced-capstone/07-RESEARCH.md
@.planning/phases/07-monitoring-advanced-capstone/07-01-SUMMARY.md
@.planning/phases/07-monitoring-advanced-capstone/07-02-SUMMARY.md

<interfaces>
From RESEARCH.md Pattern 8 (memory leak):
- leakStore array at module scope, simulateLeak() via setInterval(1000)
- 100 objects per second, each ~2KB (Buffer.alloc(1024).toString('hex'))
- Visible in nodejs_heap_used_bytes within 2-3 minutes
- Diagnostic PromQL: increase(nodejs_heap_used_bytes[5m]) or rate(nodejs_heap_used_bytes[5m])
- mem_limit: 256m on app container for predictable OOM threshold

From RESEARCH.md capstone verify.sh pattern:
- 10 checks: app running, prometheus running, grafana running, alertmanager running, prometheus scrapes app, grafana API healthy, alert rules loaded, nodejs_heap_used_bytes exists, OpenTofu state file exists, CI workflow file exists

From Phase 6 IaC patterns:
- kreuzwerker/docker v3.9.0, registry.opentofu.org source
- main.tf + variables.tf + outputs.tf structure

From Phase 5 CI/CD patterns:
- GitHub Actions: checkout@v6, setup-buildx@v4, build-push-action@v7
- Minimal CI workflow (lint + build) per RESEARCH.md recommendation — capstone challenge is monitoring/diagnosis, not CI/CD

From Phase 4 capstone pattern:
- Open-ended brief (README.md), starter code provided, verify.sh with PASS/FAIL
- Difficulty: Challenge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build advanced capstone infrastructure (app with leak, Compose stack, IaC, CI workflow, verify.sh)</name>
  <files>docker/advanced-capstone/app/app.js, docker/advanced-capstone/app/package.json, docker/advanced-capstone/app/Dockerfile, docker/advanced-capstone/prometheus/prometheus.yml, docker/advanced-capstone/prometheus/rules/alerts.yml, docker/advanced-capstone/grafana/provisioning/datasources/prometheus.yml, docker/advanced-capstone/iac/main.tf, docker/advanced-capstone/iac/variables.tf, docker/advanced-capstone/iac/outputs.tf, docker/advanced-capstone/.github/workflows/ci.yml, docker/advanced-capstone/compose.yml, docker/advanced-capstone/verify.sh, docker/advanced-capstone/README.md</files>
  <read_first>
    - .planning/phases/07-monitoring-advanced-capstone/07-RESEARCH.md (memory leak pattern, verify.sh structure, all capstone patterns)
    - docker/monitoring/app/app.js (instrumented app from plan 07-02 — base for capstone app with added leak)
    - docker/monitoring/compose.yml (monitoring stack pattern to adapt for capstone)
    - docker/monitoring/prometheus/prometheus.yml (Prometheus config pattern)
  </read_first>
  <action>
Create the entire docker/advanced-capstone/ directory tree:

**docker/advanced-capstone/app/package.json**
```json
{
  "name": "capstone-app",
  "version": "1.0.0",
  "description": "Advanced capstone app with monitoring and intentional failure",
  "main": "app.js",
  "scripts": { "start": "node app.js", "lint": "echo 'lint passed'", "test": "echo 'tests passed'" },
  "dependencies": {
    "express": "^4.21.0",
    "prom-client": "15.1.3"
  }
}
```

**docker/advanced-capstone/app/Dockerfile**
```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci

FROM node:20-alpine
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
WORKDIR /app
COPY --from=builder /app/node_modules ./node_modules
COPY app.js .
COPY package.json .
RUN chown -R appuser:appgroup /app
USER appuser
EXPOSE 3000
HEALTHCHECK --interval=10s --timeout=3s CMD wget -qO- http://localhost:3000/health || exit 1
CMD ["node", "app.js"]
```

**docker/advanced-capstone/app/app.js**
Based on the monitoring app but with the intentional memory leak from RESEARCH.md Pattern 8:
- Same Express app with prom-client instrumentation (Registry, collectDefaultMetrics, http_requests_total Counter, http_request_duration_seconds Histogram, active_connections Gauge)
- Same middleware to record request metrics
- Same endpoints: GET / (json response), GET /health (status + uptime), GET /metrics (Prometheus text format), POST /alert-webhook (logs alert body)
- Add GET /slow endpoint (100-500ms random delay for histogram data)
- ADD THE MEMORY LEAK: Module-scope `const leakStore = []`. setInterval at 1000ms calling simulateLeak() which pushes 100 objects each containing { id: Math.random(), data: Buffer.alloc(1024).toString('hex'), timestamp: new Date().toISOString() }. NO console.log about the leak — it must be silent.
- The leak accumulates ~200KB/second. With mem_limit: 256m, the app runs stable for 15-20 minutes before memory pressure is noticeable in metrics, giving the learner plenty of time to diagnose via Prometheus.

**docker/advanced-capstone/prometheus/prometheus.yml**
```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

rule_files:
  - "rules/*.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "app"
    static_configs:
      - targets: ["app:3000"]
```

**docker/advanced-capstone/prometheus/rules/alerts.yml**
```yaml
groups:
  - name: capstone-alerts
    rules:
      - alert: AppDown
        expr: up{job="app"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Capstone app is down"
          description: "{{ $labels.instance }} unreachable for 1+ minute"

      - alert: HighMemoryUsage
        expr: process_resident_memory_bytes{job="app"} > 200000000
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "High memory usage on capstone app"
          description: "Memory at {{ $value | humanize }}B — possible memory leak"

      - alert: HeapGrowthAnomaly
        expr: rate(nodejs_heap_used_bytes{job="app"}[5m]) > 50000
        for: 3m
        labels:
          severity: warning
        annotations:
          summary: "Heap growing steadily"
          description: "Heap growth rate is {{ $value | humanize }}B/s — investigate for memory leak"
```

**docker/advanced-capstone/grafana/provisioning/datasources/prometheus.yml**
Same pattern as monitoring stack:
```yaml
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
```

**docker/advanced-capstone/compose.yml**
```yaml
services:
  app:
    build: ./app
    ports:
      - "3000:3000"
    mem_limit: 256m
    networks:
      - capstone

  prometheus:
    image: prom/prometheus:v3.10.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.enable-lifecycle"
    networks:
      - capstone

  grafana:
    image: grafana/grafana:12.4.1
    ports:
      - "3001:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana-data:/var/lib/grafana
    networks:
      - capstone

  alertmanager:
    image: prom/alertmanager:v0.28.1
    ports:
      - "9093:9093"
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    networks:
      - capstone

networks:
  capstone:
    driver: bridge

volumes:
  prometheus-data:
  grafana-data:
```

Also create docker/advanced-capstone/alertmanager/alertmanager.yml (same as monitoring stack pattern — webhook receiver to app:3000/alert-webhook).

**docker/advanced-capstone/iac/main.tf**
OpenTofu config that provisions the monitoring stack via Docker provider. This is a reference/exercise file — the learner runs tofu apply to provision the infrastructure:
```hcl
terraform {
  required_providers {
    docker = {
      source  = "registry.opentofu.org/kreuzwerker/docker"
      version = "3.9.0"
    }
  }
}

provider "docker" {}

resource "docker_network" "capstone" {
  name = "capstone-network"
}

resource "docker_image" "prometheus" {
  name = "prom/prometheus:v3.10.0"
}

resource "docker_container" "prometheus" {
  name  = "capstone-prometheus"
  image = docker_image.prometheus.image_id
  ports {
    internal = 9090
    external = 9090
  }
  networks_advanced {
    name = docker_network.capstone.name
  }
  volumes {
    host_path      = abspath("${path.module}/../prometheus/prometheus.yml")
    container_path = "/etc/prometheus/prometheus.yml"
    read_only      = true
  }
  volumes {
    host_path      = abspath("${path.module}/../prometheus/rules")
    container_path = "/etc/prometheus/rules"
    read_only      = true
  }
  command = [
    "--config.file=/etc/prometheus/prometheus.yml",
    "--storage.tsdb.path=/prometheus",
    "--web.enable-lifecycle"
  ]
}

output "prometheus_url" {
  value = "http://localhost:9090"
}
```

**docker/advanced-capstone/iac/variables.tf**
```hcl
variable "prometheus_port" {
  description = "External port for Prometheus"
  type        = number
  default     = 9090
  validation {
    condition     = var.prometheus_port > 0 && var.prometheus_port < 65536
    error_message = "Port must be between 1 and 65535."
  }
}
```

**docker/advanced-capstone/iac/outputs.tf**
```hcl
output "prometheus_container_id" {
  description = "Docker container ID for Prometheus"
  value       = docker_container.prometheus.id
}

output "network_id" {
  description = "Docker network ID for capstone"
  value       = docker_network.capstone.id
}
```

**docker/advanced-capstone/.github/workflows/ci.yml**
Minimal CI workflow (per RESEARCH.md recommendation — capstone challenge is monitoring/diagnosis, not the pipeline itself):
```yaml
name: Capstone CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd docker/advanced-capstone/app && npm ci

      - name: Lint
        run: cd docker/advanced-capstone/app && npm run lint

      - name: Test
        run: cd docker/advanced-capstone/app && npm test

  build:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image
        uses: docker/build-push-action@v7
        with:
          context: docker/advanced-capstone/app
          push: false
          tags: capstone-app:latest
```

**docker/advanced-capstone/README.md**
The capstone brief:
```markdown
# Advanced Capstone: Full-Stack Monitoring Pipeline

## The Challenge

You are the new DevOps engineer at a startup. They have a Node.js API that is
"working fine" — but users have been reporting occasional slowness that nobody
can reproduce. Your job:

1. **Connect the monitoring pipeline** — the app, Prometheus, Grafana, and
   Alertmanager are provided but need to be running together
2. **Build a Grafana dashboard** — show the four golden signals (latency,
   traffic, errors, saturation)
3. **Diagnose the failure** — there is an intentional problem in the app that
   manifests in the metrics. Find it using PromQL and your dashboard.
4. **Document the incident** — write a postmortem following the template from
   the Incident Response lesson

## What You Have

- `app/` — Node.js API instrumented with prom-client
- `prometheus/` — Prometheus config with scrape targets and alert rules
- `grafana/` — Grafana with auto-provisioned Prometheus datasource
- `alertmanager/` — Alert routing config
- `iac/` — OpenTofu config to provision Prometheus via Docker provider
- `.github/workflows/ci.yml` — CI pipeline (lint + build)
- `compose.yml` — Docker Compose stack

## Steps

1. Start the stack: `docker compose up -d`
2. Verify all services are running: `docker compose ps`
3. Open Prometheus at http://localhost:9090 — check Targets page
4. Open Grafana at http://localhost:3001 — verify Prometheus datasource
5. Build a dashboard with panels for: request rate, P95 latency, error rate,
   memory usage
6. Let the app run for 5-10 minutes. Watch the metrics.
7. Something is wrong. Find it with PromQL.
8. Write the diagnostic PromQL query that proves the issue.
9. Identify the root cause in the source code.
10. Write a postmortem document.

## Verification

Run `bash verify.sh` from the project root to check your progress.

## Hints (only if stuck)

<details>
<summary>Hint 1: What to look for</summary>
Watch memory-related metrics. Is anything growing without bound?
</details>

<details>
<summary>Hint 2: The PromQL query</summary>
Try: increase(nodejs_heap_used_bytes[5m])
</details>

<details>
<summary>Hint 3: The root cause</summary>
Look at app.js for a module-scope array that never gets cleaned up.
</details>

## Difficulty: Challenge

This is the course's final assessment. No step-by-step guidance — use
everything you have learned across all 8 modules.
```

**docker/advanced-capstone/verify.sh**
Copy the exact verify.sh structure from RESEARCH.md capstone verify pattern with all 10 checks:
1. App service running (docker compose ps)
2. Prometheus service running
3. Grafana service running
4. Alertmanager service running
5. Prometheus scrapes app (up{job="app"} == 1 via API query)
6. Grafana API healthy (http://localhost:3001/api/health database=ok)
7. At least 1 alert rule loaded (Prometheus /api/v1/rules)
8. nodejs_heap_used_bytes metric exists in Prometheus
9. OpenTofu state file exists (docker/advanced-capstone/iac/terraform.tfstate) — SKIP if not exists (optional IaC step)
10. CI workflow file exists (.github/workflows/ci.yml)
RESULT: PASS/FAIL summary at end.
Make executable: chmod +x docker/advanced-capstone/verify.sh
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && test -f docker/advanced-capstone/compose.yml && test -f docker/advanced-capstone/verify.sh && test -f docker/advanced-capstone/app/app.js && test -f docker/advanced-capstone/iac/main.tf && test -f docker/advanced-capstone/.github/workflows/ci.yml && echo "PASS: all capstone files exist" || echo "FAIL: missing files"</automated>
  </verify>
  <acceptance_criteria>
    - docker/advanced-capstone/app/app.js contains "leakStore" (memory leak array)
    - docker/advanced-capstone/app/app.js contains "setInterval" (leak trigger)
    - docker/advanced-capstone/app/app.js contains "Buffer.alloc(1024)" (leak payload)
    - docker/advanced-capstone/app/app.js contains "prom-client" and "/metrics"
    - docker/advanced-capstone/app/app.js does NOT contain console.log about the leak (must be silent)
    - docker/advanced-capstone/app/package.json contains '"prom-client": "15.1.3"'
    - docker/advanced-capstone/app/Dockerfile contains "node:20-alpine" and "HEALTHCHECK" and "appuser"
    - docker/advanced-capstone/compose.yml contains "mem_limit: 256m" (predictable OOM threshold)
    - docker/advanced-capstone/compose.yml contains "prom/prometheus:v3.10.0"
    - docker/advanced-capstone/compose.yml contains "grafana/grafana:12.4.1"
    - docker/advanced-capstone/compose.yml contains "3001:3000" (Grafana port)
    - docker/advanced-capstone/prometheus/prometheus.yml contains 'job_name: "app"' and "app:3000"
    - docker/advanced-capstone/prometheus/rules/alerts.yml contains "HighMemoryUsage" and "HeapGrowthAnomaly"
    - docker/advanced-capstone/iac/main.tf contains "kreuzwerker/docker" and "3.9.0"
    - docker/advanced-capstone/iac/main.tf contains "registry.opentofu.org"
    - docker/advanced-capstone/iac/variables.tf contains "prometheus_port"
    - docker/advanced-capstone/.github/workflows/ci.yml contains "actions/checkout@v6"
    - docker/advanced-capstone/.github/workflows/ci.yml contains "docker/build-push-action@v7"
    - docker/advanced-capstone/verify.sh contains "PASS" and "FAIL" and "nodejs_heap_used_bytes"
    - docker/advanced-capstone/verify.sh is executable
    - docker/advanced-capstone/README.md contains "Advanced Capstone" and "memory" and "Difficulty: Challenge"
  </acceptance_criteria>
  <done>Complete advanced capstone directory with instrumented app (memory leak), Prometheus/Grafana/Alertmanager configs, OpenTofu IaC, CI workflow, verify.sh, and README brief. All files created.</done>
</task>

<task type="auto">
  <name>Task 2: Write full advanced capstone MDX lesson content</name>
  <files>content/modules/08-monitoring/07-advanced-capstone.mdx</files>
  <read_first>
    - content/modules/08-monitoring/07-advanced-capstone.mdx (current stub from plan 07-02)
    - content/modules/03-docker/09-foundation-capstone.mdx (Phase 4 capstone MDX pattern)
    - docker/advanced-capstone/README.md (capstone brief — just created in Task 1)
  </read_first>
  <action>
Overwrite the stub at content/modules/08-monitoring/07-advanced-capstone.mdx with full capstone content:

Frontmatter: title "Advanced Capstone: Full-Stack Monitoring Pipeline", description "Deploy a complete application with CI/CD, IaC, and monitoring — then diagnose an intentional failure using everything you have learned", module "Monitoring & Observability", moduleSlug "08-monitoring", lessonSlug "07-advanced-capstone", order 7, difficulty "Challenge", estimatedMinutes 90, prerequisites ["08-monitoring/05-incident-response"], tags ["capstone", "advanced", "monitoring", "cicd", "iac", "docker", "prometheus", "grafana", "memory-leak", "incident-response"].

## Overview
"Why This Matters" scenario: You just joined a startup as their first DevOps engineer. They have a Node.js API that is "working fine in production" — but users report intermittent slowness. The previous developer left no monitoring, no dashboards, no alerting. Your mission: set up the full observability pipeline and diagnose the hidden problem.

This is the final project of the course. Everything you have learned — Linux fundamentals, networking, Docker, CI/CD, IaC, and monitoring — comes together here. There are no step-by-step instructions. You have a brief, starter code, and a verification script.

## What You Will Build

Describe the full architecture:
- **Application layer:** Node.js API instrumented with prom-client (Docker, from Module 4)
- **CI/CD pipeline:** GitHub Actions workflow that lints and builds (from Module 5)
- **Infrastructure as Code:** OpenTofu config provisioning Prometheus via Docker provider (from Module 6)
- **Monitoring:** Prometheus scraping app metrics, Grafana dashboards, Alertmanager routing (from Module 8)
- **The Challenge:** An intentional failure hidden in the application that you must diagnose using your monitoring stack

Show an ASCII architecture diagram:
```
[GitHub Actions CI]
       |
       v
  [Docker Build]
       |
       v
  [Docker Compose Stack]
  +-----------+    +------------+    +---------+
  |   App     |--->| Prometheus |--->| Grafana |
  | (Node.js) |    | (scrape)   |    | (dash)  |
  +-----------+    +------------+    +---------+
       |                |
       v                v
  [/metrics]      [Alert Rules]
                       |
                       v
                [Alertmanager]
                       |
                       v
                [Webhook → App logs]
```

## The Capstone Brief

ExerciseCard difficulty="Challenge", scenario "You are the new DevOps engineer. The app works but users report mysterious slowness. Set up monitoring, build a dashboard, and find the problem — no hand-holding."

Steps:
1. Navigate to docker/advanced-capstone/
2. Read README.md for the full brief
3. Start the stack: docker compose up -d
4. Verify services are running: docker compose ps
5. Open Prometheus (http://localhost:9090) — check the Targets page to confirm the app is being scraped
6. Open Grafana (http://localhost:3001) — verify the Prometheus datasource is auto-provisioned
7. Build a Grafana dashboard with 4 panels: request rate, P95 latency, error rate, memory usage
8. Generate some traffic: run a loop like `for i in $(seq 1 100); do curl -s http://localhost:3000/ > /dev/null; curl -s http://localhost:3000/slow > /dev/null; done`
9. Let the app run for 5-10 minutes. Watch your dashboard.
10. Something is wrong. Use PromQL to identify the issue.
11. Write the diagnostic query that proves the problem.
12. Find the root cause in the source code (app/app.js).
13. Write a postmortem document following the template from the Incident Response lesson.
14. (Optional) Run tofu init && tofu apply in the iac/ directory to provision Prometheus via OpenTofu.
15. Run bash verify.sh to check your progress.

VerificationChecklist items:
- All 4 Docker Compose services running (app, prometheus, grafana, alertmanager)
- Prometheus scrapes app successfully (Targets page shows UP)
- Grafana dashboard has panels for the 4 golden signals
- PromQL query identifies the memory leak (increase(nodejs_heap_used_bytes[5m]) or rate(nodejs_heap_used_bytes[5m]))
- Root cause identified: leakStore array in app.js accumulating via setInterval
- Postmortem written with Summary, Timeline, Root Cause, and Action Items

## Skills Integrated

Callout type="deep-dive": "Count the modules you are using in this capstone:
1. **Linux Fundamentals** — reading logs, understanding processes, memory management
2. **Networking** — HTTP endpoints, port mapping, DNS resolution between containers
3. **Docker** — Compose stack, multi-stage builds, health checks, resource limits
4. **System Administration** — service management, log analysis, resource monitoring
5. **CI/CD** — GitHub Actions pipeline for linting and building
6. **Infrastructure as Code** — OpenTofu provisioning Docker resources
7. **Cloud Fundamentals** — understanding the cloud equivalent of everything you built locally
8. **Monitoring** — Prometheus, Grafana, Alertmanager, incident response

This is what real-world DevOps looks like."

Callout type="tip" (final course message): "Congratulations on completing Learn Systems. You have built the entire stack from bare Linux concepts to production monitoring. Every tool you used — Docker, Prometheus, Grafana, OpenTofu, GitHub Actions — is the same toolchain used by real DevOps teams. The difference between a junior and senior engineer is not knowing more tools; it is understanding WHY each tool exists and HOW it works under the hood. You now have that foundation."

QuickReference with a table: Module | Key Skill Used in Capstone | Example rows mapping each of the 8 modules to its capstone application.
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npx vitest run lib/__tests__/modules.test.ts lib/__tests__/mdx.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains 'moduleSlug: "08-monitoring"'
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains 'difficulty: "Challenge"'
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains 'estimatedMinutes: 90'
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "ExerciseCard"
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "VerificationChecklist"
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "docker/advanced-capstone"
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "nodejs_heap_used_bytes" (the diagnostic query)
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "leakStore" (root cause hint in verification)
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains "postmortem" (incident response integration)
    - content/modules/08-monitoring/07-advanced-capstone.mdx file length > 100 lines (not a stub)
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Full advanced capstone MDX lesson with architecture diagram, exercise brief, verification checklist, skills integration callout, and final course message. All tests pass. Phase 7 complete.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (all tests including monitoring module count of 7)
- `ls docker/advanced-capstone/ -R` shows complete directory tree (app/, prometheus/, grafana/, alertmanager/, iac/, .github/workflows/)
- `docker/advanced-capstone/verify.sh` exists and is executable
- `docker/advanced-capstone/app/app.js` contains the memory leak (leakStore + setInterval)
- `content/modules/08-monitoring/07-advanced-capstone.mdx` is full content (not stub)
- `grep -c "leakStore" docker/advanced-capstone/app/app.js` returns >= 1
</verification>

<success_criteria>
Complete advanced capstone project with: (1) instrumented Node.js app with silent memory leak, (2) Prometheus/Grafana/Alertmanager Docker Compose stack with 256m memory limit, (3) OpenTofu IaC config using kreuzwerker/docker v3.9.0, (4) GitHub Actions CI workflow, (5) verify.sh with 10 PASS/FAIL checks, (6) README capstone brief with hints, (7) full MDX lesson with architecture diagram and skills integration. The memory leak is detectable via increase(nodejs_heap_used_bytes[5m]) within 5 minutes of app startup.
</success_criteria>

<output>
After completion, create `.planning/phases/07-monitoring-advanced-capstone/07-03-SUMMARY.md`
</output>
