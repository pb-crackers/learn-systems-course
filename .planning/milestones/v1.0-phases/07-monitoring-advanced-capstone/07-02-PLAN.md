---
phase: 07-monitoring-advanced-capstone
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/monitoring/app/app.js
  - docker/monitoring/app/package.json
  - docker/monitoring/app/Dockerfile
  - docker/monitoring/prometheus/prometheus.yml
  - docker/monitoring/prometheus/rules/alerts.yml
  - docker/monitoring/alertmanager/alertmanager.yml
  - docker/monitoring/grafana/provisioning/datasources/prometheus.yml
  - docker/monitoring/grafana/provisioning/dashboards/dashboards.yml
  - docker/monitoring/loki/loki-config.yml
  - docker/monitoring/promtail/promtail-config.yml
  - docker/monitoring/compose.yml
  - docker/monitoring/verify.sh
  - content/modules/08-monitoring/06-cheat-sheet.mdx
  - lib/__tests__/modules.test.ts
autonomous: true
requirements: [MON-06, MON-07]

must_haves:
  truths:
    - "Learner can start the full monitoring stack with docker compose up -d and see Prometheus scraping the app, Grafana with auto-provisioned datasource, and Alertmanager routing alerts"
    - "Learner can start the Loki profile with docker compose --profile loki up -d and query logs in Grafana"
    - "Learner can run verify.sh and see PASS/FAIL output for each monitoring component"
    - "Learner can reference the monitoring cheat sheet for PromQL patterns, Grafana panel types, Loki/LogQL queries, and incident response commands"
  artifacts:
    - path: "docker/monitoring/compose.yml"
      provides: "Full monitoring Docker Compose stack"
      contains: "prom/prometheus:v3.10.0"
    - path: "docker/monitoring/app/app.js"
      provides: "Instrumented Node.js app with prom-client"
      contains: "prom-client"
    - path: "docker/monitoring/verify.sh"
      provides: "Stack verification script"
      contains: "PASS"
    - path: "content/modules/08-monitoring/06-cheat-sheet.mdx"
      provides: "MON-07 cheat sheet"
      contains: "moduleSlug: \"08-monitoring\""
    - path: "lib/__tests__/modules.test.ts"
      provides: "Vitest assertion for monitoring module lesson count"
      contains: "monitoring module has 7 lessons"
  key_links:
    - from: "docker/monitoring/compose.yml"
      to: "docker/monitoring/prometheus/prometheus.yml"
      via: "volume mount for Prometheus config"
      pattern: "prometheus.yml:/etc/prometheus/prometheus.yml"
    - from: "docker/monitoring/compose.yml"
      to: "docker/monitoring/grafana/provisioning/"
      via: "volume mount for Grafana auto-provisioning"
      pattern: "provisioning:/etc/grafana/provisioning"
    - from: "docker/monitoring/prometheus/prometheus.yml"
      to: "docker/monitoring/app/app.js"
      via: "scrape target app:3000 hits the /metrics endpoint"
      pattern: "targets:.*app:3000"
---

<objective>
Build the complete Docker Compose monitoring stack (Prometheus + Grafana + Alertmanager + cAdvisor + instrumented Node.js app + Loki/Promtail), create the verify.sh script, write the monitoring cheat sheet, and add the Vitest module count assertion.

Purpose: Provides the hands-on lab infrastructure for all monitoring exercises. The stack must work out of the box with docker compose up -d — no manual configuration. Grafana auto-provisions the Prometheus datasource. Loki/Promtail are available via a compose profile for the log aggregation lesson.
Output: docker/monitoring/ directory with full stack, content/modules/08-monitoring/06-cheat-sheet.mdx, updated modules.test.ts
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-monitoring-advanced-capstone/07-CONTEXT.md
@.planning/phases/07-monitoring-advanced-capstone/07-RESEARCH.md
@content/modules/index.ts
@lib/__tests__/modules.test.ts

<interfaces>
From RESEARCH.md verified patterns:
- prom/prometheus:v3.10.0, grafana/grafana:12.4.1, prom/alertmanager:v0.28.1
- grafana/loki:3.6.0, grafana/promtail:3.6.0, gcr.io/cadvisor/cadvisor:latest
- prom-client 15.1.3 for Node.js instrumentation
- Grafana anonymous access: GF_AUTH_ANONYMOUS_ENABLED=true, GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
- Grafana host port 3001 (app uses 3000)
- Prometheus --web.enable-lifecycle flag for config reload
- Loki/Promtail as compose profile (not always-on)

From lib/__tests__/modules.test.ts:
- Pattern: it('MODULE module has N lessons', () => { ... filter by moduleSlug ... expect(paths).toHaveLength(N) })
- Existing assertions: docker(9), sysadmin(7), cicd(5), iac(5), cloud(6)
- Need to add: monitoring(7) — 5 lessons + cheat sheet + advanced capstone

From content/modules/index.ts:
- Module slug: '08-monitoring', accentColor: 'monitoring', order: 8

Cheat sheet pattern (from Phase 5-6):
- One QuickReference per lesson topic (5 sections for MON-01-05)
- order = lesson count + 1 (order: 6)
- difficulty: "Foundation", estimatedMinutes: 5, prerequisites: []
- What's Next Callout bridging to advanced capstone
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Docker Compose monitoring stack with instrumented app and verify script</name>
  <files>docker/monitoring/app/app.js, docker/monitoring/app/package.json, docker/monitoring/app/Dockerfile, docker/monitoring/prometheus/prometheus.yml, docker/monitoring/prometheus/rules/alerts.yml, docker/monitoring/alertmanager/alertmanager.yml, docker/monitoring/grafana/provisioning/datasources/prometheus.yml, docker/monitoring/grafana/provisioning/dashboards/dashboards.yml, docker/monitoring/loki/loki-config.yml, docker/monitoring/promtail/promtail-config.yml, docker/monitoring/compose.yml, docker/monitoring/verify.sh</files>
  <read_first>
    - .planning/phases/07-monitoring-advanced-capstone/07-RESEARCH.md (all Docker Compose patterns, config files, anti-patterns)
    - docker/app/app.js (existing progressive app — reference for Express patterns)
    - docker/app/Dockerfile.basic (existing Dockerfile pattern)
  </read_first>
  <action>
Create the entire docker/monitoring/ directory tree:

**docker/monitoring/app/package.json**
```json
{
  "name": "monitored-app",
  "version": "1.0.0",
  "description": "Node.js app instrumented with Prometheus metrics",
  "main": "app.js",
  "scripts": { "start": "node app.js" },
  "dependencies": {
    "express": "^4.21.0",
    "prom-client": "15.1.3"
  }
}
```

**docker/monitoring/app/Dockerfile**
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY app.js .
EXPOSE 3000
HEALTHCHECK --interval=10s --timeout=3s CMD wget -qO- http://localhost:3000/health || exit 1
CMD ["node", "app.js"]
```

**docker/monitoring/app/app.js**
Copy the exact instrumented app from RESEARCH.md Pattern 2 with these additions:
- Add a /alert-webhook POST endpoint that logs the received alert body (for Alertmanager webhook exercise): `app.post('/alert-webhook', express.json(), (req, res) => { console.log('[ALERT WEBHOOK]', JSON.stringify(req.body, null, 2)); res.json({ status: 'received' }) })`
- Add a /slow endpoint that simulates latency (100-500ms random delay) for histogram exercise data
- Keep all 3 custom metrics: http_requests_total (Counter), http_request_duration_seconds (Histogram), active_connections (Gauge)
- Keep default metrics collection: client.collectDefaultMetrics({ register })
- The app.listen message should be: `Server on port ${PORT}, metrics at /metrics`

**docker/monitoring/prometheus/prometheus.yml**
Copy exact config from RESEARCH.md Pattern 3:
- global: scrape_interval 15s, evaluation_interval 15s
- alerting: alertmanagers target alertmanager:9093
- rule_files: "rules/*.yml"
- scrape_configs: prometheus (localhost:9090), monitored-app (app:3000), cadvisor (cadvisor:8080)

**docker/monitoring/prometheus/rules/alerts.yml**
Copy exact alert rules from RESEARCH.md Pattern 4:
- AppDown: up{job="monitored-app"} == 0 for 1m, severity critical
- HighRequestLatency: histogram_quantile(0.95, ...) > 0.5 for 2m, severity warning
- HighErrorRate: rate 5xx / total > 0.05 for 2m, severity warning

**docker/monitoring/alertmanager/alertmanager.yml**
Copy exact config from RESEARCH.md Pattern 5:
- route: group_by ['alertname','severity'], group_wait 10s, group_interval 10s, repeat_interval 1h, receiver webhook-log
- receiver webhook-log: webhook_configs url http://app:3000/alert-webhook, send_resolved true

**docker/monitoring/grafana/provisioning/datasources/prometheus.yml**
Copy exact config from RESEARCH.md Pattern 6:
- apiVersion 1, name Prometheus, type prometheus, access proxy, url http://prometheus:9090, isDefault true, editable true, jsonData httpMethod POST

**docker/monitoring/grafana/provisioning/dashboards/dashboards.yml**
Copy exact config from RESEARCH.md:
- apiVersion 1, provider name default, type file, updateIntervalSeconds 30, path /etc/grafana/provisioning/dashboards, foldersFromFilesStructure true

**docker/monitoring/loki/loki-config.yml**
Copy exact config from RESEARCH.md:
- auth_enabled false, server http_listen_port 3100, inmemory ring store, replication_factor 1
- schema_config: from 2024-01-01, store tsdb, object_store filesystem, schema v13
- storage_config: tsdb_shipper paths /loki/tsdb-index and /loki/tsdb-cache, filesystem /loki/chunks
- limits_config: reject_old_samples true, reject_old_samples_max_age 168h

**docker/monitoring/promtail/promtail-config.yml**
```yaml
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
```

**docker/monitoring/compose.yml**
Based on RESEARCH.md Pattern 7 with these specifics:
- Services: app (build ./app, ports 3000:3000), prometheus (prom/prometheus:v3.10.0, ports 9090:9090, --config.file + --storage.tsdb.path + --web.enable-lifecycle), grafana (grafana/grafana:12.4.1, ports 3001:3000, GF_AUTH_ANONYMOUS_ENABLED=true + GF_AUTH_ANONYMOUS_ORG_ROLE=Admin + GF_AUTH_DISABLE_LOGIN_FORM=true), alertmanager (prom/alertmanager:v0.28.1, ports 9093:9093), cadvisor (gcr.io/cadvisor/cadvisor:latest, ports 8080:8080, volumes /:/rootfs:ro + /var/run:/var/run:ro + /sys:/sys:ro + /var/lib/docker/:/var/lib/docker:ro)
- Loki and Promtail services with `profiles: ["loki"]` so they are NOT started by default docker compose up but only with `docker compose --profile loki up -d`
- loki: grafana/loki:3.6.0, ports 3100:3100, command -config.file=/etc/loki/loki-config.yml
- promtail: grafana/promtail:3.6.0, volumes /var/log:/var/log:ro + /var/run/docker.sock:/var/run/docker.sock + config mount, command -config.file=/etc/promtail/promtail-config.yml
- All services on "monitoring" bridge network
- Named volumes: prometheus-data, grafana-data

**docker/monitoring/verify.sh**
Script with check() function (colored PASS/FAIL printf, PASS/FAIL counters). Checks:
1. App container running: docker compose -f docker/monitoring/compose.yml ps --format json | grep app
2. Prometheus container running: same pattern
3. Grafana container running: same pattern
4. Alertmanager container running: same pattern
5. App /metrics endpoint accessible: curl -sf http://localhost:3000/metrics | grep http_requests_total
6. Prometheus scrapes app (up==1): curl Prometheus API query for up{job="monitored-app"}
7. Grafana API healthy: curl http://localhost:3001/api/health, check database=ok
8. Alert rules loaded: curl Prometheus /api/v1/rules, check count >= 1
9. App health endpoint: curl http://localhost:3000/health, check status=healthy
Final summary with RESULT: PASS/FAIL.
Make executable: chmod +x docker/monitoring/verify.sh
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && test -f docker/monitoring/compose.yml && test -f docker/monitoring/verify.sh && test -f docker/monitoring/app/app.js && echo "PASS: all monitoring stack files exist" || echo "FAIL: missing files"</automated>
  </verify>
  <acceptance_criteria>
    - docker/monitoring/compose.yml exists and contains "prom/prometheus:v3.10.0"
    - docker/monitoring/compose.yml contains "grafana/grafana:12.4.1"
    - docker/monitoring/compose.yml contains "prom/alertmanager:v0.28.1"
    - docker/monitoring/compose.yml contains "gcr.io/cadvisor/cadvisor:latest"
    - docker/monitoring/compose.yml contains 'profiles:' (Loki/Promtail as profile)
    - docker/monitoring/compose.yml contains "3001:3000" (Grafana port mapping)
    - docker/monitoring/compose.yml contains "--web.enable-lifecycle"
    - docker/monitoring/app/app.js contains "prom-client" and "http_requests_total" and "/metrics"
    - docker/monitoring/app/app.js contains "/alert-webhook" (Alertmanager webhook endpoint)
    - docker/monitoring/app/package.json contains '"prom-client": "15.1.3"'
    - docker/monitoring/app/Dockerfile contains "node:20-alpine" and "HEALTHCHECK"
    - docker/monitoring/prometheus/prometheus.yml contains "app:3000" and "cadvisor:8080"
    - docker/monitoring/prometheus/rules/alerts.yml contains "AppDown" and "HighRequestLatency" and "HighErrorRate"
    - docker/monitoring/alertmanager/alertmanager.yml contains "webhook-log" and "app:3000/alert-webhook"
    - docker/monitoring/grafana/provisioning/datasources/prometheus.yml contains "http://prometheus:9090"
    - docker/monitoring/loki/loki-config.yml contains "http_listen_port: 3100"
    - docker/monitoring/promtail/promtail-config.yml contains "docker_sd_configs"
    - docker/monitoring/verify.sh contains "PASS" and "FAIL" and is executable
  </acceptance_criteria>
  <done>Complete Docker Compose monitoring stack with Prometheus v3.10.0, Grafana 12.4.1, Alertmanager, cAdvisor, Loki/Promtail (profile), instrumented Node.js app, and verify.sh. All config files use exact versions and patterns from RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: Write monitoring cheat sheet and add Vitest module count assertion</name>
  <files>content/modules/08-monitoring/06-cheat-sheet.mdx, lib/__tests__/modules.test.ts</files>
  <read_first>
    - content/modules/06-iac/05-cheat-sheet.mdx (cheat sheet MDX pattern reference — if exists, else use 04-sysadmin/07-cheat-sheet.mdx)
    - lib/__tests__/modules.test.ts (current test file to add monitoring assertion)
    - content/modules/07-cloud/06-cheat-sheet.mdx (most recent cheat sheet pattern)
  </read_first>
  <action>
**content/modules/08-monitoring/06-cheat-sheet.mdx (MON-07)**
Frontmatter: title "Monitoring & Observability Cheat Sheet", description "Essential Prometheus, Grafana, Loki, and incident response commands and patterns in one place", module "Monitoring & Observability", moduleSlug "08-monitoring", lessonSlug "06-cheat-sheet", order 6, difficulty "Foundation", estimatedMinutes 5, prerequisites [], tags ["cheat-sheet", "prometheus", "grafana", "loki", "alerting", "monitoring"].

5 QuickReference sections (one per lesson topic):

1. **Observability Concepts** — table with Pillar | What It Measures | Tool | When To Use First rows for Metrics/Logs/Traces. Include the "start with metrics, drill into logs, trace across services" flow.

2. **Prometheus & PromQL** — table with Pattern | PromQL | Use Case rows for: instant vector (http_requests_total{job="app"}), rate (rate(http_requests_total[5m])), P95 latency (histogram_quantile(0.95,...)), sum by label (sum(rate(...)) by (route)), up check (up{job="app"}), memory (process_resident_memory_bytes), heap growth (increase(nodejs_heap_used_bytes[5m])). Include scrape config reload command: curl -X POST http://localhost:9090/-/reload.

3. **Grafana** — table with Panel Type | Best For | Example PromQL rows for: Time Series (request rate over time / rate(http_requests_total[5m])), Stat (single number / up{job="app"}), Gauge (percentage / container_memory_usage / limit), Table (top N endpoints), Heatmap (latency distribution / histogram buckets). Include key env vars: GF_AUTH_ANONYMOUS_ENABLED=true, GF_AUTH_ANONYMOUS_ORG_ROLE=Admin. Note port mapping: host 3001 -> container 3000.

4. **Log Aggregation** — table with Tool | Role | Query Example rows for: Loki (log store / {job="app"}), Promtail (log shipper / scrape_configs in YAML), LogQL text filter ({container="app"} |= "error"), LogQL count (count_over_time({job="app"}[5m])). Include the Loki vs ELK tradeoff: Loki = label-indexed (lightweight), ELK = full-text indexed (powerful but heavy).

5. **Incident Response** — table with Concept | Key Points rows for: Alert Lifecycle (metric threshold -> PENDING -> FIRING -> Alertmanager -> notification), Runbook Format (Symptoms / Diagnosis Steps / Resolution Actions / Escalation), Postmortem Format (Summary / Timeline / Root Cause / Action Items), On-Call Rule (you own it until resolved or escalated).

What's Next Callout type="tip": "Ready for the final challenge? The Advanced Capstone integrates everything you have learned across the entire course — Linux, networking, Docker, CI/CD, IaC, and monitoring — into one project with an intentional failure you must diagnose."

**lib/__tests__/modules.test.ts**
Add a new test assertion in the getAllLessonPaths describe block:
```typescript
it('monitoring module has 7 lessons', () => {
  const paths = getAllLessonPaths()
  const monitoringPaths = paths.filter(p => p.moduleSlug === '08-monitoring')
  expect(monitoringPaths).toHaveLength(7)
})
```
This counts: 01-05 lessons + 06-cheat-sheet + 07-advanced-capstone = 7. Note: the test will initially fail until plan 07-03 creates the capstone MDX file. This is expected — document in the commit message that the test expects 7 but only 6 exist until plan 07-03 runs.

IMPORTANT: Actually, to avoid a failing test between plans, write the 07-advanced-capstone.mdx stub (minimal frontmatter only, like Phase 4 did with 09-foundation-capstone.mdx) so the test passes immediately:

Create content/modules/08-monitoring/07-advanced-capstone.mdx as a stub:
```mdx
---
title: "Advanced Capstone: Full-Stack Monitoring Pipeline"
description: "Deploy a complete application with CI/CD, IaC, and monitoring — then diagnose an intentional failure"
module: "Monitoring & Observability"
moduleSlug: "08-monitoring"
lessonSlug: "07-advanced-capstone"
order: 7
difficulty: "Challenge"
estimatedMinutes: 90
prerequisites: ["08-monitoring/05-incident-response"]
tags: ["capstone", "advanced", "monitoring", "cicd", "iac", "docker", "prometheus", "grafana"]
---

## Advanced Capstone

*Full content will be added in plan 07-03.*
```

This ensures `npx vitest run` passes immediately with 7 lessons detected.
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npx vitest run lib/__tests__/modules.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/08-monitoring/06-cheat-sheet.mdx exists and contains 'moduleSlug: "08-monitoring"'
    - content/modules/08-monitoring/06-cheat-sheet.mdx contains "QuickReference" (at least 5 instances)
    - content/modules/08-monitoring/06-cheat-sheet.mdx contains "rate(http_requests_total" (PromQL in cheat sheet)
    - content/modules/08-monitoring/06-cheat-sheet.mdx contains "histogram_quantile" (PromQL in cheat sheet)
    - content/modules/08-monitoring/06-cheat-sheet.mdx contains "LogQL" (Loki query reference)
    - content/modules/08-monitoring/06-cheat-sheet.mdx difficulty is "Foundation"
    - content/modules/08-monitoring/06-cheat-sheet.mdx order is 6
    - content/modules/08-monitoring/07-advanced-capstone.mdx exists (stub for test)
    - content/modules/08-monitoring/07-advanced-capstone.mdx contains 'moduleSlug: "08-monitoring"' and 'difficulty: "Challenge"'
    - lib/__tests__/modules.test.ts contains "monitoring module has 7 lessons"
    - lib/__tests__/modules.test.ts contains "moduleSlug === '08-monitoring'"
    - npx vitest run lib/__tests__/modules.test.ts exits 0
  </acceptance_criteria>
  <done>Monitoring cheat sheet with 5 QuickReference sections, Vitest assertion for 7 monitoring lessons, and capstone stub MDX. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (all tests including new monitoring module count)
- `ls content/modules/08-monitoring/*.mdx | wc -l` returns 7 (5 lessons + cheat sheet + capstone stub)
- `ls docker/monitoring/ -R` shows complete directory tree
- `docker/monitoring/verify.sh` exists and is executable
- `docker/monitoring/compose.yml` contains all 5 base services + 2 profile services
</verification>

<success_criteria>
Complete Docker Compose monitoring stack works with docker compose up -d. Grafana auto-provisions Prometheus datasource. Loki/Promtail available via --profile loki. Cheat sheet has 5 QuickReference sections covering all lesson topics. Vitest passes with monitoring module count of 7.
</success_criteria>

<output>
After completion, create `.planning/phases/07-monitoring-advanced-capstone/07-02-SUMMARY.md`
</output>
