---
phase: 03-networking-foundations
plan: "04"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - content/modules/02-networking/08-cheat-sheet.mdx
autonomous: true
requirements: [NET-09]

must_haves:
  truths:
    - "Learner can reference the networking cheat sheet for all commands and concepts from the module"
    - "Cheat sheet includes a What's Next callout bridging to Phase 4 Docker networking"
  artifacts:
    - path: "content/modules/02-networking/08-cheat-sheet.mdx"
      provides: "Module cheat sheet with QuickReference sections for all 7 lesson topics"
      contains: "moduleSlug: \"02-networking\""
  key_links:
    - from: "content/modules/02-networking/08-cheat-sheet.mdx"
      to: "content/modules/index.ts"
      via: "moduleSlug: 02-networking matches MODULES registry slug"
      pattern: "moduleSlug.*02-networking"
---

<objective>
Create the Networking Foundations module cheat sheet with QuickReference sections covering all 7 lesson topics plus a "What's Next" callout bridging to Docker networking in Phase 4.

Purpose: Provides learners a single-page reference for all networking commands and concepts from the module, matching the LNX-11 cheat sheet pattern from Phase 2.
Output: content/modules/02-networking/08-cheat-sheet.mdx
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-networking-foundations/03-CONTEXT.md
@.planning/phases/03-networking-foundations/03-RESEARCH.md

@content/modules/01-linux-fundamentals/10-cheat-sheet.mdx (Phase 2 cheat sheet — direct pattern reference)
@content/modules/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Networking module cheat sheet</name>
  <files>content/modules/02-networking/08-cheat-sheet.mdx</files>
  <read_first>
    - content/modules/01-linux-fundamentals/10-cheat-sheet.mdx (Phase 2 cheat sheet — must replicate this pattern exactly)
    - content/modules/index.ts (confirm slug)
    - content/modules/02-networking/01-how-networks-work.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/02-tcp-ip-stack.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/03-dns.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/04-http-https.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/05-ssh.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/06-firewalls.mdx (Quick Reference section — extract key terms)
    - content/modules/02-networking/07-troubleshooting.mdx (Quick Reference section — extract key terms)
    - components/content/QuickReference.tsx (component interface)
    - components/content/Callout.tsx (for What's Next callout)
  </read_first>
  <action>
**File: content/modules/02-networking/08-cheat-sheet.mdx**

Frontmatter (exact values — matches Phase 2 cheat sheet pattern):
```yaml
title: "Networking Foundations Cheat Sheet"
description: "Quick reference for all networking commands, protocols, and concepts covered in this module"
module: "Networking Foundations"
moduleSlug: "02-networking"
lessonSlug: "08-cheat-sheet"
order: 8
difficulty: "Foundation"
estimatedMinutes: 5
prerequisites: []
tags: ["cheat-sheet", "reference", "networking", "commands"]
```

Structure — one QuickReference component per lesson topic (7 sections), following the Phase 2 pattern of one per topic (per decision in STATE.md):

1. **## Physical Networking** — QuickReference with: MAC address format, switch vs router, broadcast domain, ARP, key `ip` commands (ip addr, ip neigh)

2. **## TCP/IP Stack** — QuickReference with: Four layers (Application, Transport, Internet, Link), TCP vs UDP comparison, common port numbers (22, 53, 80, 443), CIDR notation examples, encapsulation order

3. **## DNS** — QuickReference with: Record types (A, AAAA, CNAME, MX, NS, PTR, TXT), dig commands (dig +short, dig +trace, dig -x, dig @server), resolution chain (client → recursive → root → TLD → authoritative), TTL concept

4. **## HTTP/HTTPS** — QuickReference with: HTTP methods (GET, POST, PUT, PATCH, DELETE), common status codes (200, 201, 301, 302, 400, 401, 403, 404, 500, 502, 503), curl commands (curl -v, curl -I, curl -X POST -d, curl -k), TLS handshake summary

5. **## SSH** — QuickReference with: ssh-keygen (generate keys), ssh-copy-id (copy public key), SSH config file format (Host, HostName, User, IdentityFile, ProxyJump), tunneling syntax (-L local, -R remote, -D dynamic), agent forwarding (-A)

6. **## Firewalls** — QuickReference with: iptables commands (iptables -L -n, iptables -A INPUT, iptables -P INPUT DROP), ufw commands (ufw enable, ufw allow, ufw deny, ufw status), netfilter chain flow (PREROUTING → INPUT/FORWARD/OUTPUT → POSTROUTING), security groups concept

7. **## Network Troubleshooting** — QuickReference with: Systematic checklist (ping → traceroute → ss → curl → tcpdump), ping flags (-c count, -i interval), traceroute interpretation, ss flags (-tlnp), tcpdump filters (port, host, -c count, -A ASCII), curl debugging (-v, -w, --connect-timeout)

After the 7 QuickReference sections, add:

8. **What's Next callout** (per CONTEXT.md locked decision):
```mdx
<Callout type="tip" title="What's Next: Docker Networking">
Everything you learned in this module — bridge networks, container DNS, IP routing, port
mapping, firewall rules — is exactly what Docker networking builds on. When you run
`docker network create mynet`, Docker creates a Linux bridge interface. When containers
resolve each other by service name, that's the same DNS mechanism you explored in the DNS
lesson. When you publish a port with `-p 8080:80`, that's iptables NAT rules under the
hood. The Docker module will feel familiar because you already understand the primitives.
</Callout>
```
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File content/modules/02-networking/08-cheat-sheet.mdx exists
    - File contains "moduleSlug: \"02-networking\""
    - File contains "order: 8"
    - File contains "difficulty: \"Foundation\""
    - File contains "estimatedMinutes: 5"
    - File contains "prerequisites: []"
    - File contains exactly 7 "<QuickReference" components (one per lesson topic)
    - File contains "<Callout" with "What's Next" or "Docker Networking"
    - File contains "## Physical Networking" or similar heading for each of the 7 topics
    - File contains "dig" (DNS commands), "curl" (HTTP commands), "ssh-keygen" (SSH commands), "iptables" (firewall commands), "tcpdump" (troubleshooting commands)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Cheat sheet MDX file exists with 7 QuickReference sections (one per lesson topic), a What's Next callout bridging to Docker networking, correct frontmatter with order 8 and moduleSlug "02-networking". Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm test` passes
- Cheat sheet has correct frontmatter matching Phase 2 pattern (order: 8, difficulty: Foundation, estimatedMinutes: 5, prerequisites: [])
- All 7 lesson topics have a QuickReference section
- What's Next callout mentions Docker networking
- Module now has 8 MDX files total (7 lessons + 1 cheat sheet)
</verification>

<success_criteria>
Networking cheat sheet exists at content/modules/02-networking/08-cheat-sheet.mdx with 7 QuickReference sections covering all module topics, a What's Next callout bridging to Phase 4, and valid frontmatter. Build and tests pass. The full networking module now has 8 complete MDX files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-networking-foundations/03-04-SUMMARY.md`
</output>
