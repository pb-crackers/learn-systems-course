---
phase: 10-linux-fundamentals-prototype
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/modules/01-linux-fundamentals/03-linux-filesystem.mdx
  - content/modules/01-linux-fundamentals/04-file-permissions.mdx
autonomous: true
requirements: [MIGR-06]

must_haves:
  truths:
    - "Every Foundation exercise step with a command in lessons 03 and 04 has a complete annotations array"
    - "Every exercise in lessons 03 and 04 has at least one ScenarioQuestion connecting commands to the opening scenario"
    - "Both ExerciseCards have annotated={true} prop"
  artifacts:
    - path: "content/modules/01-linux-fundamentals/03-linux-filesystem.mdx"
      provides: "Annotated Foundation exercise with ScenarioQuestions"
      contains: "annotated={true}"
    - path: "content/modules/01-linux-fundamentals/04-file-permissions.mdx"
      provides: "Annotated Foundation exercise with ScenarioQuestions"
      contains: "annotated={true}"
  key_links:
    - from: "03-linux-filesystem.mdx ExerciseCard steps"
      to: "CommandAnnotation[] in each step"
      via: "annotations array co-located in step object"
      pattern: "annotations:\\s*\\["
    - from: "04-file-permissions.mdx ExerciseCard steps"
      to: "CommandAnnotation[] in each step"
      via: "annotations array co-located in step object"
      pattern: "annotations:\\s*\\["
---

<objective>
Migrate the two remaining Foundation lessons in Linux Fundamentals (03-linux-filesystem and 04-file-permissions) to use per-flag command annotations and ScenarioQuestions.

Purpose: These are the heavier Foundation lessons — 10 command fields each (20 total). Completing them finishes all 34 Foundation command annotations in this module.

Output: Two fully annotated MDX lessons with ScenarioQuestions. All 4 Foundation lessons in the module are now migrated.
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-linux-fundamentals-prototype/10-CONTEXT.md

<interfaces>
From types/exercises.ts:
```typescript
export interface CommandAnnotation {
  token: string
  description: string  // Max 120 chars, verb-first, no backticks/braces/angle brackets
  example?: string
}

export interface ExerciseStep {
  step: number
  description: string
  command?: string
  annotations?: CommandAnnotation[]
}
```

From components/content/ScenarioQuestion.tsx:
```typescript
interface ScenarioQuestionProps {
  question: string
  answer: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate 03-linux-filesystem.mdx and add ScenarioQuestions</name>
  <files>content/modules/01-linux-fundamentals/03-linux-filesystem.mdx</files>
  <read_first>
    - content/modules/01-linux-fundamentals/03-linux-filesystem.mdx
    - docs/design/annotation-style-guide.md
    - types/exercises.ts
    - components/content/ExerciseCard.tsx
    - components/content/ScenarioQuestion.tsx
  </read_first>
  <action>
Read the full MDX file. The exercise "Navigate the Filesystem Hierarchy and Explore Inodes" has 10 steps with commands. The scenario is about onboarding to a new Linux server — understanding the filesystem layout and diagnosing a "no space left on device" error that appeared despite df showing plenty of space (inode exhaustion).

For each of the 10 steps, author a complete `annotations` array:

1. `docker run --rm -it learn-systems-linux bash` — docker, run, --rm, -it, learn-systems-linux, bash
2. `ls /bin /etc /var/log /proc /dev | head -10` — ls, the 5 directory paths, head, -10
3. `stat /etc/hostname` — stat, /etc/hostname
4. `ls -i /etc | head -15` — ls, -i, /etc, head, -15
5. `echo 'test content' > /tmp/testfile.txt && ln /tmp/testfile.txt /tmp/testfile-hardlink.txt && ls -li /tmp/testfile*.txt` — echo, the string, >, /tmp/testfile.txt, &&, ln, paths, &&, ls, -l, -i, glob pattern
6. `ln -s /tmp/testfile.txt /tmp/testfile-softlink.txt && ls -li /tmp/testfile*.txt` — ln, -s, paths, &&, ls, -l, -i, glob
7. `rm /tmp/testfile.txt && cat /tmp/testfile-hardlink.txt && cat /tmp/testfile-softlink.txt 2>&1` — rm, path, &&, cat, paths, 2>&1
8. `findmnt --tree` — findmnt, --tree
9. `df -h && echo '---' && df -i` — df, -h, &&, echo, &&, df, -i
10. `ls -la /dev | grep '^[bc]' | head -10` — ls, -l, -a, /dev, grep, the pattern, head, -10

Add `annotated={true}` to the ExerciseCard props.

Author at least 2 ScenarioQuestions tying back to the scenario. The RIGHT kind of question:
- "You ran df -h and see 40% disk used, but a colleague reported 'no space left on device.' What would you check next, and what command would you use?" (ties to the inode exhaustion scenario — answer: df -i to check inode usage)
- "You found a symlink pointing to a config file that was deleted. The application is failing to start. Would creating a hard link instead have prevented this failure? Why?" (ties hard/soft link understanding to the onboarding/diagnostic scenario)

Place ScenarioQuestions in the ExerciseCard children area.
  </action>
  <verify>
    <automated>grep -c 'annotations:' content/modules/01-linux-fundamentals/03-linux-filesystem.mdx | grep -q '[1-9][0-9]' && grep -c 'ScenarioQuestion' content/modules/01-linux-fundamentals/03-linux-filesystem.mdx | grep -q '[2-9]' && grep -q 'annotated={true}' content/modules/01-linux-fundamentals/03-linux-filesystem.mdx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 exercise steps with commands have `annotations` arrays
    - Every token in every command is annotated (no partial coverage)
    - `annotated={true}` prop is present on the ExerciseCard
    - At least 2 ScenarioQuestion components are present
    - ScenarioQuestions reference the server onboarding and inode exhaustion scenario
    - Annotation descriptions are verb-first, max 120 chars, no forbidden characters
    - Annotations are ordered left-to-right matching command token order
  </acceptance_criteria>
  <done>Lesson 03-linux-filesystem.mdx has complete per-flag annotations on all 10 command steps and at least 2 scenario-contextualized questions</done>
</task>

<task type="auto">
  <name>Task 2: Annotate 04-file-permissions.mdx and add ScenarioQuestions</name>
  <files>content/modules/01-linux-fundamentals/04-file-permissions.mdx</files>
  <read_first>
    - content/modules/01-linux-fundamentals/04-file-permissions.mdx
    - docs/design/annotation-style-guide.md
    - types/exercises.ts
    - components/content/ExerciseCard.tsx
    - components/content/ScenarioQuestion.tsx
  </read_first>
  <action>
Read the full MDX file. The exercise "Secure a Web Application File Structure" has 10 steps with commands. The scenario is about setting up a web application's file permissions — the app runs as www-data and needs to read config, write logs, and execute scripts, but the config contains database credentials that must be private.

For each of the 10 steps, author a complete `annotations` array:

1. `docker run --rm -it learn-systems-linux bash` — docker, run, --rm, -it, learn-systems-linux, bash
2. `mkdir -p /opt/webapp/{config,logs,scripts,public} && ls -la /opt/webapp/` — mkdir, -p, the path with brace expansion, &&, ls, -l, -a, /opt/webapp/
3. `echo 'DB_PASSWORD=secret123' > /opt/webapp/config/app.conf && touch /opt/webapp/logs/app.log` — echo, the string, >, path, &&, touch, path
4. `ls -la /opt/webapp/config/app.conf /opt/webapp/logs/app.log` — ls, -l, -a, the two file paths
5. `chmod 600 /opt/webapp/config/app.conf && ls -la /opt/webapp/config/app.conf` — chmod, 600, path, &&, ls, -l, -a, path
6. `chmod 750 /opt/webapp/scripts && ls -la /opt/webapp/` — chmod, 750, path, &&, ls, -l, -a, path
7. `chmod +t /opt/webapp/logs && ls -la /opt/webapp/` — chmod, +t, path, &&, ls, -l, -a, path
8. `stat /opt/webapp/logs | grep -E 'Access.*Uid'` — stat, path, grep, -E, the pattern
9. `useradd -m testuser && su - testuser -c 'cat /opt/webapp/config/app.conf'` — useradd, -m, testuser, &&, su, -, testuser, -c, the command string
10. `ls -la / | grep tmp && stat /tmp` — ls, -l, -a, /, grep, tmp, &&, stat, /tmp

Add `annotated={true}` to the ExerciseCard props.

Author at least 2 ScenarioQuestions tying back to the scenario. The RIGHT kind of question:
- "The config file has permissions 600 (owner read/write only). If the web application runs as www-data but the file is owned by root, can the application read the config? What would you need to change?" (ties chmod knowledge to the real web app scenario)
- "A junior developer set the logs directory to chmod 777 so everyone can write logs. What security risk does this create, and what permission would you use instead?" (ties permission best practices to the scenario)

Place ScenarioQuestions in the ExerciseCard children area.
  </action>
  <verify>
    <automated>grep -c 'annotations:' content/modules/01-linux-fundamentals/04-file-permissions.mdx | grep -q '[1-9][0-9]' && grep -c 'ScenarioQuestion' content/modules/01-linux-fundamentals/04-file-permissions.mdx | grep -q '[2-9]' && grep -q 'annotated={true}' content/modules/01-linux-fundamentals/04-file-permissions.mdx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 exercise steps with commands have `annotations` arrays
    - Every token in every command is annotated (no partial coverage)
    - `annotated={true}` prop is present on the ExerciseCard
    - At least 2 ScenarioQuestion components are present
    - ScenarioQuestions reference the web application permission scenario
    - Annotation descriptions are verb-first, max 120 chars, no forbidden characters
    - Annotations are ordered left-to-right matching command token order
  </acceptance_criteria>
  <done>Lesson 04-file-permissions.mdx has complete per-flag annotations on all 10 command steps and at least 2 scenario-contextualized questions</done>
</task>

</tasks>

<verification>
- grep for `annotated={true}` in both files — must be present
- grep for `annotations:` — must appear 10 times in each file
- grep for `ScenarioQuestion` — must appear at least 2 times in each file
- No forbidden characters in annotation description strings
- TypeScript compilation: `npx tsc --noEmit 2>&1 | grep -v '__tests__'` should show no new errors
</verification>

<success_criteria>
- Lessons 03-linux-filesystem and 04-file-permissions are fully annotated Foundation exercises
- Each has at least 2 ScenarioQuestions that connect commands to the exercise scenario
- All 34 Foundation command fields in the Linux Fundamentals module are now annotated
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/10-linux-fundamentals-prototype/10-02-SUMMARY.md`
</output>
