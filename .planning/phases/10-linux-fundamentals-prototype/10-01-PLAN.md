---
phase: 10-linux-fundamentals-prototype
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/modules/01-linux-fundamentals/01-how-computers-work.mdx
  - content/modules/01-linux-fundamentals/02-operating-systems.mdx
autonomous: true
requirements: [MIGR-06]

must_haves:
  truths:
    - "Every Foundation exercise step with a command in lessons 01 and 02 has a complete annotations array"
    - "Every exercise in lessons 01 and 02 has at least one ScenarioQuestion connecting commands to the opening scenario"
    - "Both ExerciseCards have annotated={true} prop"
  artifacts:
    - path: "content/modules/01-linux-fundamentals/01-how-computers-work.mdx"
      provides: "Annotated Foundation exercise with ScenarioQuestions"
      contains: "annotated={true}"
    - path: "content/modules/01-linux-fundamentals/02-operating-systems.mdx"
      provides: "Annotated Foundation exercise with ScenarioQuestions"
      contains: "annotated={true}"
  key_links:
    - from: "01-how-computers-work.mdx ExerciseCard steps"
      to: "CommandAnnotation[] in each step"
      via: "annotations array co-located in step object"
      pattern: "annotations:\\s*\\["
    - from: "ExerciseCard"
      to: "AnnotatedCommand component"
      via: "annotated={true} prop enables rendering"
      pattern: "annotated=\\{true\\}"
---

<objective>
Migrate the first two Foundation lessons in Linux Fundamentals (01-how-computers-work and 02-operating-systems) to use per-flag command annotations and ScenarioQuestions.

Purpose: Validate the annotation authoring pattern on real content. These two lessons have 14 command fields total (7 each) — a manageable first batch to establish the pattern before the heavier lessons 03 and 04.

Output: Two fully annotated MDX lessons with ScenarioQuestions, serving as the prototype for all remaining Foundation content.
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-linux-fundamentals-prototype/10-CONTEXT.md

<interfaces>
From types/exercises.ts:
```typescript
export interface CommandAnnotation {
  token: string
  description: string  // Max 120 chars, verb-first, no backticks/braces/angle brackets
  example?: string
}

export interface ExerciseStep {
  step: number
  description: string
  command?: string
  annotations?: CommandAnnotation[]
}

export interface ExerciseCardProps {
  title: string
  scenario: string
  difficulty: Difficulty
  objective: string
  steps: ExerciseStep[]
  children?: React.ReactNode
  mode?: ExerciseMode
  annotated?: boolean
  challengePrompt?: string
}
```

From components/content/ScenarioQuestion.tsx:
```typescript
interface ScenarioQuestionProps {
  question: string
  answer: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate 01-how-computers-work.mdx and add ScenarioQuestions</name>
  <files>content/modules/01-linux-fundamentals/01-how-computers-work.mdx</files>
  <read_first>
    - content/modules/01-linux-fundamentals/01-how-computers-work.mdx
    - docs/design/annotation-style-guide.md
    - types/exercises.ts
    - components/content/ExerciseCard.tsx
    - components/content/AnnotatedCommand.tsx
    - components/content/ScenarioQuestion.tsx
  </read_first>
  <action>
Read the full MDX file. The exercise "Inspect Hardware Through the /proc Filesystem" has 7 steps with commands. The scenario is about a junior DevOps engineer profiling a slow server's hardware.

For each step's command, author a complete `annotations` array following the annotation style guide:
- Token ordering: left-to-right as they appear in the command
- Base command annotated first, then flags, then positional args
- Descriptions: verb-first, max 120 chars, explain what the token DOES, no backticks/braces/angle brackets
- Every token in every command must be annotated — no partial annotations

The 7 commands to annotate:
1. `docker build -t learn-systems-linux docker/linux/ && docker run --rm -it learn-systems-linux bash` — annotate: docker, build, -t, learn-systems-linux (image name), docker/linux/ (context dir), &&, docker (second), run, --rm, -it, learn-systems-linux (image), bash
2. `cat /proc/cpuinfo | grep -E 'model name|cpu cores|siblings|processor' | head -20` — annotate: cat, /proc/cpuinfo, grep, -E, the pattern, head, -20
3. `grep -c '^processor' /proc/cpuinfo` — annotate: grep, -c, the pattern, /proc/cpuinfo
4. `cat /proc/meminfo | head -20` — annotate: cat, /proc/meminfo, head, -20
5. `free -h` — annotate: free, -h
6. `lsblk` — annotate: lsblk
7. `df -h` — annotate: df, -h

Add `annotated={true}` to the ExerciseCard props.

Add ScenarioQuestions between exercise steps (as children of ExerciseCard, placed AFTER the VerificationChecklist, OR interspersed before the VerificationChecklist). Since ScenarioQuestion is a client component used in MDX, place them as `children` content after the steps but before the VerificationChecklist closing tag. The questions MUST tie back to the scenario: "You're profiling a slow server to decide whether to upgrade or move the workload."

Author at least 2 ScenarioQuestions for this exercise. Examples of the RIGHT kind of question:
- "You see 2 CPU cores in /proc/cpuinfo. Your application is single-threaded. Would adding more cores help, or do you need faster clock speed? Why?" (ties command output to the scenario's upgrade decision)
- "free -h shows 200MB available out of 8GB total, but 6GB is buff/cache. Should you recommend a RAM upgrade? Why or why not?" (ties memory reading to the upgrade decision)

Place ScenarioQuestions as JSX within the ExerciseCard's children area (inside the ExerciseCard tags, after steps but before or alongside VerificationChecklist).

IMPORTANT: ScenarioQuestion is already registered in mdx-components.tsx. Use it directly as `<ScenarioQuestion question="..." answer="..." />` in the MDX JSX.
  </action>
  <verify>
    <automated>grep -c 'annotations:' content/modules/01-linux-fundamentals/01-how-computers-work.mdx | grep -q '[7-9]\|[1-9][0-9]' && grep -c 'ScenarioQuestion' content/modules/01-linux-fundamentals/01-how-computers-work.mdx | grep -q '[2-9]' && grep -q 'annotated={true}' content/modules/01-linux-fundamentals/01-how-computers-work.mdx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 exercise steps with commands have `annotations` arrays
    - Every token in every command is annotated (no partial coverage)
    - `annotated={true}` prop is present on the ExerciseCard
    - At least 2 ScenarioQuestion components are present
    - ScenarioQuestions reference the server hardware profiling scenario
    - Annotation descriptions are verb-first, max 120 chars, no forbidden characters
    - Annotations are ordered left-to-right matching command token order
  </acceptance_criteria>
  <done>Lesson 01-how-computers-work.mdx has complete per-flag annotations on all 7 command steps and at least 2 scenario-contextualized questions</done>
</task>

<task type="auto">
  <name>Task 2: Annotate 02-operating-systems.mdx and add ScenarioQuestions</name>
  <files>content/modules/01-linux-fundamentals/02-operating-systems.mdx</files>
  <read_first>
    - content/modules/01-linux-fundamentals/02-operating-systems.mdx
    - docs/design/annotation-style-guide.md
    - types/exercises.ts
    - components/content/ExerciseCard.tsx
    - components/content/ScenarioQuestion.tsx
  </read_first>
  <action>
Read the full MDX file. The exercise "Explore the Kernel Interface and Trace System Calls" has 7 steps with commands. The scenario is about debugging a containerized application failing with "Operation not permitted" — diagnosing whether it's a syscall-level issue or application bug.

For each step's command, author a complete `annotations` array:

The 7 commands to annotate:
1. `docker run --rm -it learn-systems-linux bash` — annotate: docker, run, --rm, -it, learn-systems-linux, bash
2. `cat /proc/version && echo '---' && cat /proc/uptime` — annotate: cat, /proc/version, &&, echo, '---', &&, cat, /proc/uptime
3. `cat /proc/loadavg` — annotate: cat, /proc/loadavg
4. `strace -e openat,getdents64 ls / 2>&1 | head -20` — annotate: strace, -e, openat,getdents64 (the filter value), ls, /, 2>&1 (redirect stderr), head, -20
5. `strace -c ls /usr/bin 2>&1` — annotate: strace, -c, ls, /usr/bin, 2>&1
6. `cat /proc/1/cmdline | tr '\0' ' ' && echo && ls /proc/1/fd | head -10` — annotate all tokens
7. `dmesg | tail -20` — annotate: dmesg, tail, -20

Add `annotated={true}` to the ExerciseCard props.

Author at least 2 ScenarioQuestions tying back to the debugging scenario. The RIGHT kind of question:
- "You ran strace -e openat on the failing container and saw 'openat(...) = -1 EACCES'. Based on what you learned about system calls, is this a missing container capability or an application code bug? How would you tell the difference?" (ties strace output to the debugging scenario)
- "The container's PID 1 is /bin/bash (from /proc/1/cmdline). If the application receives SIGTERM, will it shut down gracefully? Why does PID 1 matter here?" (ties /proc reading to the container debugging scenario)

Place ScenarioQuestions in the ExerciseCard children area alongside VerificationChecklist.
  </action>
  <verify>
    <automated>grep -c 'annotations:' content/modules/01-linux-fundamentals/02-operating-systems.mdx | grep -q '[7-9]\|[1-9][0-9]' && grep -c 'ScenarioQuestion' content/modules/01-linux-fundamentals/02-operating-systems.mdx | grep -q '[2-9]' && grep -q 'annotated={true}' content/modules/01-linux-fundamentals/02-operating-systems.mdx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 exercise steps with commands have `annotations` arrays
    - Every token in every command is annotated
    - `annotated={true}` prop is present on the ExerciseCard
    - At least 2 ScenarioQuestion components are present
    - ScenarioQuestions reference the container debugging scenario
    - Annotation descriptions are verb-first, max 120 chars, no forbidden characters
    - Annotations are ordered left-to-right matching command token order
  </acceptance_criteria>
  <done>Lesson 02-operating-systems.mdx has complete per-flag annotations on all 7 command steps and at least 2 scenario-contextualized questions</done>
</task>

</tasks>

<verification>
- grep for `annotated={true}` in both files — must be present
- grep for `annotations:` in both files — must appear 7 times each (one per step with a command)
- grep for `ScenarioQuestion` in both files — must appear at least 2 times each
- No forbidden characters (backticks, curly braces as literal text, angle brackets) inside annotation description strings
- TypeScript compilation: `npx tsc --noEmit 2>&1 | grep -v '__tests__'` should show no new errors
</verification>

<success_criteria>
- Lessons 01-how-computers-work and 02-operating-systems are fully annotated Foundation exercises
- Each has at least 2 ScenarioQuestions that connect commands to the exercise scenario
- The annotation authoring pattern is validated on real content
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/10-linux-fundamentals-prototype/10-01-SUMMARY.md`
</output>
