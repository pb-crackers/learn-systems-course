---
phase: 04-docker-foundation-capstone
plan: "04"
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - content/modules/03-docker/08-cheat-sheet.mdx
  - content/modules/03-docker/09-foundation-capstone.mdx
  - docker/capstone/verify.sh
  - docker/capstone/api/app.js
  - docker/capstone/api/package.json
  - docker/capstone/web/index.html
  - docker/capstone/web/nginx.conf
  - docker/capstone/README.md
autonomous: true
requirements: [DOC-09, CAP-01]

must_haves:
  truths:
    - "Learner can reference the Docker cheat sheet for all Docker and Compose commands covered in the module"
    - "Learner can read the capstone project brief and understand the requirements without step-by-step instructions"
    - "Learner can run verify.sh after deploying their capstone stack and receive PASS/FAIL feedback on all success criteria"
  artifacts:
    - path: "content/modules/03-docker/08-cheat-sheet.mdx"
      provides: "Docker module cheat sheet with 7 QuickReference sections"
      contains: "QuickReference"
    - path: "content/modules/03-docker/09-foundation-capstone.mdx"
      provides: "Foundation capstone project brief with ExerciseCard and VerificationChecklist"
      contains: "ExerciseCard"
    - path: "docker/capstone/verify.sh"
      provides: "Comprehensive capstone verification script testing entire deployed stack"
      contains: "PASS"
    - path: "docker/capstone/api/app.js"
      provides: "Minimal starter API code for capstone (learner Dockerizes it)"
    - path: "docker/capstone/web/index.html"
      provides: "Minimal web frontend for capstone"
    - path: "docker/capstone/README.md"
      provides: "Capstone project brief with API contract and requirements"
  key_links:
    - from: "content/modules/03-docker/09-foundation-capstone.mdx"
      to: "docker/capstone/verify.sh"
      via: "ExerciseCard step referencing 'Run the provided verify.sh'"
      pattern: "docker/capstone/verify.sh"
    - from: "docker/capstone/verify.sh"
      to: "docker compose CLI"
      via: "docker compose -f docker/capstone/compose.yml commands"
      pattern: "docker compose.*capstone"
---

<objective>
Create the Docker module cheat sheet and the Foundation Capstone project (brief, starter code, verification script).

Purpose: The cheat sheet consolidates all Docker knowledge. The capstone is the first open-ended project — learner integrates Linux, networking, and Docker skills to deploy a 3-service Compose app without step-by-step guidance. verify.sh is the most comprehensive verification in the course.
Output: Cheat sheet MDX, capstone MDX, capstone scaffold in docker/capstone/, comprehensive verify.sh
</objective>

<execution_context>
@/Users/phillipdougherty/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phillipdougherty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docker-foundation-capstone/04-CONTEXT.md
@.planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
@.planning/phases/04-docker-foundation-capstone/04-01-SUMMARY.md
@.planning/phases/04-docker-foundation-capstone/04-02-SUMMARY.md
@content/modules/02-networking/08-cheat-sheet.mdx (pattern reference)

<interfaces>
From 04-RESEARCH.md capstone architecture:
- 3-service stack: web (nginx reverse proxy), api (Node.js Express), db (PostgreSQL)
- docker/capstone/ directory structure: verify.sh, api/app.js, api/package.json, web/index.html, web/nginx.conf, README.md
- Learner creates: Dockerfiles, compose.yml, deploy.sh
- Provided: starter app code, verify.sh, README with requirements

From 04-RESEARCH.md verify.sh pattern:
- Runs on HOST using docker compose commands
- check() function with PASS/FAIL counters
- $((PASS + 1)) not ((PASS++)) under set -e
- docker compose exec -T in scripts
- python3 for JSON parsing (not jq)
- || echo "fallback" for commands that may fail
- COMPOSE_FILE="docker/capstone/compose.yml"
- Checks: services running, port accessibility, inter-service networking, non-root user, named volume, deploy.sh exists

From 04-RESEARCH.md capstone ExerciseCard pattern:
- Steps are REQUIREMENTS not instructions
- Hints go in VerificationChecklist items via hint field
- difficulty="Challenge"

From cheat sheet pattern (Phase 2/3):
- One QuickReference per lesson topic (7 sections for 7 lessons)
- order = 8 (second-to-last, before capstone at 9)
- difficulty: "Foundation", estimatedMinutes: 5, prerequisites: []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker module cheat sheet (DOC-09)</name>
  <files>
    content/modules/03-docker/08-cheat-sheet.mdx
  </files>
  <read_first>
    content/modules/02-networking/08-cheat-sheet.mdx
    content/modules/03-docker/01-what-are-containers.mdx
    content/modules/03-docker/02-docker-images.mdx
    content/modules/03-docker/03-docker-containers.mdx
    content/modules/03-docker/04-docker-volumes.mdx
    content/modules/03-docker/05-docker-networking.mdx
    content/modules/03-docker/06-docker-compose.mdx
    content/modules/03-docker/07-dockerfile-best-practices.mdx
  </read_first>
  <action>
    Create `content/modules/03-docker/08-cheat-sheet.mdx`:

    - Frontmatter: title: "Docker & Containerization Cheat Sheet", description: "Quick reference for all Docker commands, Dockerfile instructions, and Compose syntax covered in this module", module: "Docker & Containerization", moduleSlug: "03-docker", lessonSlug: "08-cheat-sheet", order: 8, difficulty: "Foundation", estimatedMinutes: 5, prerequisites: [], tags: ["cheat-sheet", "reference", "docker", "commands", "dockerfile"]

    - Brief intro paragraph: "All Docker commands and concepts from this module in one place."

    - 7 QuickReference sections, one per lesson topic. Follow the exact Phase 3 cheat sheet pattern (read 02-networking/08-cheat-sheet.mdx for structure):

      1. **Container Internals** (from DOC-01): namespace types table (PID, NET, MNT, UTS, IPC, USER), cgroups subsystems, docker inspect --format for PID, /proc/<PID>/ns/ path
      2. **Docker Images** (from DOC-02): docker build, docker images, docker pull, docker tag, docker image inspect, docker rmi, Dockerfile instructions table (FROM, RUN, COPY, CMD, EXPOSE, WORKDIR, ENV, ENTRYPOINT)
      3. **Docker Containers** (from DOC-03): docker run flags (-d, -p, -e, --name, --rm, -v, --network, --memory, --cpus), docker exec, docker logs, docker stop, docker start, docker rm, docker stats, docker inspect
      4. **Docker Volumes** (from DOC-04): docker volume create/ls/inspect/rm, -v syntax for named volumes and bind mounts, comparison table (named vs bind mount vs tmpfs)
      5. **Docker Networking** (from DOC-05): docker network create/ls/inspect/rm, --network flag, bridge vs host vs none, container DNS resolution, -p port publishing
      6. **Docker Compose** (from DOC-06): docker compose up/down/ps/logs/exec, compose.yml structure (services, volumes, networks), depends_on with condition, environment/env_file, healthcheck syntax, profiles
      7. **Dockerfile Best Practices** (from DOC-07): multi-stage FROM AS pattern, .dockerignore entries, non-root USER instruction, HEALTHCHECK instruction, layer caching order (COPY package.json before COPY .), minimal base images table (alpine/slim/full)

    - What's Next Callout type="tip": "You're now ready for the Foundation Capstone — an open-ended project that combines Linux, networking, and Docker skills. After that, the System Administration module will show you how to manage the services these containers are running."
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/03-docker/08-cheat-sheet.mdx exists
    - 08-cheat-sheet.mdx contains 'moduleSlug: "03-docker"' and 'order: 8'
    - 08-cheat-sheet.mdx contains 'difficulty: "Foundation"' and 'prerequisites: []'
    - 08-cheat-sheet.mdx contains at least 7 QuickReference components
    - 08-cheat-sheet.mdx contains "Callout" with What's Next content
    - npm run build completes without error
  </acceptance_criteria>
  <done>Docker cheat sheet has 7 QuickReference sections covering all lesson topics, follows the Phase 2/3 pattern exactly, and includes a What's Next callout bridging to the capstone and Phase 5.</done>
</task>

<task type="auto">
  <name>Task 2: Foundation capstone MDX, starter code, and verify.sh (CAP-01)</name>
  <files>
    content/modules/03-docker/09-foundation-capstone.mdx
    docker/capstone/api/app.js
    docker/capstone/api/package.json
    docker/capstone/web/index.html
    docker/capstone/web/nginx.conf
    docker/capstone/README.md
    docker/capstone/verify.sh
  </files>
  <read_first>
    .planning/phases/04-docker-foundation-capstone/04-RESEARCH.md
    content/modules/03-docker/08-cheat-sheet.mdx
  </read_first>
  <action>
    1. Create `docker/capstone/` directory structure: api/, web/ subdirectories.

    2. Create `docker/capstone/api/app.js` — minimal Express API (~20 lines):
       - `GET /health` returns `{ status: "healthy" }`
       - `GET /api/items` returns a hardcoded JSON array of items (or queries PostgreSQL if DATABASE_URL env is set)
       - `POST /api/items` accepts `{ name }` and stores it (in-memory array or PostgreSQL)
       - Listens on `process.env.PORT || 3000`
       - This is the STARTER code — learner's job is to Dockerize it, NOT write it

    3. Create `docker/capstone/api/package.json`:
       - name: "capstone-api", version: "1.0.0"
       - main: "app.js", scripts: { "start": "node app.js" }
       - dependencies: { "express": "^4.21.0", "pg": "^8.13.0" }

    4. Create `docker/capstone/web/index.html` — minimal HTML page (~30 lines):
       - Title: "Foundation Capstone"
       - Fetches /api/items from the API and displays them
       - Simple form to POST a new item
       - Uses vanilla JS fetch() — no frameworks

    5. Create `docker/capstone/web/nginx.conf` — nginx config that:
       - Serves static files from /usr/share/nginx/html
       - Reverse proxies /api/* to http://api:3000/api/*
       - This is PROVIDED as a hint — learner may or may not use it
       - Comment explaining the reverse proxy pattern

    6. Create `docker/capstone/README.md`:
       - Project title: "Foundation Capstone: Dockerized Multi-Service App"
       - **The Challenge**: Brief scenario description (same as ExerciseCard scenario)
       - **Architecture**: ASCII diagram showing web → api → db flow with ports
       - **Services**: Table with service name, base image, port, notes for each (web: nginx, api: node:20-alpine, db: postgres:16-alpine)
       - **API Contract**: GET /health, GET /api/items, POST /api/items — request/response formats
       - **Requirements**: Numbered list matching ExerciseCard steps (Dockerfiles, compose.yml, networking, volumes, deploy.sh)
       - **Success Criteria**: Matching VerificationChecklist items
       - **Getting Started**: "The API and web source code are provided. Your job is to write the Dockerfiles, compose.yml, and deploy.sh."
       - **Verification**: "Run `bash docker/capstone/verify.sh` from the project root"
       - Do NOT include implementation hints in the README — hints are in the MDX VerificationChecklist only

    7. Create `docker/capstone/verify.sh`:
       - Follow the EXACT pattern from 04-RESEARCH.md (the full verify.sh example)
       - Preamble: check python3 exists (`command -v python3 >/dev/null || { echo "python3 required"; exit 1; }`)
       - COMPOSE_FILE="docker/capstone/compose.yml"
       - Checks (7 total, matching VerificationChecklist):
         a) All three services running (web, api, db) — use docker compose ps --format json with python3 JSON parsing, || echo "missing" fallback
         b) Web service accessible on localhost:8080 — curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/
         c) API responds via web proxy — docker compose exec -T web curl -s http://api:3000/health (with -T flag!)
         d) Database data persists across restart — docker compose restart db, then check API still returns data
         e) API runs as non-root — docker compose exec -T api id -u returns non-zero
         f) deploy.sh exists and is executable — [ -x docker/capstone/deploy.sh ]
         g) All services on same custom network — docker network inspect shows capstone network with 3 containers
       - Every FAIL message includes diagnostic hint and exact command to investigate
       - Summary: RESULT: PASS (all N checks) or RESULT: FAIL (N of M failed)
       - Exit 1 on any failures
       - Make executable: chmod +x

    8. Create `content/modules/03-docker/09-foundation-capstone.mdx`:
       - Frontmatter: title: "Foundation Capstone: Deploy a Dockerized App", description: "Integrate Linux, networking, and Docker skills to deploy a multi-container application — no step-by-step guidance", module: "Docker & Containerization", moduleSlug: "03-docker", lessonSlug: "09-foundation-capstone", order: 9, difficulty: "Challenge", estimatedMinutes: 90, prerequisites: ["03-docker/06-docker-compose", "03-docker/07-dockerfile-best-practices", "01-linux-fundamentals/07-shell-scripting"], tags: ["capstone", "project", "docker-compose", "dockerfile", "shell-scripting", "integration"]

       - ## Overview: "This is your first real project. No step-by-step guide. No hand-holding." Explain the capstone concept: integrate everything from Modules 1-3 (Linux, networking, Docker) into a working deployment. Callout type="tip" reassuring learner that everything they need was covered in the previous lessons.

       - ## The Challenge: Describe the scenario (new DevOps engineer, first task: Dockerize a three-tier app). Reference docker/capstone/README.md for the full project brief.

       - ## Architecture: ASCII diagram showing: Browser → nginx (web, :8080) → Express API (api, :3000) → PostgreSQL (db, :5432). Explain each service's role.

       - ## Project Requirements: ExerciseCard with difficulty="Challenge" following the EXACT pattern from 04-RESEARCH.md:
         - scenario: real-world DevOps engineer scenario
         - objective: deploy production-style multi-service Docker Compose app
         - steps: 6 REQUIREMENTS (not instructions!) — write API Dockerfile (non-root), write web Dockerfile (multi-stage, under 100MB), create compose.yml (custom bridge network, API not externally accessible), configure PostgreSQL named volume, write deploy.sh (build + start + health status, exit non-zero on failure), run verify.sh
         - VerificationChecklist children with 7 items and hints (from RESEARCH.md example pattern)

       - ## Getting Started: Brief pointers — "Start by reading docker/capstone/README.md", "The API and web source code are provided in docker/capstone/", "You write the Dockerfiles, compose.yml, and deploy.sh"

       - ## What You're Practicing: Callout listing skills from each module: Linux (shell scripting, file permissions, processes), Networking (DNS resolution, port mapping, connectivity testing), Docker (Dockerfiles, Compose, volumes, networking, best practices)

       - No Quick Reference section — this is a project, not a lesson
  </action>
  <verify>
    <automated>cd /Users/phillipdougherty/learn-systems && bash -n docker/capstone/verify.sh && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - content/modules/03-docker/09-foundation-capstone.mdx exists
    - 09-foundation-capstone.mdx contains 'moduleSlug: "03-docker"' and 'order: 9' and 'difficulty: "Challenge"'
    - 09-foundation-capstone.mdx contains "ExerciseCard" and "VerificationChecklist"
    - 09-foundation-capstone.mdx contains "01-linux-fundamentals/07-shell-scripting" in prerequisites
    - 09-foundation-capstone.mdx does NOT contain step-by-step implementation instructions (steps are requirements)
    - docker/capstone/verify.sh exists, is executable, and passes bash -n syntax check
    - docker/capstone/verify.sh contains "docker compose" (not "docker-compose")
    - docker/capstone/verify.sh contains "exec -T" for in-script exec commands
    - docker/capstone/verify.sh contains "$((PASS + 1))" (not "((PASS++))")
    - docker/capstone/verify.sh contains "python3" for JSON parsing
    - docker/capstone/api/app.js exists and contains "express" and "/health" and "/api/items"
    - docker/capstone/api/package.json exists and contains "capstone-api"
    - docker/capstone/web/index.html exists
    - docker/capstone/web/nginx.conf exists and contains "proxy_pass"
    - docker/capstone/README.md exists and contains "Foundation Capstone"
    - npm run build completes without error
  </acceptance_criteria>
  <done>Foundation Capstone is complete: open-ended project brief MDX with ExerciseCard (requirements, not instructions) and VerificationChecklist with hints. Starter code provided (api/app.js, web/index.html, web/nginx.conf). Comprehensive verify.sh tests the entire deployed stack with 7 PASS/FAIL checks. README.md has API contract, architecture diagram, and requirements. Learner creates Dockerfiles, compose.yml, and deploy.sh.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (all 9 Docker MDX files compile)
- `npm test` passes (9 Docker lessons asserted by integration test from Plan 03)
- `bash -n docker/capstone/verify.sh` passes syntax check
- Cheat sheet has 7 QuickReference sections
- Capstone MDX has ExerciseCard with requirements-only steps
- Capstone verify.sh follows all anti-patterns from research (no ((PASS++)), no docker-compose, exec -T, python3 for JSON)
</verification>

<success_criteria>
- Docker cheat sheet covers all 7 lesson topics in QuickReference format
- Capstone project brief is open-ended (requirements, not step-by-step instructions)
- Capstone starter code (API + web) is minimal — learner writes Dockerfiles and orchestration
- verify.sh is the most comprehensive verification in the course (7 checks covering services, networking, persistence, security, automation)
- All 9 Docker module lessons build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-docker-foundation-capstone/04-04-SUMMARY.md`
</output>
